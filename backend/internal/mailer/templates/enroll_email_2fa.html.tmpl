{{- /*
Code shape. Enrolment and sign-in look alike on purpose — the reader is doing
the same physical thing — and what tells them apart is the label and the copy,
which is where the difference actually lives.
*/ -}}
{{- define "enroll_email_2fa" -}}
{{- template "chrome.shape_code" (dict "Doc" . "Accent" "#6f6bd8" "Tint" "#f0efff") -}}
{{- end -}}
