{{- /*
Action shape. The link is the credential: it exists in this message and nowhere
else, because the table keeps only a sha256 of it. The URL box under the button
is what lets the reader check the host before clicking.
*/ -}}
{{- define "password_reset" -}}
{{- template "chrome.shape_action" (dict "Doc" . "Accent" "#6f6bd8" "Tint" "#f0efff") -}}
{{- end -}}
