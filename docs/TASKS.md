# Foldex — Tasks

## Como ler

Lista faseada de tasks `T1..T30`. Cada fase desbloqueia a próxima — segue em ordem. A coluna **Status** é atualizada no final de cada task com `✅ done` + data + hash de commit (quando aplicável). `🚧` = em andamento, `⏳` = bloqueado, vazio = pending.

> Convenções: backend = Go (`/backend`), web = Vite/React (`/web`), ext = MV3 (`/extension`).

---

## Fase 0 — Infra

| #   | Task                                                                                          | Status |
|-----|-----------------------------------------------------------------------------------------------|--------|
| T1  | Init repo + `Makefile` + `.env.example` + `docker-compose.yml` (Postgres 16 + healthcheck).   | ✅     |
| T2  | Scaffold `docs/VISION.md`, `docs/ARCHITECTURE.md`, `docs/TASKS.md`.                           | ✅     |
| T3  | Backend `Dockerfile` + `go.mod` + `cmd/server/main.go` + `internal/server/router.go` com `/healthz`. | ✅     |
| T4  | Web `Dockerfile` (multi-stage → nginx) + `nginx.conf` + Vite scaffold servindo "Hello Foldex". | ✅     |
| T5  | `docker compose up` smoke E2E (backend + db + web sobem; `/healthz` 200; SPA carrega).        |        |

## Fase 1 — Backend core

| #   | Task                                                                                          | Status |
|-----|-----------------------------------------------------------------------------------------------|--------|
| T6  | `internal/config/` env loader; `internal/db/` pgxpool init + ping; graceful shutdown em `main.go`. | ✅     |
| T7  | Migration `000001_init.up/down.sql` (tag, link, link_tag, índices, pg_trgm). Makefile `migrate-up`. | ✅     |
| T8  | `internal/tags` CRUD: handler/service/repository/model/dto.                                   | ✅     |
| T9  | `internal/links` CRUD + filtros (`q`, `tag`, `sort`) + set de tags via PATCH.                  | ✅     |
| T10 | `internal/redirect` `/go/:id` com `UPDATE ... RETURNING url` e 302.                            | ✅     |
| T11 | Testes de repositório com `testcontainers-go` (tags, links, redirect).                         |        |

## Fase 2 — Frontend core

| #   | Task                                                                                          | Status |
|-----|-----------------------------------------------------------------------------------------------|--------|
| T12 | MUI dark theme + TanStack Query + axios client tipado (`web/src/api/`).                       | ✅     |
| T13 | `LinkGrid` + `LinkCard` + `TagSidebar` + chips de filtro.                                      | ✅     |
| T14 | `NewLinkDialog` com Autocomplete de tags (create-on-the-fly).                                  | ✅     |
| T15 | `CommandPalette` (⌘K) + atalho ⌘N pra `NewLinkDialog`.                                          | ✅     |
| T16 | Edit/delete flows com optimistic updates.                                                      | ✅     |

## Fase 3 — Preview worker

| #   | Task                                                                                          | Status |
|-----|-----------------------------------------------------------------------------------------------|--------|
| T17 | `internal/preview/fetcher.go`: `http.Client` 5s + parser `<title>`, OG, favicon. SSRF guard.   | ✅     |
| T18 | `internal/preview/worker.go`: pool N workers (env `PREVIEW_WORKER_CONCURRENCY`).               | ✅     |
| T19 | Hook em `links.Create` (enqueue) + endpoint `POST /api/links/:id/refresh-preview`.             | ✅     |
| T20 | Front: badge `preview_status` + botão retry no `LinkCard`.                                     | ✅     |

## Fase 4 — Import/Export

| #   | Task                                                                                          | Status |
|-----|-----------------------------------------------------------------------------------------------|--------|
| T21 | Netscape HTML parser (`golang.org/x/net/html`) → insert por URL.                               | ✅     |
| T22 | JSON format versionado `{ version:1, tags, links }` (import + export).                         | ✅     |
| T23 | `ImportPage` com drag-drop + botão Export (Netscape + JSON).                                   | ✅     |

## Fase 5 — Browser extension

| #   | Task                                                                                          | Status |
|-----|-----------------------------------------------------------------------------------------------|--------|
| T24 | MV3 manifest + Vite + `@crxjs/vite-plugin` + popup React + options page.                       | ✅     |
| T25 | Popup form: prefill URL/título da aba, tag picker, save → `POST /api/links`.                   | ✅     |
| T26 | UX "save & close" + toast + persistência da base URL em `chrome.storage.local`.                | ✅     |

## Fase 6 — Polish

| #   | Task                                                                                          | Status |
|-----|-----------------------------------------------------------------------------------------------|--------|
| T27 | README quickstart + seção screenshots (placeholder) + gif demo (placeholder).                  | ✅     |
| T28 | `scripts/seed.sql` com tags (Jira, Dashboard, Docs) + 5 links de exemplo.                       | ✅     |
| T29 | `scripts/backup.sh` (`pg_dump` template + comentário sobre cron).                               | ✅     |
| T30 | Atualizar este doc com log de conclusão + lessons learned.                                     |        |

---

## Log de conclusão

| Data       | Task   | Hash | Notas |
|------------|--------|------|-------|
| 2026-08-29 | **publish-manifest cancelava o próprio matrix** | — | O grupo `${{ github.workflow }}-manifest` (PR #95) era um grupo só para o job. As três células (backend, web, backup-agent) entram nele juntas; o GitHub cancela as pendentes quando a próxima entra na fila (`Canceling since a higher priority waiting request for release-manifest exists`). Run 33258546278: os seis builds por digest passaram, a célula backend do manifest morreu em 1s, create-release-tag nem chegou. O grupo passou a ser `-manifest-${{ matrix.image.name }}`: serializa duas publicações da MESMA imagem disputando `:latest`, e deixa as três células do mesmo run se sobreporem — elas escrevem tags diferentes (`:latest`, `:latest-backup-agent`, o repo web). Guard em `test-release-gate.sh`. |
| 2026-08-29 | **Release v2.18.0 publicou as imagens e não criou a tag** | — | `publish-manifest` do backup-agent criou `justoeu/foldex-backend:sha-5212079-backup-agent` e o passo seguinte inspecionou `…:sha-5212079` — a tag do backend, no mesmo repositório, que a célula irmã talvez ainda não tivesse escrito. Falhou `not found` depois do `imagetools create` ter dado certo, e `create-release-tag` (que depende do matrix inteiro) foi skipped: imagens no Hub, git tag ausente. O inspect passa a interpolar `matrix.image.suffix`. De quebra, `flavor.latest=false`: `latest=auto` mintava `:latest` em toda célula semver, duplicando o `type=raw` (gated em `publish_latest`) — no log do mesmo run `latest-backup-agent` saía duas vezes. Guard em `test-release-gate.sh`; mutante que tira o suffix do IMAGE_REF morre. Re-disparar `v2.18.0` depois do merge: a tag git ainda não existe, então o validator aceita. |
| 2026-08-27 | **A agenda diz para ONDE cada job manda** | — | Perguntado por quem opera, olhando o card do espelho: "copia os objetos do RustFS para o bucket externo — para onde ele copia?". A tela não respondia, e a resposta importava: naquela instância `RUSTFS_ENDPOINT` e `BACKUP_S3_ENDPOINT` são o MESMO host, mudando só o bucket — um espelho que não sobrevive a nada que ele existe para cobrir, e nada na tela dizia isso. O heartbeat passou a carregar por job um `destination` `{endpoint, bucket, prefix}` e o editor de agenda o renderiza sob a descrição. Os quatro jobs falam com o bucket, cada um sob o seu prefixo (`backups/dump/` para dump e drill — o drill RESTAURA o que o dump escreveu —, `backups/rustfs/`, `backups/users/`), e um job incapaz também publica o seu: onde ele mandaria é parte da configuração, e um job que não roda é exatamente quando se confere isso. São os três campos NÃO secretos, de propósito: o INV-171 ganhou o corolário "o ENDEREÇO é publicado, o ACESSO nunca" e um guard (`TestAgent_HeartbeatCarriesNoCredential`) que lê a linha crua do heartbeat e recusa qualquer uma das quatro chaves dentro dela — tudo o que esse payload carrega está numa tela. O README afirmava que "a env do agente não é exposta pela API", o que deixou de ser inteiramente verdade; corrigido nos dois idiomas em vez de deixar a frase envelhecer. Layout medido em Chrome real com um endpoint deliberadamente absurdo (58 caracteres): quebra em duas linhas dentro do card, sem estourar a coluna nem rolar a página, com `min-width: 0` na coluna do head, que é o que permite a quebra em vez do empurrão. |
| 2026-08-27 | **Login → pedido do OTP apagava a viewport inteira** | — | Reportado por quem usa: entre "Entrar" e o pedido do código a página piscava 100% em vez de trocar o card. Causa: o `AuthGate` carrega `TwoFactorScreen`/`EnrollTotpScreen` por `lazy()`, e o fallback do `Suspense` é o `boot` — `.fx-auth fx-auth-boot`, sobreposição de tela cheia. Para as outras quatro telas lazy o fallback é honesto (entram no MOUNT, por token na URL, sem nada pintado); as duas de desafio entram por TRANSIÇÃO a partir do formulário já pintado, e como atualização urgente o React commita o fallback. Fix em duas partes, ambas necessárias e cada uma provada por mutação: `startTransition` na troca de sessão (o React SEGURA a tela anterior enquanto a nova suspende — sem isso, chunk lento continua apagando) e aquecimento dos dois chunks enquanto o formulário está na tela (torna a espera imperceptível, e o segundo fator é o DEFAULT, então quem olha o formulário está a um submit dali). Prefetch sozinho não bastaria: `React.lazy` chama a fábrica no primeiro render e anexa um `then`, então suspende ao menos uma vez mesmo com o módulo em cache. Registrado como INV-174. O review achou duas coisas que o fix original errou e as duas foram pagas na hora: (1) a regra classificava as telas por "token na URL" quando o critério real é "havia algo pintado?" — a `ForgotScreen` entra por `useState` local do gate, a um clique do formulário, e tinha exatamente o mesmo defeito (fix: prefetch + transição própria no `onForgotPassword`, com o vermelho reproduzindo o mesmo `Loading…` de tela cheia); (2) a transição estava larga demais — o `queryClient.clear()` do `applySession` é NÃO-escopado e o argumento que o torna seguro é a árvore desmontar no MESMO commit, o que só vale enquanto a atualização é urgente (fix: adiar só o status `two_factor_required`, o único que jamais coincide com um clear porque não carrega user id, fixado por teste e provado por mutação). A alegação de vazamento cross-tab do relatório não se sustentou: naquele caminho o `clear()` já está separado do flip por um `await fetchMe()` inteiro, então a janela é anterior ao change e dominada pelo round-trip — o estreitamento foi feito pelo argumento da mesma aba, não por aquele. O teste começou mockando o módulo com uma fábrica pendente e o mock não segurava o `React.lazy` no registro do Vitest — passou a modelar a suspensão em React, que é sobre o que a asserção fala. Achado de tabela no caminho: `COMPOSE_APP := docker compose -f docker-compose.yml` — o `-f` explícito desliga o pickup automático do `docker-compose.override.yml`, então todo `make up*` recriava o backup-agent sem a identidade age e ele entrava em crashloop. |
| 2026-08-27 | **ADR-45: um vocabulário de agendamento para os quatro jobs de backup, e a agenda que a env semeia** | — | O ADR-44 tinha dado a cada job uma gramática própria (`dump` só `times[]`, `drill` só `time`+`weekday` de UM dia, `mirror` só `interval_min`, `user_zip` só `enabled`+`time`), e a validação recusava campo "estrangeiro": nenhum job escolhia um CONJUNTO de dias, não existia "segunda, quarta e sexta", e o formulário precisava de quatro componentes de campos. A síntese: a segurança do ADR-44 está nos PISOS, não na pobreza da forma. Documento único `{mode, times[], weekdays[], interval_min, enabled}` com `mode` EXPLÍCITO (linha com `times` e `interval_min` juntos seria meio-honrada em silêncio); pisos assimétricos — 1..6 horários, intervalo 15..1440 e ≥1 dia para todos, **≥5 dias para o `dump`** (a proteção da instância, não conveniência), `user_zip` o único deslígável. A matemática subiu de `Anchor` (agora só o parser da sintaxe da env) para `Timing`, cujo `MaxGap` enumera a grade da semana e SUBSTITUI os três casos especiais — um horário num único dia dá 7 dias naturalmente. O heartbeat passou a publicar a baseline da env ESTRUTURADA, que é o que faz "a env é a primeira opção, o banco é a sobrescrita" existir na tela e não só no documento. Migração 000043 + `RequiredSchemaVersion` 42→43 nos dois gates. Na UI: um editor só, dias multisseleção com atalhos, segmentado de modo, e a agenda centralizada com foco ao clicar num card (`useRevealTarget`, rAF + `scrollIntoView({block:'center'})`, `behavior:'auto'` sob reduced-motion) — as duas colunas passaram a terminar na mesma linha (`stretch` + `flex:1` no último card da lateral), conferido no navegador em 510px/451px/564px iguais nos quatro jobs. Execução em worktrees paralelos (backend/frontend/docs) com red-to-green obrigatório. O sweep de 5 agentes não achou HIGH nenhum e achou um BLOCKER + 12 achados reais, todos pagos na hora: (1) **blocker** — a chave de remontagem do editor não incluía a CHEGADA da baseline, então o primeiro fetch com `agent: null` semeava das constantes da tela e nenhum poll corrigia: quem salvasse sem tocar em nada gravava a opinião da tela por cima da agenda da env, exatamente o defeito que a mudança existia para matar; (2) a migração e o `normalized` DISCORDAVAM em três formas legadas — provado com red-to-green de verdade, rodando a migração ANTIGA contra 16 linhas legadas num Postgres descartável: 7 divergências, entre elas `{"enabled":false,"times":["02:30"]}` no `user_zip` voltando COM agenda e SEM `enabled`, isto é, um job que o operador desligou explicitamente voltando a rodar; (3) rascunho magro deixava campos vazios ao religar o `user_zip` ou trocar de modo → 400 garantido (fix: rascunho GORDO por construção, `payloadOf` canonicaliza no envio, e o `switchMode` perdeu seus defaults concorrentes); (4) a baseline da env pode estar ABAIXO do piso (`BACKUP_DUMP_AT="03:30 sun"` é legal na env e ilegal como linha) e o formulário semeava dela → alargamento para os sete dias mantendo os horários, a única correção automática segura porque aumenta a proteção; (5) duas fronteiras de `memo` que não fechavam sob o poll de 60s — `runNow` dependia do objeto do `useMutation` (identidade nova a cada render, derrubando os quatro `JobCard`) e o `ScheduleCard` dependia de `data` inteiro, cujo `seen_at` avança a cada heartbeat (fix: props estreitadas; provado com spies de passagem em `useCopy` e `formatMinutes`, cada um dentro de exatamente uma subárvore memoizada, e com assert de que o poll REALMENTE aconteceu — a primeira versão do teste passava pelo motivo errado); (6) as mensagens de recusa do mock tinham derivado das reais e dois testes as afirmavam como "as palavras do servidor" — provavam que a UI renderiza *uma* string, nunca o contrato; (7) todo negativo de `TestValidateJobConfig_FloorsHold` era `assert.Error` pelado, então recusa pelo motivo ERRADO passava (provado por mutação: troquei a mensagem de weekday inválido por "repeats" e a tabela antiga passou); (8) o ramo de máximo interior do `MaxGap` era a ÚNICA linha descoberta do arquivo — justamente a borda que a reescrita introduziu; (9) `down.sql` sem teste nenhum; (10) trilha de auditoria sem payload, agora com documento anterior + novo no PUT e no DELETE (INV-047); (11) render de `times` ilimitado a partir de linha escrita à mão (clampado na SEMENTE, não só no render, senão a tela mostra 6 e o servidor recusa 9); (12) o segmentado de modo era um radiogroup vestido de `role="tablist"`. Registrados como aceitos/follow-up: `RequiredSchemaVersion` é PISO, não igualdade — agente antigo sobe contra schema 43 e ignora `weekdays` em silêncio (roda MAIS vezes, direção segura, mas sem log; fechar pede comparar a versão do heartbeat na banda); `ScheduleFields` com 234 linhas pede split dos três pickers quando o arquivo crescer de novo. Validação de layout feita com o harness de CSS (dump da DOM real + bundle de produção servido num http.server), porque a tela exige sessão e não há como autenticar daqui — o clique-a-clique no app real fica para o usuário. |
| 2026-08-26 | **Remodelagem da tela "Política de senha e acesso"** | — | De pilha única de campos para três seções lado-a-lado (o-que-isto-governa à esquerda, controles à direita, separadores) — Credenciais, Acesso de administradores, Entrar com Google — com hints por campo, switch `fx-toggle-row` no auto-provision e divulgação progressiva (o papel das contas novas só aparece com o provisionamento LIGADO: um default para contas que não podem nascer é um controle que não responde nada). O sweep pagou de novo em remodel "só visual": (1) o switch estilizado dentro de `fieldset disabled` ficava com cara 100% ativa na visão read-only do admin — o checkbox nativo o UA acinzenta, a casca CSS não (fix: `:has(input:disabled)` espelha o estado); (2) o checkbox real do toggle é `opacity:0` e focável, e NÃO havia anel de foco — navegação por Tab pousava no switch sem indicação nenhuma, padrão herdado do LinkDialog que o remodel ia propagar (fix na classe compartilhada: `:has(input:focus-visible)`, conserta as duas telas). Teste read-only agora asserta `toBeDisabled()` no mecanismo (fieldset), não só a ausência do botão Salvar. |
| 2026-08-26 | **PR6 do ADR-44: agenda de backup configurável na UI, com pisos que a configuração não alcança** | — | Migração 000042 (`backup_schedule` linha-por-job + `backup_agent_state` heartbeat) + `backupagent/schedule.go` (JobConfig, `ValidateJobConfig` com os pisos, `Timing` multi-âncora com MaxGap/Due/PreviousSlot, ScheduleStore) + agent.go reescrito para agenda VIVA (snapshot de Timings sob RWMutex, canal de mudança fechado a cada swap, scheduleLoop unificado âncora+intervalo, sync ~30s que recarrega agenda e grava heartbeat) + permissão `instance.backup_schedule` TRAVADA owner-only + `GET/PUT/DELETE /api/admin/backup/schedule[/{job}]` com auditoria própria + editor na banda (badges env/banco, motivo traduzido para job incapaz, confirmação quando a mudança REDUZ proteção via `reducesProtection` puro). Princípio = INV-173: env decide QUAIS jobs existem (credencial/identidade), o banco decide QUANDO rodam, e os pisos (dump 1..6×/dia nunca off; drill sempre semanal; mirror 15..1440min nunca off; user_zip é o único deslígável) valem nos DOIS lados — o PUT e o load do agente chamam a MESMA função, então linha escrita por SQL direto degrada à baseline. Sweep+triple review acharam e o fix pagou na hora: (1) race real de ordem — `timing()` lido antes de `changeCh()` deixava um swap entre as duas leituras fechar um canal que ninguém segurava; job desabilitado→habilitado dormia até a PRÓXIMA edição (fix: inverter a ordem; teste `TestScheduleLoop_WakesAJobEnabledAfterStart`); (2) mirror-off ficava FORA do heartbeat e a UI oferecia editores para linha que nenhum processo leria — a lição do mailer dentro da feature que existe para evitá-la (fix: agentState força os 4 jobs); (3) banner de agente parado comparava `Date.now()` contra `seen_at` cacheado sem `refetchInterval` — qualquer rerender após 2min no ar acusava agente morto vivo (fix: refetch 60s); (4) `intervalStart` — edições frequentes de agenda reiniciavam a contagem de um intervalo que nunca disparou. Registrados como aceitos: âncoras do dump mais próximas que a duração do job são absorvidas pelo run em andamento (serial por job — N horários é teto, não promessa); advisory lock único entre jobs (skip-don't-wait, ADR-43); `updated_by_email` visível a quem tem `instance.backup`. |
| 2026-08-26 | **Compose do v2.16.0 não parseava — e nenhum guard olhava** | — | O serviço `backup` shipou com o bloco `RUSTFS_*` duplicado (merge dos agentes paralelos do PR3/PR4): chave YAML duplicada é PARSE ERROR no compose v2, então o `docker-compose.yml` inteiro recusava carregar — `make up` quebrado para todo operador, com CI verde, porque todo guard que lê esse arquivo o lê com grep/awk (que atravessa chave duplicada sem ver) e `docker compose config` não rodava em lugar nenhum. Fix: dedupe (blocos byte-idênticos, nenhuma env perdida — conferido contra o que `backupagent/config.go` lê) + guard novo `scripts/test-compose-config.sh` no ci.yml: `docker compose config --no-interpolate --quiet` sobre `docker-compose*.yml` (o `--no-interpolate` preserva os gates `${VAR:?}` do services.yml para usuários reais sem disparar num runner sem .env; detecção de chave duplicada acontece ANTES da interpolação), com self-test que prova que o guard sabe falhar e red-before confirmado contra o HEAD quebrado. Lição: a stack de produção local rodava o binário antigo — um parse error no compose só aparece no PRÓXIMO `docker compose` de alguém, que foi exatamente como apareceu (ao habilitar o profile `backup`). |
| 2026-08-26 | **ADR-43 completo: os 5 PRs do backup operacional em produção** | — | PR1 dump (#86, v2.12.0) → PR2 drill (#87, v2.13.0) → PR3 mirror (#88, v2.14.0) → PR4 user_zip (#89, v2.15.0) → PR5 status (#90, v2.16.0). Migrações 000040/000041 aplicadas e VERIFICADAS no Postgres real (genfin, db foldex; RequiredSchemaVersion 41). PR2/3/4 implementados por três agentes em worktrees paralelas e integrados em série com sweep §9 completo cada; blockers de sweep dignos de nota: contagens fora do snapshot do pg_dump (fix: pg_export_snapshot + --snapshot), mídia espelhada em CLARO (fix: age por objeto), traversal via sufixo de restore (fix: gramática no linkObjectID + skip no espelho), origem com storage.New criando bucket de typo (fix: NewReadOnly). Follow-ups abertos: alerta e-mail via outbox (SDD §9.3), helper spool→age→sha→upload (3 variantes), catch-up de intervalo sem seam de jitter, screenshot da banda nos READMEs, validação da UI em navegador real. |
| 2026-08-26 | **PR2 do ADR-43: restore drill do backup operacional** | — | Job `drill` no registry table-driven do agente: baixa o dump REAL do S3 (`Uploader.OpenObject`), confere sha256 do ciphertext contra o registrado, decifra com `BACKUP_AGE_IDENTITY_FILE` (postura keyfile: sem autogenerate, arquivo ruim falha o BOOT), restaura num Postgres efêmero dentro do container (initdb/pg_ctl/createdb/pg_restore via seam `command` — precedente do dump) e compara contagens por tabela + `schema_migrations.version` com o `meta` que o dump agora grava ANTES do `pg_dump`. `drill_of_run_id` carimbado na escolha da fonte (o run que falha no meio ainda diz qual dump validava). Razões novas normalizadas (`drill_*`); teardown incondicional em ctx fresco. `BACKUP_DRILL_AT` default vazio = OFF (divergência deliberada do SDD: ligar por default exigiria a identidade privada em todo host). Followup do PR1 pago: `Anchor.PreviousSlot` extraído com teste próprio. Lições: (1) homebrew libpq tem `initdb` SEM o servidor — o gate do teste e2e precisa de `exec.LookPath("postgres")` além dos clients; (2) `pg_ctl`/clients herdariam um `PGPORT` perdido do ambiente — porta explícita nos dois lados do socket; (3) `compareCounts` puro porque o cluster stubado não responde SQL. `.env.example` NÃO atualizado (bloqueado pelo tooling da sessão) — pendência anotada. |
| 2026-08-26 | **PR3 do ADR-43: job `mirror` — espelho incremental RustFS→S3** | — | `ObjectInfo` (`internal/storage` + cópia do backupagent) ganhou `ETag`/`LastModified` (minio-go já entrega no ListObjects, custo zero); `mirror.go` lista origem e destino e copia por `mirrorDelta` — chave ausente, tamanho diferente, ou `LastModified >= watermark` (último sucesso − 1h de overlap); **ETag nunca é critério** (§11.6: etag multipart depende do part size → re-cópia eterna) e o teste carrega o caso mutante que mata quem tentar. Deleções NÃO se propagam (sem flag no v1). Cópia via `OpenObject`→`PutObjectStream` num pool fail-fast (teto `resourcebudget.BackgroundWorkerConcurrency`); origem é um SEGUNDO `storage.Client` apontado ao RustFS pelas mesmas env `RUSTFS_*` do backend, obrigatórias só com o mirror ligado (`BACKUP_MIRROR_INTERVAL_MIN`, default 360, 0 desliga). `restoreInFlight` ganhou seu primeiro caller de produção: o run espera até 10 min (probe/1 min, injetável em teste) e só então falha `restore_in_flight` — razão operacional excluída de `ConsecutiveFailures`, scheduler intocado. `jobSpec` ganhou `interval` como alternativa à âncora (ticker + catch-up por `intervalDue`, função pura); `RunStore.Succeed` grava `objects_scanned/objects_copied/bytes_copied` via `Artifact.Mirror` (`MirrorStats`), sem mexer na assinatura de `jobSpec.run`. Divergências devolvidas ao SDD §5.3: espera-no-job em vez de "reagenda +10 min"; `BACKUP_MIRROR_DELETE` explicitado como futuro, não implementado. O sweep §9 endureceu o job na mesma entrega: objetos passam a sair CIFRADOS por objeto (`<key>.age`, mesmos recipients do dump — a mídia é o único payload que o dump cifrado não carrega, e antes ia em claro; diff por tamanho desligado no modo cifrado, ciphertext ≠ source por construção), chave com componente `..` é pulada com aviso (e o sufixo do `linkObjectID` no `internal/backup` ganhou gramática `^\.[A-Za-z0-9]{1,16}$` — um ZIP artesanal não planta mais material de traversal que o espelho copiaria), a origem usa `storage.NewReadOnly` (typo no bucket falha o boot em vez de criar um bucket vazio espelhado "com sucesso" para sempre), `New` recusa mirror habilitado sem origem, e a gauge de bytes passou a dizer `BytesCopied`. `.env.example` atualizado no próprio commit. |
| 2026-08-26 | **PR1 do ADR-43: backup-agent com dump cifrado agendado** | — | Quarto binário `cmd/backup-agent` (imagem própria `postgres:18.4-alpine` + Go, estágio `backup-agent`, profile compose `backup`): scheduler por âncora HH:MM com catch-up decidido por `backup_run` (mig. 000040 — molde do mail_outbox, sem attempts: run falho é LINHA, retry é o próximo slot), pg_dump -Fc → age → sha256 do ciphertext → spool 0600 → S3 externo, retenção GFS com modo `bucket` endurecido, métricas Prometheus + alert rules versionadas (`deploy/observability/`) amarradas ao registry por teste (`TestAlertRulesOnlyReference…`). `scripts/backup.sh` REMOVIDO — estava quebrado desde a separação dos compose files (`exec db` sem `-f`) e era o único 'backup' documentado. Lições: (1) o probe de advisory lock precisa de UMA conexão pinada — adquirir e soltar pelo pool cai em sessões diferentes e o unlock falha em silêncio; (2) `schema_migrations` não existe no testdb (que aplica .sql direto), então o gate do agente se testa criando a tabela como o migrate faria; (3) o recorder de GFS precisa de DOIS dias para ter vítima — um dia só é sempre o próprio slot diário. INV-171/172 novos. RequiredSchemaVersion NÃO bumpou (o backend ainda não lê backup_run; bump no PR5). |
| 2026-08-25 | **SDD do backup operacional agendado (ADR-43)** | — | Escrito `docs/SDD-OPS-BACKUP.md` + ADR-43: quarto binário `cmd/backup-agent` (imagem `postgres:18.4-alpine` + Go, profile `backup`), quatro jobs (dump -Fc cifrado com age → S3 externo, restore drill em Postgres efêmero in-container, mirror RustFS→S3 por watermark sem propagar delete, user_zip agendado), estado em `backup_run` (mig. 000040, molde mail_outbox, índice parcial único + advisory lock `FOLDXBKP`), retenção GFS com modo endurecido, monitoria em três canais (métricas no agente + dashboard/alertas Grafana versionados em `deploy/observability/` com regra `absent()` — a lição do incidente do mailer — + banda no settings hub atrás de `instance.backup` + e-mail via outbox), faseado em 5 PRs (PR2-4 paralelizáveis em worktrees). Achados no caminho: `scripts/backup.sh` está QUEBRADO desde a separação dos compose files (`exec db` sem `-f docker-compose.db.yml`) e será removido no PR1; ETag multipart não serve de critério de diff de espelho (depende do part size → re-cópia eterna); `EnqueueTx` exige `AUTH_ENCRYPTION_KEY` no processo que enfileira. Implementação pendente — só documentação neste change. |
| 2026-08-24 | **Sweep do ADR-42: o mesmo defeito em três níveis** | — | O sweep achou 4 HIGH de revisão + 5 de qualidade de teste, e os piores são a MESMA falha com uma indireção a mais entre o gate e a fonte viva. (1) `RequirePermission(p)` sem parâmetro: um mount site esquecido aplica a compilada em silêncio — foi o que a assinatura nova resolveu. (2) `AdminHandler.Mount` capturando `Grants()` como VALOR: os gates de `/api/admin` congelavam no snapshot do boot, então uma revogação só valia após reiniciar. (3) `cmd/server/main.go` sem `Deps.Grants`: o router inteiro caía em `roleperm.Default()`, e `/links`, `/notes`, `/tags`, `/import` e `/backup/restore` seguiam na compilada — enquanto os gates que `main` conecta à mão (admin, folders, policy) HONRAVAM, deixando a revogação PARCIALMENTE aplicada, o que se lê como instabilidade e não como bug. O parâmetro obrigatório resolveu o nível 1 e me deu confiança falsa nos níveis 2 e 3. **Três permissões (`backup.export`, `invites.read`, `invites.write`) eram oferecidas pela matriz e nenhuma rota aplicava** — lacuna de documentação enquanto a matriz era compilada, toggle que salva/audita/não faz nada depois de editável, contradizendo o comentário do próprio `permissions.go`. **`policy.Put` chamava `Validate` e não `ValidateForWrite`**, então o piso acima de 72 virava 500 em vez do 400 que ensina o limite. **`useCopy` com booleano** deixava o botão dizendo Copiado sobre outro segredo — corrigido derivando `copied` do valor, não chamando o `reset` que ninguém chamava. Também: `ValidateWrite` não checava o chamador com conjunto vazio; lost update no DELETE-then-INSERT (advisory lock por papel); e não existia o Load periódico que o comentário citava. |
| 2026-08-24 | **Limpar duplicação quebrou autorização** | — | Colapsar as quatro cópias de `if grants == nil` num `OrDefault` trocou quatro comparações CONCRETAS corretas por comparações de INTERFACE: um `*roleperm.Repository` nulo dentro de uma interface não é `nil`, então a função devolvia o typed nil e o gate de admin dava panic no primeiro request de qualquer handler construído sem store — todo teste que não se importa com permissões configuradas, e qualquer deploy onde o store falhe. Não apareceu em `go build`, `go vet`, nos testes de unidade do `roleperm` nem no pacote `auth`: só a integração completa, no pacote `server`, tocou o caminho. É o argumento concreto para o gate do §6.1. A correção não é voltar às quatro cópias — é a função tratar o typed nil, que é exatamente o que quatro call sites não deveriam ter de lembrar. |
| 2026-08-24 | **Dois guards meus passavam por engano** | — | `TestServerDepsCarriesTheLiveGrants` afirmava que o campo `Grants` estava PRESENTE, não que carregava um store vivo: `Grants: nil` e `Grants: roleperm.Default()` passavam os dois — o guard reproduzia o defeito que a doc dele descreve. Agora verifica o valor E que o identificador vem de um `roleperm.NewRepository`. `TestEveryPermissionIsEnforcedSomewhere` prova que uma permissão é NOMEADA num gate, não que o gate está MONTADO: remover `r.With(export)` das cinco rotas de backup mantendo a variável sobrevivia a ele e ao pacote. As duas camadas se complementam e nenhuma substitui a outra. Padrão a lembrar: **um teste que afirma "X existe" quase nunca é o que se queria — se queria "X faz efeito"**. Mesma classe nos dois falsos-verdes que o agente achou: a recarga passava com um único tick (a escrita sempre chega antes dele) e o teste do frontend usava um fixture em que a resposta do servidor era idêntica ao draft, então a checkbox ficava marcada com ou sem `setQueryData`. |
| 2026-08-23 | **Matriz RBAC configurável (ADR-42)** | — | A tela `Papéis e permissões` eram quatro pilhas de chips, e uma pilha de chips só mostra o que um papel TEM — então "negado" e "não existe" renderizavam idêntico, que é a única distinção pela qual uma tela de permissões existe. Virou grade: permissões nas linhas, papéis nas colunas, porque a pergunta com que se abre a tela é "quem pode X" (uma linha), não "o que o papel Y pode". A matriz saiu do `map` compilado para a tabela `role_permission` (mig 000039), semeada com exatamente o que o mapa tinha. **O piso é o que torna a configurabilidade segura, e cada regra dele fecha um buraco específico:** o `owner` NUNCA lê a tabela (as permissões dele vêm do mapa compilado em toda resolução — é isso, e não disciplina de handler, que garante que nenhum estado da tabela, truncamento incluído, produza uma instância sem ninguém capaz de consertá-la; a CHECK constraint recusa uma linha `owner`, então nem o SQL direto chega lá); `roles.assign` é intravável porque é a META-permissão, e um papel que pudesse RECEBER o poder de conceder concederia a si mesmo todo o resto num segundo passo, o que tornaria o travamento das outras decorativo; `policy.write`/`instance.transfer` pelo motivo do ADR-35; e `content.read` é travada na direção OPOSTA — não pode ser removida, porque uma conta que não lê a própria biblioteca está quebrada, não restrita, e o dono não sabe qual das duas é. Uma permissão travada é lida do mapa compilado QUALQUER que seja a linha, o que faz do travamento uma garantia em vez de "a tela atual não oferece" — um INSERT à mão é ignorado na resolução. **A escrita é limitada pelo CHAMADOR, não por uma lista de permissões-de-owner:** enunciada assim, uma permissão destravada no futuro fica coberta por construção, enquanto uma lista teria de ser lembrada. Revogar não é limitado assim, senão um admin nunca desfaria uma concessão do dono. **O gate virou `RequirePermission(grants, p)` e essa é a segurança inteira do ADR:** como default de pacote, um mount site esquecido continuaria aplicando a matriz compilada em silêncio e a revogação do dono pareceria salvar sem mudar nada — como parâmetro, esquecer é erro de compilação, e foi o compilador que listou os sete pontos. `Can` roda em toda requisição, então a resolução é um snapshot sob RWMutex, não uma query; uma leitura que falha mantém o snapshot anterior, porque cair na matriz compilada restauraria em silêncio permissões que o dono revogou. `testdb.Reset` RESSEMEIA a tabela: truncar e parar não deixa um banco limpo, deixa uma instância com todo papel editável reduzido ao piso, e o próximo teste com repositório real veria escritas comuns responderem 403 sem nada apontando a causa. |
| 2026-08-23 | **Piso de senha: bound de escrita ≠ bound de leitura** | — | `MaxPasswordFloor` era 128 e `MaxPasswordLen` é 72 BYTES (truncamento do bcrypt), então o intervalo 73–128 era configurável e inutilizava toda criação de senha. Três agentes do sweep anterior apontaram independentemente. A armadilha na correção: `policy.Get` chama `Validate` e cai em `Default()` se falhar — baixar a constante faria um documento gravado com 100 decodificar como default, ou seja, piso 8 E allowlist do Google vazia E TTL de OTP padrão. Uma regra ficando MAIS ESTRITA teria sido a coisa que desligou as outras. Então o bound de ESCRITA apertou (`ValidateForWrite`, recusa acima de 72 com mensagem nomeando o bcrypt) e o de LEITURA ficou no histórico 128: o valor gravado é honrado como escrito, e o dono só é recusado quando SALVA — o momento em que está olhando para o campo. `WarnUnenforceableFloor` loga no boot, senão a instância recusa toda senha e nada diz por quê. O gate do cliente limita em `min(piso, 72)` para que a recusa do servidor, a única resposta que nomeia o número real, continue alcançável. |
| 2026-08-23 | **SecretBand / useCopy / useInstancePolicy** | — | Três superfícies de segredo mostrado uma vez — token de API, semente TOTP, senha temporária gerada — tinham crescido independentemente com as mesmas classes, o mesmo override inline de `alignItems` e três cópias do handler de clipboard que engole a falha. Viraram um componente. O que torna isso mais que higiene: `translate="no"` passou a ser ESTRUTURAL. A tradução de página do Chrome/Edge faz POST do texto visível para `translate.googleapis.com`; o `value` de um `<input>` é isento, um nó de texto com credencial não é — e cada uma das três superfícies teria de lembrar sozinha. Nenhuma lembrou. A chave `['admin','policy']` também estava literal em três lugares, que é como uma delas acaba lendo um cache que as outras não escrevem mais. |
| 2026-08-23 | **Senha temporária: confirmação e geração** | — | O campo que instala a primeira senha de OUTRA pessoa era o único do produto sem confirmação — e o pior lugar possível para a omissão: em qualquer outro campo o digitador sabe o que quis digitar e se recupera, aqui o erro fica instalado numa conta que ele não possui, o endereço nasce NÃO VERIFICADO de propósito (então não há link de reset) e a vítima só descobre como um login que falha, sem distinguir senha errada de endereço errado. Ganhou confirmação e, ao lado, **Gerar uma senha** — porque pedir para confirmar um valor que você teve de inventar é o pedido errado. `lib/generatePassword.ts`: alfabeto de 57 símbolos sem `0`/`O` e sem `1`/`l`/`I` (o valor é TRANSCRITO — lido em voz alta, anotado, colado num chat — e um par que sobrevive a essa viagem vale mais que os ~0,2 bits por caractere que os excluídos somariam; a 20 caracteres ainda são ~116 bits), com **rejeição** dos bytes ≥ 228 em vez de `%`: 256 % 57 = 28, então o módulo simples deixaria as 28 primeiras letras ~1,3× mais prováveis. O teste dirige o gerador com um stub que alterna um byte rejeitado e um aceito e afirma a saída EXATA, então uma implementação com módulo falha — verificado por reversão. O comprimento vem do piso da INSTÂNCIA (`GET /api/admin/policy`, ADR-35), não da constante do cliente: fixar 20 seria um botão que o servidor sempre recusa numa instância endurecida, o mesmo defeito de oferecer o fator e-mail onde `MAIL_DRIVER=log`; o botão fica desabilitado só enquanto a query está PENDENTE, e uma query que FALHA cai no piso compilado e ainda gera. O valor gerado dispensa a confirmação (não há erro de digitação a pegar) e ganha uma banda em claro com Copiar — ele precisa ser legível ou o administrador não tem o que entregar, e a banda existe porque o toggle do `PasswordInput` esquece o estado a cada remount de propósito (INV-149). Editar à mão revoga o status de gerado e a confirmação volta. Teto conhecido e NÃO mascarado: `MaxPasswordFloor` é 128 e `MaxPasswordLen` é 72 BYTES (o ponto de truncamento do bcrypt), então um owner que puser o piso acima de 72 torna toda senha inaceitável — o gerador limita em 72 e deixa a recusa do servidor ser a resposta honesta. Registrado em INV-166. |
| 2026-08-23 | **Sweep: o gate do piso virou botão morto** | — | O sweep de 5 agentes achou um blocker que era meu: o gate de submit passou a usar o piso da instância, mas `MaxPasswordFloor` é 128 e `MaxPasswordLen` é 72 BYTES (truncamento do bcrypt), então numa instância com piso entre 73 e 128 o Gerar produzia 72 caracteres que o PRÓPRIO cliente recusava — nada era enviado, e a única resposta que nomeia o número real (e portanto revela a má configuração) ficava inalcançável. Contradizia o texto que eu havia escrito no INV-166 na mesma sessão. O gate virou `min(piso, 72)` e o piso passou a aparecer sob o campo: um gate que pode recusar por um motivo que nenhuma constante da tela conhece precisa dizer qual é o motivo, ou o admin vê botão desabilitado e reporta feature quebrada. **Segurança achou um vazamento que valia por si:** a banda em claro é um nó de TEXTO, e a tradução de página do Chrome/Edge faz POST do texto visível para `translate.googleapis.com` — o `value` de um `<input>` é isento (por isso o INV-149 não precisava disso), um `<code>` com credencial não. Não havia um único `translate=` em todo o `web/src`: a semente TOTP, o token de API em claro e a folha de códigos de recuperação carregavam a mesma exposição desde que shiparam, e foram retrofitadas no mesmo change. Também corrigi uma frase FALSA que eu tinha acabado de pôr no README — `autoComplete="new-password"` impede o autofill, não o prompt de salvar, então o cofre do administrador pode guardar a senha temporária do outro usuário; o README agora nomeia a troca em vez de negá-la. Test Quality provou por reversão que a queda para o piso compilado quando a query de policy FALHA estava documentada e destravada (`disabled={!policy.data}` deixava o Gerar morto para sempre, 33/33 verdes) e que o byte 228 — o limite da rejeição em si — não tinha teste, então `b > limit` sobrevivia. Seis casos novos, cada um verificado por mutação. |
| 2026-08-22 | **Drawer no lugar do diálogo sem estilo** | — | O "Adicionar usuário" subiu como controles soltos espalhados pela viewport inteira. Causa: `overrides.css` estiliza só o PAR `.fx-confirm.fx-modal`, e eu apliquei apenas `fx-confirm` — terceira variação, nesta mesma sessão, do mesmo tipo de defeito (`.fx-btn` e `.fx-inline-error` não tinham regra NENHUMA; `.fx-input` direto num input perdia para o reset; aqui a regra existe mas exige duas classes). Virou `.fx-drawer`: painel ancorado à direita, altura cheia, fundo OPACO (`--fx-bg`, não token de superfície — atrás dele há conteúdo escurecido, e um formulário onde se digita credencial não pode ter texto atravessando), corpo com rolagem própria para as ações continuarem alcançáveis, e dispensa só no mousedown que começa E termina no scrim, para um arrasto de seleção não jogar fora uma credencial meio digitada. Registrado no §5 com o par de classes nomeado. |
| 2026-08-22 | **Adicionar usuário (exceção declarada ao §4), e os ícones que viraram decoração** | — | O dono relatou que a tela de usuários não tinha adicionar, alterar nem excluir. **Duas das três existiam** — e eu as tinha tornado invisíveis na sessão anterior: os cinco glifos sem rótulo na borda direita da linha eram desativar, encerrar sessões, enviar recuperação e excluir. Tooltip não é descoberta; exige já suspeitar que há algo ali. Voltaram como ícone + texto (`.fx-rowact`). Regressão minha, não falta de feature — e a lição virou bullet no §5. **Adicionar de fato não existia.** Oferecidas três formas; o dono escolheu explicitamente a criação direta COM senha temporária, que contraria o §4 (*um administrador nunca escolhe a credencial de outro*). Implementado como **exceção declarada**, com ADR-40 dizendo por extenso o que ela custa: entre a criação e a primeira troca, duas pessoas conhecem uma credencial, e nenhuma entrada de auditoria distingue um acesso do dono da conta de um do administrador que digitou a senha dela. O diálogo avisa — o dono aceitou a troca sabendo, o próximo administrador não. **O que não foi abandonado:** piso de senha configurado, endereço não verificado, papel nunca `owner` (handler E repositório), senha fora da auditoria. **O sweep achou 1 HIGH e ele era exatamente a armadilha que o `PolicyReader` documenta:** eu tinha injetado a política por SETTER, e os três harnesses deixaram nil — então o teste do piso afirmava sobre a constante compilada e apagar a ligação do `main.go` não quebrava nada. Virou argumento posicional do construtor, com teste que sobe o piso para 20 e prova o mutante morrendo. **Três bugs meus que os testes pegaram:** o cap de nome que eu "apliquei" com um `replace` sem assert e nunca entrou no arquivo (o teste falhou e revelou); a invalidação de cache em `['admin-users']` quando a página usa `['admin', 'users']`, ou seja a lista nunca atualizaria depois de criar; e a mensagem de senha curta montada com a constante do CLIENTE, que numa instância com piso 20 dizia "pelo menos 8" — passou a usar a mensagem do servidor. Contradição no meu próprio ADR também corrigida: ele recomendava `force-password-reset`, que exige endereço verificado e portanto NÃO alcança as contas criadas por essa rota. Duas consequências ficaram declaradas em vez de escondidas: nada fecha a janela (não há rotação forçada no primeiro login) e um erro de digitação no endereço cria conta ativa que o dono legítimo nunca fica sabendo. 13 testes de integração + 8 de frontend; suíte de auth completa verde (212s). |
| 2026-08-22 | **A conta virou trilha lateral, e "Sair" virou "Logout"** | — | Duas correções em cima da página nova. **Copy:** `auth.sign_out` e as três strings de logout-all em pt-BR passaram de "Sair" para "Logout" — conferido por regex que nenhuma sobrou. **Layout:** empilhar as cinco seções numa coluna de 760px foi a primeira forma e não sobreviveu ao contato — três telas de rolagem, ou seja a largura desperdiçada E nada localizável, as duas falhas ao mesmo tempo. Virou trilha à esquerda + painel à direita em largura cheia, uma seção por vez, sticky, virando faixa horizontal rolável abaixo de 900px. A trilha custa um clique e devolve o que os quatro tiles faziam bem: "onde fica X" se responde olhando, não rolando. **E fecha algo que eu tinha deixado pela metade:** `canonicalSection` resolvia `profile`/`security`/`tokens` para `account` e JOGAVA FORA qual deles era — um deep-link que dizia `security` sabia exatamente aonde queria ir e chegava no Perfil. Agora `SECTION_TAB` os leva ao painel certo, e os nomes sobrevivem à fusão como DESTINOS, não como apelidos mantidos vivos por compatibilidade. Testes: rail com `aria-current`, uma seção por vez (o painel anterior somenos), e tabela cobrindo os quatro deep-links. Os harnesses das suítes portadas passaram a abrir o painel que testam via `initialTab`. 1152 testes verdes, 92,63% stmts / 87,52% branches. |
| 2026-08-22 | **A página de conta refeita, e o `password/change` que nunca teve chamador** | — | O usuário perguntou para que servia a tela "Login & perfil" e a resposta honesta era: para quase nada. Os dois blocos só apareciam quando FALTAVA algo — o formulário de senha só se a conta não tivesse senha, o bloco do Google só com OAuth configurado — então uma conta comum abria um cartão com uma linha de status e nenhuma ação. Por trás disso, um buraco de verdade: **não existia "alterar senha" logado em lugar nenhum**. `POST /api/auth/password/change` existia desde o ADR-30, testado no servidor, e nada na SPA chamava; o caminho era sair e usar "esqueci a senha" — um fluxo de RECUPERAÇÃO fazendo as vezes de uma edição comum, numa conta cujo dono está logado e não prova nada. Agora `AccountPage` absorve quatro tiles (perfil, acesso, dois fatores, tokens) mais sessões, em FAIXAS e não cartões: cartão sugere fronteira independente e nenhuma dessas é — senha e Google se condicionam, e uma fronteira entre dois cartões esconde exatamente isso. Os chips do hero respondem "tenho senha? tenho segundo fator?" sem rolar. `profile`/`security`/`tokens` continuam ACEITOS e resolvem para `account`, porque o menu do avatar linka `profile`. **Três bugs pré-existentes que a página nova expôs, nenhum introduzido agora:** `.fx-btn`/`.fx-btn-primary` não tinham regra em NENHUMA das folhas apesar de 14 usos, então toda ação de 2FA e tokens renderizava como texto puro; `.fx-input` aplicado direto num `<input>` perdia borda e fundo para `.fx-shell input`; e `.fx-inline-error`, em quatro chamadas, também não tinha CSS — e as quatro reportam uma operação de credencial RECUSADA, o pior tipo de mensagem para renderizar como prosa. Todos ficaram invisíveis enquanto viviam sozinhos atrás dos próprios tiles. **O sweep de 5 agentes achou 4 HIGH, todos meus.** Duas chaves de i18n órfãs em três locales e um `has_password ?? true` que nunca dispara mas lê como "não sei, então tem" para uma flag de CREDENCIAL. E o agente de testes provou **dois testes vacuosos e quatro mutações sobreviventes**: o meu `does not ask for a second factor` afirmava sobre `/current 6-digit code/i`, que é rótulo do TwoFactorSection em OUTRA faixa — casava com NADA, e passava contra um mutante que renderizava o campo sempre; e o herdado `signs out` esperava `/your profile/i` sumir, string que não existe em lugar nenhum do app, resolvendo no primeiro tick com ou sem logout. Sobreviviam também: apagar o gate do step-up, apagar a exigência da senha atual, e apagar o desfecho de sucesso inteiro. Todos mortos agora, com contrapartida positiva onde o negativo sozinho era satisfeito por uma árvore em que o rótulo não existe. **E o meu teste novo de falha do logout-all achou um defeito de verdade**: `try/finally` sem `catch` deixava a rejeição escapar do handler async como unhandled rejection. **Cuidado registrado:** o agente de testes restaurou `PasswordCard.tsx` para uma cópia anterior ao meu refactor de helpers, desfazendo-o em silêncio — reapliquei e confirmei por `grep`. É a segunda vez na memória do projeto que um subagente com escrita reverte trabalho ao "restaurar". Validado em browser real com um entry temporário e sessão semeada, já que a página exige login. |
| 2026-08-22 | **Estado ativo no hub, olho na senha, lembrar e-mail, ações por ícone** | — | Quatro pedidos numa rodada. **(1) O segmento Pessoal × Administração não mostrava onde você estava** — e não era markup faltando: `.fx-shell button` é (0,1,1) e desespecificava `.fx-hub-seg-btn-active` (0,1,0), levando `background`, `color` E `padding`. Medido no browser, não deduzido: as duas opções voltavam `rgba(0,0,0,0)` e `padding: 0px`, idênticas. A MESMA linha de CSS governa o seletor de visualização do topbar, que estava quebrado junto. `.fx-side-row` e `.fx-topbar .fx-seg` escaparam só porque o `overrides.css` os redeclara encadeados. Terceira vez que essa armadilha do §5 aparece. **(2) `PasswordInput`** trocado nos 21 campos de senha da árvore. **(3) Lembrar meu e-mail** no login — só o endereço, gravado só depois que as credenciais são aceitas, apagado no instante do desmarque. **(4) Ações da administração viraram ícones**, rotuladas duas vezes (aria-label com a conta + tooltip), com a frase da ação CONTÍGUA no rótulo: `Sign out everywhere ({{email}})`, nunca `Sign {{email}} out everywhere` — interpolar no meio quebra tanto quem lê quanto quem consulta por nome. **Um bug que eu quase embarquei:** escrevi o guard do cascade como teste Vitest importando `foldex.css?raw` e ele ficou VERDE — contra o arquivo quebrado. `vitest.config.ts` tem `css: false`, que resolve esse import para string VAZIA, então os três casos afirmavam sobre `""`. Virou `scripts/test-css-button-reset.mjs`, com self-test por fixture no CI, porque um guard cujo caminho de falha nunca é exercitado é exatamente a história que ele mesmo conta. **Um bug pré-existente que o olho expôs:** `.fx-auth-input` é `content-box` com `width: 100%`, então TODO campo das telas de auth era 28px mais largo que a própria coluna — invisível enquanto o painel tinha folga, óbvio quando a senha precisou de espaço para o botão. **O sweep achou 3 HIGH, todos meus.** Dois agentes acharam o mesmo independentemente: `PasswordInput` tinha `className = 'fx-input'` por padrão, e 8 dos 21 sítios põem `.fx-input` numa `div` WRAPPER com input pelado — o default desenhava uma segunda pílula de 42px dentro da primeira, e o campo encolhia porque `.fx-input input { flex: 1 }` deixou de alcançar o input. Sem default agora; verificado no browser: 394×21, idêntico ao baseline intocado. O terceiro foi spell-jacking — revelar torna o campo `type="text"`, e o Enhanced Spell Check do Chrome manda o conteúdo de um campo de texto para um serviço remoto; o conjunto exposto era o pior possível (senha mestra, senhas de pasta, step-up de 2FA). Suprimido no componente. **E o agente de testes desmontou dois testes meus:** `JSON.stringify(localStorage)` é VACUOSO contra o mock (as entradas vivem num closure, retorna `{"length":N}` — ele provou rodando com a senha gravada e passou), e o teste "grava só depois de aceitar" nunca marcava a caixa, então não podia falhar pelo motivo que anunciava. Ambos reescritos e provados por mutação. Também derrubou meu `tabIndex={-1}`: deixar o olho fora da ordem do Tab tira a revelação justamente de quem mais precisa dela. 3 mutantes no guard de CSS + 10 na tabela de especificidade (a heurística antiga errava 5) + 3 nos testes de storage + 1 no rótulo de transferência — todos mortos. Validado em browser real. |
| 2026-08-22 | **Bandeiras de idioma nas telas deslogadas** | — | A troca de idioma só existia no topbar, ou seja depois de entrar — e quem cai no login, no convite ou no link de redefinição num idioma que não lê não tinha saída. A fileira de bandeiras (🇺🇸 EN · 🇧🇷 PT · 🇪🇸 ES) foi montada em `AuthShell`, não por tela: as **nove** telas passam por esse frame, então uma montagem cobre todas e a décima nasce coberta. **Fileira e não dropdown** porque um menu escrito *Idioma* num idioma que a pessoa não fala é um controle que ela precisa adivinhar antes de abrir; **a bandeira é decorativa** — bandeira é país, não idioma, e o Windows desenha o par de indicadores regionais como letras soltas, então o nome do idioma é o rótulo acessível e o código fica visível ao lado. `useLocaleChoice` nasceu para não duplicar a regra que importa: o write-through para `app_user.locale`, sem o qual `useAccountLocale` reimpõe a preferência antiga no próximo load e desfaz calado a escolha que a pessoa viu acontecer. Anônimo não escreve — `useCurrentUser` só é não-nulo em `authenticated`, e `VerifyEmailScreen` é a ÚNICA tela que o `AuthGate` monta acima do short-circuit autenticado. **O sweep de 5 agentes achou 1 HIGH, e era a premissa do meu próprio teste:** eu tinha escrito "telas de meio-caminho (2FA, enrollment) podem ter sessão" e testado isso montando `AuthShell` com sessão autenticada — um estado que o `AuthGate` nunca produz para aquelas telas. O teste não provava nada sobre elas e escondia o único caminho real. Reescrito: `VerifyEmailScreen` para o caminho que escreve, `two_factor_required` para o que não escreve. **O agente de performance achou o defeito de verdade:** o dropdown fechava ao escolher, a fileira não — clicar nas três dentro de um round-trip é gesto comum, e as respostas concorrentes liquidam em qualquer ordem, deixando a conta com a que CHEGOU por último em vez da que foi CLICADA por último; o `useAccountLocale` então impõe isso no próximo load, que é exatamente a falha que o write-through existe para evitar. Escritas passaram a ser serializadas, com a última escolha reemitida ao fim da que está em voo. Outros achados corrigidos: `data-tooltip` inerte (o `TooltipPortal` só monta dentro do shell autenticado), `button.fx-auth-locale` qualificado por elemento contra a armadilha `.fx-shell button` do §5 — `.fx-auth` já é reusado DENTRO do shell em seis lugares —, e a contagem de telas que eu escrevi errada em três lugares diferentes e discordantes (sete no comentário, oito no CLAUDE.md, seis nos READMEs, nove na realidade); onde o número não sustentava argumento nenhum, saiu. **8 mutantes aplicados, 8 mortos** — o primeiro "sobrevivente" era falso: remover o guard de anônimo fazia `user.locale` estourar em vez de regredir, então o mutante foi refeito com `user?.locale` e matou 4 testes. Validado em browser real (login e redefinição, PT persistindo através de reload). |
| 2026-08-22 | **O banco empacotado nunca subiu, e o `make migrate-up` documentado não migrava** | — | Dois defeitos silenciosos encontrados ao consertar o DAST, e um deles achou o outro. **(1) `docker-compose.db.yml` monta o volume em `/var/lib/postgresql/data` com `postgres:18`.** A imagem 18 guarda o cluster num subdiretório por major e **recusa iniciar** quando encontra o caminho legado usado como mount — ela não distingue "volume novo no layout antigo" de "dados 17 que ninguém pg_upgradeou", e subir de qualquer jeito serviria um banco vazio. O pin `postgres:18` está lá **desde o commit inicial** (14/mai), então `make db-up` saiu 1 em toda instalação limpa desde o primeiro dia e nenhum volume no mundo pode ter dados no layout antigo — é exatamente isso que torna seguro mudar o ponto de montagem. Passou despercebido porque a única máquina rodando foldex aponta para um Postgres do host e o DAST nunca chegou ao banco. Reproduzido em volume limpo antes de tocar em qualquer coisa, e as duas saídas possíveis (mount em `/var/lib/postgresql` × `PGDATA` em subdiretório) foram testadas de fato, não escolhidas pela documentação. `scripts/test-db-compose-boots.sh` **sobe o arquivo real** — ler o compose não responde isso, quem decide é a imagem e em runtime; o overlay do teste muda só identidade (nome, projeto), nunca imagem, mount ou healthcheck. Vermelho e verde provados. **(2) `MIGRATE` usava `$(PWD)`.** O Makefile da raiz delega com `$(MAKE) -C backend`, e `make -C` faz chdir sem atualizar a variável de ambiente PWD — só CURDIR acompanha. O caminho resolvia para `<raiz>/db/migrations`, inexistente; o Docker cria a origem ausente de um bind mount como diretório vazio em vez de recusar, e o golang-migrate saía **0** com `no change`. Circular: `db.CheckSchemaVersion` faz o backend sair 1 e mandar rodar `make migrate-up`, o comando que acabou de não fazer nada — linha 59 do README. **DAST:** parado desde 1º/jul. Das três causas, uma já tinha morrido sozinha (`RUSTFS_ENDPOINT` virou `rustfs:9000` em a2a6941) e eu confirmei em vez de "corrigir"; restavam a chave TLS 0600 do runner contra o container uid 101, e as migrations que nunca rodavam. Agora o workflow roda o **mesmo** `make migrate-up` do README, o que é como o defeito (2) apareceu. **Erro meu no meio do caminho:** pus `--wait` no `up` do Postgres, ele reprovou o container e eu culpei o flag num commit inteiro — o flag estava certo, o Postgres morria mesmo; minha reprodução isolada não pegou porque não tinha volume montado. O que o `--wait` não sabe dizer é o porquê, então o loop que ficou no lugar sai cedo em container morto e despeja o log, que é a parte que importa. **Sweep:** 5 agentes, 0 HIGH, e três achados reais — `make` ecoa a receita expandida antes de rodá-la, e a expansão carrega `POSTGRES_PASSWORD` dentro do `DB_URL`, agora num log de Actions retido (recipes com `@`; resta a exposição em `ps`, declarada); o branch de diagnóstico do meu guard era **inalcançável** porque `set -e` mata a atribuição quando o `grep` não casa, ou seja o script existe para tornar uma falha alta e falhava calado; e `-path` nunca era conferido contra o alvo do mount — duas strings independentes que só por acaso concordam, e renomear uma sozinha reproduz o mesmo "exit 0, no change" pelo outro lado. Mutantes reaplicados: M3a/M3b/M7/M1 mortos com mensagem nomeada, e o rename consistente (código correto) segue verde, que é o teste de falso positivo. `migrate/migrate` passou a digest, já que o DAST agora o puxa. **Terceira execução do DAST expôs mais um defeito meu, do PR #75:** `up -d --wait rustfs-init` reprova um container de tarefa única que termina COM SUCESSO — `--wait` espera running|healthy, e quem terminou não é nem um nem outro ("container foldex-rustfs-init exited (0)" → exit 1). A mesma linha estava em `make storage-up`, ou seja o `make up` de quem usa o object store empacotado também quebrava. Virou `scripts/storage-up.sh`, um só, chamado pelo Makefile e pelo workflow; esperar o bootstrap SAIR 0 é garantia estritamente mais forte que esperá-lo rodando. O guard roda contra um FIXTURE e não contra o arquivo real: `docker-compose.services.yml` nomeia os volumes globalmente (`name: foldex_*`), então não existe execução isolada dele — descoberto do jeito difícil, quando o `down -v` de uma sonda minha ficou a uma checagem de segurança do compose de apagar o object store do usuário. O arquivo real é provado pelo DAST, em runner descartável. **Quarta execução ficou VERDE sem escanear nada** — e esse é o achado que fecha o círculo com o CSP: o ZAP mirava `https://web`, ou seja porta 443, mas o nginx roda como usuário não-root e escuta **8443** dentro do container (`listen 8443 ssl`); os 9444 do health check são o lado HOST do mapeamento, que o ZAP não usa porque alcança o container pelo nome de serviço na rede compartilhada. Todo scan desde que o workflow foi escrito conectou numa porta fechada, e `|| true` mais `if-no-files-found: warn` transformaram "não escaneei nada" em sucesso, sem artefato nenhum. "Nada foi escaneado" e "nada foi encontrado" não são a mesma resposta, e só uma delas é boa notícia. Agora: alvo em 8443; alcançabilidade provada ANTES do scan a partir da mesma rede (imagem do curl com digest verificado puxando-a de fato, não de memória); um passo que abre o JSON e falha se não houver relatório ou se nenhum site foi visitado, imprimindo os achados no log do job em vez de deixá-los num artefato que ninguém baixa; e `if-no-files-found: error`. Os três estados do assertor testados localmente (sem relatório / site vazio / relatório real). Achados seguem informativos, que é a postura do §2 — o que deixou de ser informativo é a existência do scan. **Primeiro relatório de verdade: 11 regras** — CSP wildcard e `style-src unsafe-inline` (Medium), COOP/COEP/CORP ausentes e `Server` vazando versão (Low), resto informativo; tratados no lote seguinte, com o `img-src ... https:` confirmado como anterior ao PR #77. **O CI pegou uma corrida no meu script que a minha máquina escondia:** `docker compose ps -q` lista só containers RODANDO, e o container que esse script existe para tratar é justamente o que termina — no Linux ele já tinha saído quando o `ps` rodava, e o id vazio era lido como "não subiu". Verde local, vermelho no CI, que é a ordem honesta para uma corrida; corrigido com `ps -aq`. Em seguida escrevi um terceiro caso de teste afirmando reproduzir essa corrida de forma determinística — **não reproduzia**: o segundo `up -d` reinicia o container terminado, então o id volta a ser encontrado das duas formas. O caso ficou (rodar duas vezes é propriedade real), mas o comentário passou a dizer o que ele de fato prova. Terceira vez na sessão em que um comentário meu era mais forte que o teste embaixo dele. |
| 2026-08-22 | **A SPA estava sem CSP em produção** | — | Medido contra a stack rodando, não deduzido: `/`, `/index.html` e `/sw.js` retornavam **0 de 6** headers de segurança, enquanto `/assets/*.js` e o webmanifest retornavam 6 de 6 — ao contrário do que importa, já que CSP, X-Frame-Options e `frame-ancestors` governam o DOCUMENTO. Causa: o nginx herda `add_header` só enquanto o nível interno não declara nenhum, e três `location` declaravam um `Cache-Control`; como `location /` chega ao index.html pelo redirect interno do `try_files`, a página principal caía justamente no bloco que descartava tudo. Corrigido movendo o `Cache-Control` para um `map $uri $fx_cache_control` no `nginx.main.conf` e apagando os três blocos, de modo que nenhum `location` declara `add_header` — repetir os seis em cada um era a alternativa, e três cópias divergem. Duas premissas do `map` foram provadas empiricamente antes (o `$uri` pós-`try_files` casa `/index.html`; valor vazio não emite header). `scripts/test-nginx-headers.sh` sobe a config real e FAZ as requisições — ler o arquivo não responde isso e nenhum linter aponta; prova por reversão: a config do HEAD falha em **31** asserções (30 de header em 5 caminhos + o guard de origem) — eu tinha dito 12, número tirado de uma saída que eu mesmo cortei com `head`. Ligado ao CI ao lado do teste de redação do access log. **O defeito viveu três meses e atravessou um scan verde.** Foi introduzido em 15/mai por `7bf27a6` — um fix de cache de service worker que adicionou `add_header` em três `location` sem saber que isso descarta o herdado. O DAST de 14/jun rodou contra essa stack e foi marcado `success`, porque o ZAP baseline é informativo (`|| true`) e o relatório virou artefato que ninguém abriu; depois disso o job falhou em jul e ago. Um scanner que não trava nada e cujo relatório não é lido é um scanner que não está rodando. **O CSP passou a valer pela primeira vez e quebrava a própria SPA:** o `foldex.css` abre com um `@import` das três fontes do Google (o Vite hoista para o primeiro byte do CSS buildado) e o preview de imagem do LinkDialog usa `blob:`. Decisão do dono: liberar `fonts.googleapis.com` em style-src, `fonts.gstatic.com` em font-src e `blob:` em img-src, mantendo `script-src 'self'`; auto-hospedar as fontes fica como follow-up. **O sweep achou 3 HIGH no meu próprio teste**, todos com a mesma raiz — ele checava só NOMES de header, em 7 respostas todas 200 de rota estática: sobreviviam remover ` always` (todo 4xx/5xx ia a 0/6), neutralizar todos os VALORES (CSP `default-src *`, XFO `ALLOWALL`, HSTS `max-age=0` — presentes e inúteis) e reintroduzir o bug original com `add_header` de 2 espaços em `location /api/`, porque meu guard checava indentação e não aninhamento. Reescrito: sonda as rotas proxied (502, que é o único jeito de exercitar `always`), afirma substring de significado por header, tem checagem negativa de `unsafe-` no script-src, guard por profundidade de chave em awk, teste do redirect `:8080` contra Host forjado, nome por PID e porta efêmera, imagem digest-pinada, e falha nomeada quando o nginx não sobe. 10 mutações aplicadas, 10 mortas. Achado do próprio script durante a verificação: ele vazava o container quando a saída era cortada por `head` (SIGPIPE), e o nome fixo travava toda execução seguinte — passou a remover qualquer sobra antes de subir. |
| 2026-08-22 | **Um layout de e-mail por mensagem** | — | O usuário tinha pedido para *melhorar o template* e eu li o primeiro item da lista dele ("está 100% em inglês") como se fosse a lista inteira: entreguei o idioma e parei. As onze mensagens compartilhavam um `layout.html.tmpl` com campos opcionais, então um código de entrada e um alerta de sessão encerrada chegavam com a mesma cara — e o SDD já previa arquivos por mensagem, era o código que tinha divergido. Agora `chrome.html.tmpl` guarda os blocos sem significado próprio e cada mensagem tem o seu, em três formas tiradas do conteúdo: **ação** (botão + URL por extenso), **código** (dígitos ANTES do texto) e **aviso** (sem slot de botão). `render` usa `ExecuteTemplate(env.Template, doc)` e `loadAssets` recusa o boot se uma entrada do catálogo não tiver layout. **A forma aviso virou garantia estrutural:** `session_revoked` e `reset_unavailable` não têm mais como ganhar link, e o teste injeta `ActionURL` de propósito nos três idiomas. Acabamento: barra de acento por tipo, rótulo localizado (`eyebrow`), preheader, caixa de URL selecionável, bloco de código 34px, `color-scheme: light only`. Copy continua nos três catálogos — inclusive a assinatura, sob a chave reservada `_footer`. Arm de texto segue ÚNICO e obrigatório: texto puro não tem layout para diferenciar. Gerador de preview atrás da tag `previewgen`. **Dois erros meus na rodada:** escrevi o rodapé hardcoded em português num sistema cujo ponto é i18n (peguei antes de seguir), e repeti para o usuário o "uma correção de copy toca 30 lugares" do comentário do arquivo sem conferir que o texto mora em JSON e não no HTML — número errado, dado justamente enquanto ele decidia. Achado ao confirmar: `_chrome.html.tmpl` funcionava com o glob atual, mas a forma de diretório do `//go:embed` descarta `_` inicial e quebraria as onze de uma vez; renomeado. **O sweep desmontou minha verificação:** o agente de testes aplicou 32 mutações e **17 sobreviveram** — os 3 que eu apliquei eram exatamente os 3 em torno dos quais escrevi os testes. Dois agentes acharam, independentemente, que a garantia "sem link" valia só no HTML: o arm de texto imprimia a URL injetada duas linhas acima da frase que promete não haver link. Condicionar em `.ActionURL` consertava a URL e deixava o `Code` vazando; a correção real foi dar ao arm de texto as mesmas três formas. Também sobreviviam: apagar o botão do reset (nenhum teste afirmava que uma mensagem COM link tem link), um `{{define "chrome.button"}}` colado num arquivo apagando o botão das onze com suíte verde, heading/body/footnote removíveis de todos os layouts, os dois predicados de chave reservada divergindo, e o acento de alerta virando índigo de rotina. Corrigidos com `refuseDuplicateDefinitions`, namespace `chrome.`, `isReservedKey` e seis testes novos. 14 mutantes reaplicados, 11 mortos; sobrevivem 2 cosméticos (remover a marca e a barra de acento), aceitos e declarados. O 14º sobrevivente era eu de novo: um comentário meu dizia que o gate em `.Action` era "the whole guarantee on this arm" quando a garantia é a forma — reescrito. Saída HTML byte-idêntica à revisada. |
| 2026-08-21 | **Mail AMQP: o consumidor que ninguém subiu, e o e-mail em inglês** | — | Achado em produção local: reset de senha publicou, confirmou e liquidou como `published`, e nada foi enviado — o serviço `mailer` carrega `profiles: ["amqp"]` e nunca subiu. Eu dei a fase 2 como pronta usando o exchange `foldex.mail` ter passado a existir como prova de ponta a ponta; quem declara o exchange é o publisher, então aquilo atestava metade do caminho. **Observabilidade:** `Topology.Declare` devolve `SendQueueState` (contagem de consumidores vem de graça no declare, zero round-trip extra) e o `AMQPSink` avisa ao conectar numa fila sem consumidor — retrato na conexão, não vigilância, e o comentário diz isso; `mailworker.handle` loga um INFO por envio com `outbox_id`/`template` e **nunca** o destinatário (`logsafe` redige `email`, não `recipient`). **Compose:** `COMPOSE_PROFILES` no `.env` acima de `MAIL_TRANSPORT`, e `rustfs`/`rustfs-init` saíram do `docker-compose.yml` (já viviam no `services.yml`, e as cópias tinham divergido) porque o `${RUSTFS_ROOT_SECRET_KEY:?}` delas recusava o arquivo INTEIRO — mesma classe do `AMQP_URL:?` que eu tinha corrigido só na instância; `RUSTFS_SECRET_KEY` do backend virou `:-` porque o object store é opcional no `main.go`; `make storage-up` + `NEED_FOLDEX_STORAGE` espelham o que já existia para o Postgres. **Locale:** e-mail chegou em inglês com a interface em português — as duas pernas do servidor estavam certas (sonda com `Accept-Language: pt-BR` gravou `locale=pt`); o que faltava é que `navigator.language` (interface) e `Accept-Language` (fallback) são configurações separadas. **Implementei adoção na conta e descartei**: o Cérbero e o Code Review mostraram, e eu confirmei, que `""` é valor selecionável no Perfil (*seguir meu navegador*) e o schema não o distingue de *nunca escolheu* — a escrita não solicitada no mount tornava a opção inoperante no instante em que era salva. Ficou o caminho menor: `POST /api/auth/password/forgot` aceita um `locale` com o idioma que a SPA está EXIBINDO, e `localeForHinted` o classifica exatamente onde o header já estava, abaixo da preferência do destinatário — ordenação que é todo o argumento de segurança num endpoint anônimo. Da tentativa descartada sobrou uma correção que vale por si: `UpdateOwnProfile` escrevia `name` incondicionalmente, então uma escrita de idioma replayava o nome em cache e revertia rename feito em outra aba; `name` virou tri-state e nasceu `auth.updateLocale`, que não manda nome nenhum. **Achados do sweep que eu não tinha visto:** `scripts/test-init-env.sh` apontava para o `docker-compose.yml` de onde o store saiu (saía com 1 — o único guard da árvore contra um root previsível no RustFS); o `depends_on: rustfs-init` que apaguei não tinha substituto, e `storage.New` faz um `BucketExists` sem retry, então virou `up -d --wait`; `up-build`, `stop-all`, `nuke` e `ps` nunca souberam do store; e o DAST passou a escanear sem `/api/backup/*`. 11 mutantes aplicados, 11 mortos — dois deles só depois de eu corrigir testes que não construíam a condição que o nome prometia. |
| 2026-08-16 | **Perfil do usuário + logout no app (pós-sweep)** | — | Novo endpoint `PATCH /api/auth/profile` (rename próprio, cap 120, DTO estrito — role/status morrem como `invalid_json`; sob `Authenticate`+`RejectAPIToken`; responde payload `/me`). Hub ganha seção Perfil (avatar com iniciais, nome editável, e-mail/role read-only, sair e sair-de-todos-com-confirmação). Topbar ganha `UserMenu`: avatar → perfil (deep-link via `openSettingsAt`) + sign out — única saída do app autenticado. i18n en/pt/es. Testes: 4 de integração backend (rename/strict-DTO/token-scope/cap-em-runes-CJK) + 8 ProfileSection + 2 UserMenu + hub deep-link. Sweep 5 agentes: 0 HIGH; corrigidos — key do remount inclui o contador `n` e o gear limpa o deep-link (blocker: 2º clique no avatar dead-clickava), fast-path no UpdateUser puro-rename (pula o advisory lock global), cap em runes (`utf8.RuneCountInString`, simétrico ao maxLength do SPA), hook `usePortalMenu` extraído (UserMenu+LocalePicker), `MAX_DISPLAY_NAME` compartilhado, gradiente do avatar em tokens. || 2026-08-16 | **Settings Hub unificado com segmento RBAC** | — | Topbar consolidado: botões Admin e Import/Export removidos do quicknav e do menu mobile; um único gear abre o hub. `SettingsPage` reconstruída como hub com segmento Pessoal × Administração (visível só para admin, espelhando os 404 de `/api/admin/*`), tiles para Conta, Segurança/2FA, Tokens, Senha mestra, Pastas bloqueadas + atalhos Import/Export e Stats; seções detalhadas com botão voltar; `AdminUsersPage` incorporada (sem pagehead próprio); guarda contra demotion mid-flight. `AppView` perde `'admin'`. i18n en/pt/es sincronizado (chaves novas; `topbar.admin/import_export` e `admin.page_*` removidas). Testes: hub RBAC (user vs admin), navegação tile→seção→voltar, atalhos, Topbar/MobileMenu sem botões removidos; 1015 testes, cobertura 92,93% statements / 87,15% branches. |
| 2026-08-16 | **Remoção completa do SHARED_SECRET + fix de chunking do bundle** | — | Perímetro deprecado desde o ADR-30 removido de ponta a ponta: config/env/compose, guard do router (+`hmac256`), warnings de boot, helpers da SPA (`foldex.secret`), campo da extensão, delegação `AllowsDownloadNavigation` do backup e toda a documentação current-state; `validateSecureDefaults` agora recusa apenas non-loopback + `AUTH_ENABLED=0`. Testes atualizados (novo contrato: `/api` anônimo → 401 sem header substituto). Junto: fix do ciclo de chunks axios⟷vendor-query no Vite 8/Rolldown que crashava o boot da SPA em produção (`e is not a function`) — axios colocado com `client.ts` e satélites CJS do React agrupados em `vendor-react`; validado headless com render da tela de setup. |
| 2026-08-16 | **Final review closure — segurança, concorrência e contratos** | — | Sweep final sem HIGH encontrou gaps adicionais. RED→GREEN: logout cross-tab removido é autoritativo; paginação tem desempate estável; stats/change-check excluem conteúdo protegido inclusive na corrida de move; erros de fetch não logam URL; Web Push usa egress público sem redirects, semáforo global de quatro deliveries e deadline no teste; deadlines de 31 min ficam só nas rotas de backup; contadores de entries invalidam apenas em mutações de cardinalidade; migration 000030 tem round-trip; backup web inclui notes; shutdown/preview/LinkDialog/proxy Vite mantêm as correções anteriores. |
| 2026-08-16 | **LEAK-HYD-008 — browser backup sem Blob** | — | Firefox/Safari agora recebem ticket opaco de 256 bits, TTL 60 s, one-time e ligado a user/session; GET nativo consome antes do slot e streama um único `Export`. Status autenticado mantém histórico sem ler ZIP no JS. Estado em memória é bounded (128), request-pruned e explicitamente sticky por instância. Testes cobrem cross-user/session, token inválido, replay, expiry, admission e construtor `Blob` nunca chamado. |
| 2026-08-15 | **Audit MEDIUM wave — 9 findings** | — | RED→GREEN e painel 3/3 fecharam `BUG-ART-006/013/014/015/018/019/020`, `LEAK-HYD-011` e `TEST-ARG-007`: slugs/migration 000029, lifecycle não dismissível de operações destrutivas, precache offline com geração anterior, validação de push, retry/cancelamento de convite, feedback OTP, Ctrl/Meta+Enter e suíte comportamental direta. Gates finais: backend 85,9%; frontend 983 testes, 92,4% statements e 86,1% branches; extensão 15 testes. Pack canônico: 82/140 concluídos, 20/55 MEDIUM. |
| 2026-08-14 | **SEC-SEN-004 - contrato de aprovacoes Bash locais** | - | RED confirmou que o fixture original `Bash(python3 *)` era aceito. O harness tracked agora testa fixtures mesmo sem o arquivo local ignorado e, quando presente, parseia `.claude/settings.local.json`; recusa glob/wildcard, Git amplo, package runners, interpreters/shells gerais e chaining/substitution, preservando comandos exatos legitimos. O gate roda uma vez e bloqueia CI; o contrato de CI trava wiring, job e ausencia de `continue-on-error`. |
| 2026-08-14 | **Auth HIGH closure — password reset credential epoch** | — | RED→GREEN: migration reversível `000028` prende resets comuns e administrativos ao `app_user.token_version`; NULL legado falha fechado e não bloqueia emissão nova. Consumo trava `app_user` e exige igualdade exata antes de spend/hash/revogação, preservando uso único, 2FA e atomicidade. Integração cobre troca de senha, logout-all, role/status/revogação admin, corrida com bump, captura pelos dois issuers, SMTP rollback e down→up→down. |
| 2026-08-14 | **CI final test/security closure** | - | Contrato RED->GREEN: `coverage-run` executa `./...` (inclui os testes de `cmd/server` e `cmd/rustfs-bootstrap`) sem ampliar `coverpkg` para boot/helpers e preserva o gate 85%; testes Bun da extensao bloqueiam CI; Chrome live vira pre-requisito executavel via `CHROME_PATH`; o `docker:dind` mutavel e inutil foi removido em favor do Docker do host. O contrato estatico recusa perda de cmd/extensao/Chrome e qualquer service image sem versao+digest. |
| 2026-08-14 | **ARCH-ATL-001 — repository error boundary** | — | RED→GREEN: `auth`, `folders`, `links`, `notes`, `tags` e system repositories deixaram de importar/propagar tipos HTTP. `internal/pkg/domainerr` concentra not-found/invalid-input; conflitos e credenciais usam sentinels locais com `errors.Is`. Handlers possuem matrizes explícitas de status/code/message, preservando 404 cross-tenant, conflitos, folder lock/password, invite/session revoke e targets públicos. Check AST falha para qualquer `repository*.go` de produção que importe `net/http` ou `httperr`. |
| 2026-08-14 | **LEAK-HYD-006 — acceptance closure** | — | RED→GREEN: export entrou no mesmo slot fail-fast de validate/restore antes de query/spool; matriz 3×3 prova `429 backup_busy` sem service/body/temp work. Cursor gated de 50k clicks prova encode row-a-row. LIST de RustFS virou callback streaming: 150k metadados globais são descartados e só keys owner-scoped ficam retidas, sob caps explícitos de 99.998 files, key 1.024 bytes, manifest 32 MiB, file 64 MiB e expanded 4 GiB. Note media é otimizado uma vez para spool 0600 bounded e publicado via `SectionReader`; ledger de skip permanece intacto. |
| 2026-08-14 | **DUP-ECH-007 / DUP-ECH-008 - entity purge + public target policy** | - | RED->GREEN: `entityrefs.PurgeOwnerSet` passou a remover `link_tag`/`click_log` para um conjunto inteiro com kind e owner comprovados, e o staging wipe do importer reutiliza esse primitivo antes do delete owner-first sem encerrar a tx nem perder as temp tables. Contratos destrutivos cobrem colisao polimorfica, set multiplo, vazio/kind invalido, subtree temporaria, purge total e isolamento do bystander; o importer preserva tambem refs alheias e o budget constante. `internal/pkg/publictarget` concentra `publicid.Parse` + `PUBLIC_NUMERIC_IDS` para `/go` e `/n`, com fetch por ID/slug injetado; matrizes cruzadas cobrem flag on/off, max int64, overflow, zero, sinais e fallback de slug. Full integration com `CGO_ENABLED=0`, coverage 85.4%, fmt/vet/build e N+1 sem HIGH. |
| 2026-08-14 | **DUP-ECH-005 / DUP-ECH-006 — content list planner + slug create tx** | — | RED→GREEN: `internal/pkg/listquery` passou a ser a fonte única dos predicados owner-first, busca por shape, tag AND com `entity_kind`, folder/ungrouped, pinned-first, sort e paginação usados por links, notes e pelos dois braços do `UNION ALL` de entries. Suíte table-driven compara cada braço com o repositório canônico em todos os escopos/sorts e mede duas aquisições constantes (lista + tags em lote) com 40 rows. Create de link/note reutiliza um lifecycle de tx por tentativa via `pgx.BeginFunc`, mantendo slug global, 409 explícito, limite 100 e pending tags/media na tx vencedora; retries e exaustão provam zero conexão retida. |
| 2026-08-14 | **TEST-ARG-002 / TEST-ARG-003 — auth TOTP + change-check/push integration** | — | Evidência de integração sem mudança de produção: duas challenges `totp` vivas e independentes disputam concorrentemente o mesmo counter; exatamente uma consome challenge, cria sessão e alcança a superfície protegida, enquanto a outra recebe `invalid_code` e o counter final confirma o CAS no banco. No fluxo change-check, link e subscription de A percorrem repository real → worker → adapter de `cmd/server` → sender → repository Postgres; só o endpoint de A é enviado/marcado, e B permanece intocado. Ambos os focados passaram com `CGO_ENABLED=0` 10x e com `-race` 3x. |
| 2026-08-14 | **BUG-ART-002 / TEST-ARG-005 / LEAK-HYD-006 / N1-NEX-002 / CC-DAE-010 — backup P0 closure** | — | RED→GREEN: migration reversível 000027 grava ledger owner/archive/mode + mappings de entity/note-file no mesmo commit do skip; repeat completo faz zero DB mutation/LIST/PUT e retry após PUT fail reutiliza IDs/keys sem duplicar folder/note/tag/link/associação/click. Export não materializa `Snapshot`: cursor row-a-row → spool 0600 bounded/hash/count → ZIP após commit. Skip/duplicate usam staging + CopyFrom e preload bounded de slugs; pgx fica em budget constante para 48 rows. Skip troca HEAD/key por LIST paginado dos três namespaces; wipe usa multi-delete de keys owner-scoped; PUTs continuam em pool 8 com cancellation. Parser ZIP web foi dividido em readers tipados/bounded com casos adversariais. |
| 2026-08-13 | **LEAK-HYD-004 — dispatcher bounded de auth mail** | — | RED→GREEN: goroutines destacadas por envio foram substituídas por um dispatcher único (2 workers, fila 32) com cancellation e Stop/join após drain HTTP. Reset/OTP/verificação reservam admission antes de persistir; fila cheia preserva token anterior e budget, e budget+OTP agora commitam juntos. Resend autenticado de verificação coalesce 60 s. Force-reset administrativo segue SMTP síncrono e rollback em erro. Testes cobrem bound, backpressure, cancellation/join, erro de transporte, cooldown e invariantes de credencial. |
| 2026-08-13 | **LEAK-HYD-001 / LEAK-HYD-002 — primeiro hardening de archive/spool** | — | RED→GREEN: validate/restore compartilham preflight com 100k entries únicos, caps 32 MiB manifest/256 MiB database/64 MiB file/4 GiB expandido, leitura real max+1 + CRC e cardinalidade bounded (250k conteúdo, 2M relações/eventos, 1k app_settings legado). Um slot antes do `CreateTemp` limita a um spool vivo; concorrente recebe `429 backup_busy` sem ler body/criar arquivo. Fake de temp prova cleanup antes de release; export streaming, idempotência e N+1 ficaram fora do escopo. |
| 2026-08-12 | **Screenshot/media final hardening** | — | Upload manual passou a usar key operation-owned + swap DB atômico que retorna a URL superseded exata, preservando prioridade manual e bytes publicados em falha; cleanup de upload/fallback ignora cancelamento do caller sob timeout próprio. Limite de 256 requests agora é contado via CDP e provado com Chrome real sobre HTTP/2 dentro de HTTPS/CONNECT. Restore valida/re-encoda note media antes do wipe sob caps de 16 MiB/50 MP. N+1 sem HIGH; race, repeat, backend CI/coverage 85.3%, frontend 859 testes/90.45% lines/83.28% branches e builds verdes. |
| 2026-08-12 | **Screenshot final review closure** | — | Reviews finais fechados test-first: o teste Chromium real revelou que `rod.Page` sobrescrevia o BrowserContext informado; captura agora navega pelo browser clonado no contexto e prova cookie/cache/subresource isolados. Fallback e endpoint manual usam keys UUID próprias, CAS antes de publicar e cleanup que nunca apaga bytes concorrentes. Recovery não reroda jobs ativos, usa projeção estreita + índice parcial 000026. Excesso de CONNECT invalida captura. Pool espera 7 s e força kill/cleanup dentro do shutdown global de 12 s. Focused/race/integration verdes. |
| 2026-08-12 | **Auth HIGH closure — proof + mutation atomicity** | — | RED→GREEN com failure injection: login 2FA atomiza proof/challenge/session; confirmação TOTP atomiza seed/counter/recovery e sessão obrigatória, validando sessão de Settings; set-password, disable, regenerate e OAuth unlink revalidam session+epoch no write boundary, bumpam epoch/revogam peers quando mudam credenciais e fazem rollback integral em falha tardia. Proof TOTP replayed não reseta limiter antes do CAS. Métodos repository que permitiam confirmação/recovery parcial foram removidos. |
| 2026-08-12 | **Auth HIGH closure — credential epoch e races** | — | RED→GREEN: migration reversível `000025` prende `auth_challenge` e enrollment TOTP pendente ao `app_user.token_version`, deixando estado legado sem epoch falhar fechado. Resolve/budget/consume, enrollment e sessão usam ordem `app_user` primeiro e revalidam epoch vivo sob lock. Troca de senha trava a linha antes de verify+write e revoga na mesma tx; duas requests com a prova antiga têm um vencedor. Conversão OAuth consome challenge condicional antes de vincular/remover senha; unlink valida senha dentro da tx; callbacks mantêm contrato de redirect. Integração concorrente cobre mutations de challenge/sessão, enrollment settings, conversão, unlink e recovery/reset. |
| 2026-08-12 | **Public note media com `SHARED_SECRET`** | — | RED→GREEN: integração com segredo configurado provou `401` na imagem UUID pública. O roteador agora monta somente `GET /api/files/notes/{uuid}.{ext}` antes do guard; o handler aceita UUID canônico + extensão raster conhecida e responde 404 para nome malformado/traversal sem consultar o bucket. `screenshots/` e `images/` continuam 401 sem segredo e depois owner-scoped. Docs en/pt-BR e contratos de arquitetura atualizados. |
| 2026-08-12 | **Folders/UI HIGH closure — cascade protegido** | — | RED→GREEN: `DeleteCascade` agora materializa subtree owner-scoped, trava todas as folders na tx e recusa atomicamente com `409 descendant_protected` + `count` se houver descendente protegido diferente da raiz. Unlock válido da raiz continua permitindo delete sem fronteiras protegidas; API token continua recusado. `FolderDialog` tenta DELETE, abre o fluxo de senha existente em `folder_locked`, guarda token só na sessão e repete uma vez; `folder_locked`/`descendant_protected`/falhas genéricas ficam inline, sem unhandled rejection. Integração cobre recusa, atomicidade/lock e token; frontend cobre card, retry e erro contado. |
| 2026-08-11 | **SEC-SEN-013 / SEC-SEN-017 + LEAK-HYD-003 / DUP-ECH-007** | — | Migration reversível 000022 adiciona ownership/lease de mídia de note e refs com FKs compostas owner-scoped, sem backfill por `body_html`. Upload registra lease; Create/Update sincronizam refs em lote só para keys owned; delete/cascade/wipe usam ownership/refs e cleanup bounded, com sweeper para leases órfãos. `GET /api/files/notes/*` permanece público para `/n/{slug}`, enquanto mídia id-derived continua autenticada e owner-scoped. Restore re-keya todo `notes/<uuid>`, reescreve body/cover, rejeita `user_id`; helper `entityrefs` centraliza purge polimórfico. RED provou delete cross-tenant, defacement por wipe e o `401` indevido da mídia pública; integration cobre deny path, cleanup em delete/update, lease GC, FK cross-tenant e down→up. |
| 2026-08-11 | **SEC-SEN-011 / SEC-SEN-012** | — | OTP de e-mail e recovery deixaram de usar SHA-256 enumerável: `AUTH_ENCRYPTION_KEY` deriva subchaves HMAC-SHA256 separadas, com versão/purpose e binding de usuário/challenge. Recovery passou a 16 símbolos base32 (80 bits), `XXXX-XXXX-XXXX-XXXX`; migration irreversível `000023` invalida digests antigos. Mantidos consumo condicional, replay guard e discriminador `numericOTP`; admin/OAuth/session assurance ficaram fora deste lote. |
| 2026-08-11 | **SEC-SEN-007 / SEC-SEN-010** | — | `force-password-reset` deixou de instalar/devolver senha conhecida pelo admin: exige SMTP + e-mail alvo verificado, prepara token na tx, entrega e só então publica; falha SMTP faz rollback sem mudar hash/epoch/sessões. O target escolhe senha no reset existente, inclusive Google-only, mantendo TOTP e revogação atômica no consumo. Invite/reset/verify migraram de query/path para fragmento removido no bootstrap; lookup/OAuth invite usam body POST, `Optional` preserva CSRF, e nginx não loga query/referrer. RED→GREEN em integração/frontend. |
| 2026-08-11 | **SEC-SEN-016 / SEC-SEN-018** | — | Chromium continua pooled, non-root e sandboxed; cada screenshot usa BrowserContext incognito e proxy local exclusivos. Policy compartilhada bloqueia metadata/credenciais cloud + RFC6598 no preview permissivo e os registries special-purpose completos no screenshot pre/post-dial sem fechar RFC1918 intranet por default. Capturas são serializadas para o proxy global atribuir bloqueios sem contaminar outro contexto. Proxy cobre redirect/subresource/CONNECT, limita a 32 túneis ativos; flags fecham QUIC/WebRTC UDP direto. Uma instância explícita de `screenshot.Pool` compartilhada pelo worker/endpoint retém launcher/PID sem globals, lança fora do mutex, limita fila a 5 s e separa startup/execução sob envelope caller de 70 s. Retirement faz teardown rastreado assíncrono; shutdown cancela startup/captura e aguarda até 12 s, cobrindo close + cleanup/retry. Cleanup rooted/symlink-safe percorre lotes limitados, limpa profile mesmo em falha pré-PID e força Kill+cleanup em connect/context/close incerto. Mídia owner-scoped usa cache privado; só mídia UUID pública de notes usa cache público. RED→GREEN cobre boundaries, ambos endpoints RFC6598, proxy global, flags, cleanup exato, startup bounded, active-close, connect/create/dispose failure, stale recovery, fila/teardown, cap de CONNECT, fallback completo, race/repeat e isolamento live de BrowserContext. |
| 2026-08-03 | **ADR-30 PR1 — segmentação multi-tenant** | Migration 000017 (12 tabelas de identidade + `user_id NOT NULL` em link/note/folder/tag/push_subscription + FKs compostas `(user_id, folder_id)`), validada contra Postgres 18.4 real antes de escrever código em cima. Novo leaf `internal/pkg/authctx`; ~60 métodos de repositório passam a receber `uid` explícito; queries legitimamente sem escopo isoladas em `repository_system.go`. Backup mudou de contrato: nenhuma tabela de auth no ZIP e restore sempre escreve para quem chamou. Novo pacote `internal/security` com 12 casos cross-tenant + um check estático sobre todos os literais SQL da árvore. `AUTH_ENABLED=0` com shim de bootstrap admin → zero mudança de comportamento. **Três bugs reais encontrados durante a implementação, todos de vazamento cross-tenant:** o fallback de colisão de tag no restore skip buscava o id por `name` sem dono; o de link no restore duplicate buscava por `url` sem dono; e o wipe chamava `DeleteObjectsPrefix` no bucket inteiro (chaves são planas, sem segmento de tenant) — o que teria apagado os screenshots de todos os outros usuários. Lição: a suíte cross-user passou de primeira, o que só virou evidência depois de provar por mutação que ela realmente falha quando a guarda é removida. |
| 2026-08-03 | **ADR-30/31/32 — design de auth multi-usuário** | Fase de design concluída: `docs/SDD-AUTH-RBAC.md` (16 seções) + ADR-30 (sessão por cookie httpOnly com refresh rotativo e detecção de reuso, CSRF double-submit **assinado** contra a linha da sessão, RBAC, posse por `uid` explícito em vez de RLS), ADR-31 (OAuth Google: `sub` é a única chave de login; e-mail coincidente abre **conversão** exigindo a senha atual, nunca login) e ADR-32 (`/go/{id}` numérico vira opt-in — com múltiplos tenants ele é primitiva de enumeração). Supersede o ADR-3. Decisões travadas com o dono: 1º usuário vira admin e depois só convite; SMTP + Mailpit com fallback de log; TOTP obrigatório para admin + OTP por e-mail; tudo privado por usuário (nem admin vê conteúdo alheio); migration 000017 adota os dados existentes num admin de bootstrap. Implementação faseada em 4 PRs, com `AUTH_ENABLED=0` até o PR4. Achados durante o design: `router.go:90` não lista `PUT` no CORS embora `settings` monte `r.Put` (bug pré-existente, invisível porque prod é same-origin); `testdb.Reset` já esquecia `app_setting`; as chaves de objeto do backup são planas, então `DeleteObjectsPrefix` no wipe apagaria arquivos de outros tenants. |
| 2026-08-03 | **RustFS app user bootstrap** | Root (`RUSTFS_ROOT_*`) só no servidor + `rustfs-init`. Backend usa IAM user `foldex` com policy canned `foldex-app` limitada ao bucket (ações reduzidas ao uso real em 2026-08-11). Binário `cmd/rustfs-bootstrap` (madmin) no target Docker `rustfs-bootstrap`. |
| 2026-08-11 | **Security defaults: dev, release e RustFS** | Vite dev passou a loopback com opt-in explícito para LAN; release valida strict semver + ancestralidade em `origin/main` antes de expor credenciais; `make env` gera/persiste segredos RustFS e placeholders passam a ser recusados; policy do app foi reduzida às ações S3 usadas. Contratos RED→GREEN cobrem os quatro limites. |
| 2026-08-12 | **Release sem workflow histórico** | Publicação saiu de push de branch/tag e virou `workflow_dispatch` exclusivo de `main`, com target strict semver ou SHA completo, ancestry em `origin/main`, semver igual aos dois manifests, tag preexistente recusada e tag nova criada só depois dos dois manifests, checkout explícito do SHA e publishers dependentes do gate. Credenciais Docker Hub foram migradas de repository secrets para o environment protegido `release` (reviewer obrigatório, branch policy `main`, admin bypass desligado), então um tag histórico pode carregar seu YAML antigo mas não recebe credenciais. Target histórico não move `latest`; artifacts exigem exatamente um digest amd64 e um arm64. `test-release-gate.sh` cobre trigger ausente, ref histórica, strict semver/SHA, ancestry, pins, permissões e ordenação de secrets. |
| 2026-08-03 | **MinIO → RustFS** | Object store migrado para RustFS `1.0.0-beta.12` (S3 API). Compose service `rustfs`, env `RUSTFS_*` (fallback legado `MINIO_*`), client Go continua em minio-go. Testcontainers genéricos. Docs/i18n atualizados. |
| 2026-07-01 | ADR-29 | —    | Senha master (recuperação) + palavra-dica por pasta. Migration 000016 (`app_setting` KV + `folder.password_hint`). Novo pacote `internal/settings` + leaf `internal/pkg/pwhash`. `POST /api/folders/{id}/reset-password` (recuperação, não bypass de view). Frontend: 4º view `SettingsPage` (lazy) + `api/settings.ts` + campo de dica no `FolderDialog` + dica no `PasswordPromptDialog`. Backup round-trip verbatim (`app_setting` + `password_hint`, snapshot v5, schema v11). i18n en/pt/es. Docs: SDD-FOLDER-MASTER-PASSWORD.md + ADR-29 + CLAUDE.md §4/§8 + README (pt/en). Lição: a linha de reset some da lista `locked` no refetch — a seção mantém uma linha "done" persistente em estado separado. |
| 2026-05-11 | T1     | —    | Repo + Makefile + .env.example + docker-compose.yml (Postgres 16 com healthcheck, ports só em 127.0.0.1). |
| 2026-05-11 | T2     | —    | Docs SDD: VISION.md, ARCHITECTURE.md, TASKS.md. |
| 2026-05-11 | T3     | —    | Backend Dockerfile (distroless) + go.mod + cmd/server/main.go (graceful shutdown) + healthz. |
| 2026-05-11 | T4     | —    | Web Dockerfile (nginx multi-stage) + nginx.conf (proxy /api e /go) + Vite/React scaffold. |
| 2026-05-11 | T6     | —    | pgxpool init + ping em internal/db; config loader em internal/config. |
| 2026-05-11 | T7     | —    | Migration 000001_init.up/down.sql com pg_trgm e índices GIN. |
| 2026-05-11 | T8     | —    | Tags CRUD (handler/repository/dto/model) com `link_count` agregado. |
| 2026-05-11 | T9     | —    | Links CRUD + filtros `q`, `tag` (AND), `sort=created\|clicks\|recent`, tag set via PATCH. |
| 2026-05-11 | T10    | —    | `/go/:id` com `UPDATE ... RETURNING url` atômico + 302. |
| 2026-05-11 | T12-16 | —    | Front: theme dark MUI, TanStack Query, LinkGrid, LinkCard, TagSidebar, LinkDialog, CommandPalette ⌘K, atalho ⌘N. |
| 2026-05-11 | T17    | —    | Preview fetcher com `golang.org/x/net/html`, parser de title/OG/favicon, resolve relativos. |
| 2026-05-11 | T17    | —    | SSRF guard no dialer: bloqueia loopback, RFC1918, link-local, IMDS. |
| 2026-05-11 | T18    | —    | Worker pool com `PREVIEW_WORKER_CONCURRENCY`, channel buffered (256), graceful stop. |
| 2026-05-11 | T19    | —    | `links.Create` enfileira preview; `POST /links/:id/refresh-preview` reenfileira. Crash-recovery: requeue de `preview_status='pending'` no boot. |
| 2026-05-11 | T20    | —    | LinkCard mostra `preview_status` (chip colorido) + botão retry. |
| 2026-05-11 | T21    | —    | Netscape HTML parser (preserva folders → tags). |
| 2026-05-11 | T22    | —    | Formato JSON v1: `{ version, exported_at, tags, links }`. Import idempotente por URL. |
| 2026-05-11 | T23    | —    | ImportPage com drag-drop + toggle Netscape/JSON + botões Export. |
| 2026-05-11 | T24-26 | —    | Extension vanilla MV3 (sem bundler): popup.html/.js, options.html/.js, manifest, README. ⌘+Enter salva. |
| 2026-05-11 | T11    | —    | Suite completa: unit (testify) + integration (testcontainers-go, build-tag `integration`). Backend coverage 85.9% (gate ≥85%). Frontend Vitest 4 + RTL com mock axios; gate ≥85%/80% branches. `CLAUDE.md` documenta as invariantes. |
| 2026-05-11 | T27    | —    | README quickstart. Screenshots/gif: placeholders pendentes. |
| 2026-05-11 | T28    | —    | `scripts/seed.sql` com 4 tags (jira/dashboard/docs/work) e 5 links de exemplo. |
| 2026-05-11 | T29    | —    | `scripts/backup.sh` (pg_dump + rotação 14 backups + comentário cron). |
| 2026-05-11 | T30    | —    | Este log. |

## Iterações pós-MVP (2026-05-12)

| Data       | Mudança                                                                          | Notas |
|------------|----------------------------------------------------------------------------------|-------|
| 2026-05-12 | **Redesign aurora** (handoff Claude Design)                                       | Stack mudou: dropei MUI render-side, agora plain React + `web/src/styles/foldex.css` (CSS do handoff) + `overrides.css`. ~75% menor bundle (~80kB vs 317kB). |
| 2026-05-12 | **Portas custom: backend 9089, web 9088**                                         | Substituiu 8080/5173. Atualizado em compose, vite.config, nginx, Dockerfile, extension defaults, README. |
| 2026-05-12 | **Postgres separado em `docker-compose.db.yml`**                                  | Rede `foldex` externa. `make db-up`/`apps-up` separados. Suporta reusar Postgres do host (`POSTGRES_HOST=localhost` + `extra_hosts: ["localhost:host-gateway"]`). |
| 2026-05-12 | **`POSTGRES_HOST`/`POSTGRES_SSLMODE` parametrizáveis**                            | DSN derivado de `POSTGRES_*` only — `DB_URL` opcional pra overrides custom. |
| 2026-05-12 | **SSRF default permissivo** (`PREVIEW_STRICT_SSRF` opcional)                      | IMDS sempre bloqueado. Resolve o problema de capturar previews de Jira/Grid/intranet. |
| 2026-05-12 | **Migration 000003: `click_log`** — append-only de cliques                        | Cada `/go/:id` insere uma row. |
| 2026-05-12 | **Migration 000004: backfill** dos cliques pré-existentes                         | Materializa `click_count` antigo em rows com `clicked_at = last_clicked_at`. |
| 2026-05-12 | **Migration 000005: `link.pinned`** + índice                                      | Coluna boolean + `link_pinned_created (pinned DESC, created_at DESC)`. |
| 2026-05-12 | **Migration 000006: drop `click_count` + `last_clicked_at`**                       | Click_log vira single source of truth. Repositórios derivam via `LEFT JOIN LATERAL`. |
| 2026-05-12 | **Stats endpoints** `/api/stats/summary,daily,top,tags`                           | Backend pkg `internal/stats` com testcontainers; coverage gate mantido em 85%. |
| 2026-05-12 | **Stats page** no front: 4 KPIs + AreaChart 60d + MoM + top links + tag dist     | SVG inline, sem nova lib. Hover no chart mostra tooltip data+cliques com linha guia. |
| 2026-05-12 | **Pin feature**: toggle no LinkDialog + badge clicável no card + ordering SQL    | Pinados sempre no topo em qualquer sort. |
| 2026-05-12 | **`ConfirmDialog` global**: substitui `window.confirm()`                          | Provider em `main.tsx`, hook `useConfirm()` async. Botões com fx-confirm-btn (compactos, gradients). |
| 2026-05-12 | **Tag manager modal** (`TagManagerDialog`)                                       | Sidebar volta limpa (só dot+nome+count). Editar/apagar tag via modal próprio aberto por "Gerenciar tags" no rodapé. |
| 2026-05-12 | **Sidebar collapse** (rail 44px + chevron expand)                                 | Estado em localStorage. |
| 2026-05-12 | **`TagDialog` em modo edit** (mesmo modal, prop `tag`)                            | Title/kicker mudam. Salva via `useUpdateTag`. |
| 2026-05-12 | **`Esc` fecha modais** (hook `useEscape`)                                         | Aplicado em ConfirmDialog, TagDialog, LinkDialog, CommandPalette. |
| 2026-05-12 | **Tags pendentes no LinkDialog**: só salvam quando o link salva                  | `id: 0` + cor escolhível clicando no dot do chip (cicla paleta de 8 cores). |
| 2026-05-12 | **Tooltips CSS-only** (`data-tooltip` + opcional `data-tooltip-side`)              | Substitui `title` nativo. Delay 180ms, transição smooth, posições T/B/L/R. |
| 2026-05-12 | **Polimento da navegação de tags**                                                 | Remove borda interna do item ativo/focado na sidebar; estado selecionado agora usa wash + marcador lateral sem apertar texto/count. |
| 2026-05-12 | **Dark mode neutral** (charcoal/slate em vez de roxo)                             | Só accent indigo mantém hue. Aurora blobs muted. |
| 2026-05-12 | **Atalhos**: `⌘K` palette, `⌥N` novo link                                        | `⌘N` é hard-claimed pelo browser, não chega no SPA. |
| 2026-05-12 | **`/go/N` button label = "Acessar"** + path no tooltip                            | Não vaza implementation detail na UI. |
| 2026-05-12 | **Makefile coverage**: `-covermode=atomic -count=1`                              | Atomic resolve a média entre test binaries; `count=1` evita cache stale do test profile. |
| 2026-05-12 | **Frontend bundler: bun**                                                         | npm trava em mirror npm privado malconfigurado (versão vazia em `@rollup/rollup-darwin-arm64`). Bun resolve fresh. Dockerfile usa `oven/bun:1.3-alpine`. |
| 2026-05-12 | **`CLAUDE.md` consolidado**: 8 seções com invariantes (versões, testes, docs, dados, UX, DoD, estilo, arquitetura) | Trumps any other doc em caso de conflito. |
| 2026-05-13 | **Card preview com altura fixa**                                                  | Trocou `aspect-ratio` por `height: 150px; min/max-height` e `object-fit: scale-down` na `<img>` — todos os cards alinhados, imagens grandes escalam pra caber, pequenas não esticam. |
| 2026-05-13 | **"preview falhou" gated por ausência de imagem**                                | Não mostra mais o aviso quando o link já tem `og_image_url` (screenshot ou upload). |
| 2026-05-13 | **Tag color: gradient inline**                                                    | `tag.color` aceita `linear-gradient(135deg, c1, c2)`. `TagDialog` ganhou tabs Sólida/Gradiente. Helper `web/src/lib/tagColor.ts` extrai cor primária pra `--chip-c` (porque `color-mix` precisa de cor). Sem schema change — coluna já era TEXT. ADR-17. |
| 2026-05-13 | **Logo "fx" + favicon SVG**                                                      | Topbar troca documento genérico por monograma "fx" branco sobre gradient indigo. `web/public/favicon.svg` linkado no `index.html`. |
| 2026-05-13 | **Grid de 5 colunas**                                                             | `.fx-grid` e `.fx-pingrid` rodam 5 colunas em viewports ≥1680px, com fallback 4/3/2/1 conforme largura. Preview reduzido de 200 → 150px de altura pra preservar proporção do card. |
| 2026-05-13 | **Screenshot como fallback (não compulsório)**                                   | `preview.Worker.maybeScreenshot` roda só quando o fetch HTML veio sem `og:image` E o link não foi atualizado por upload E o host é público (`preview.IsPublicURL`). MinIO ausente = fallback desligado. ADR-16. |
| 2026-05-13 | **HTTPS local via mkcert**                                                        | README documenta `mkcert -install` + emissão pra `web/certs/cert.pem`/`key.pem` que o nginx do container web já consome em :443. |
| 2026-05-13 | **Density picker no Topbar, ao lado do view-mode**                               | DensityPicker (3/5/8) saiu do pagehead e entrou no `fx-viewseg` do Topbar com `<DensityIcon>` (SVG de N barras). Só aparece quando `viewMode === 'cards'`. Persistido em `foldex.grid.cols`. |
| 2026-05-13 | **Grid alinhado à esquerda (CSS Grid, não column-count)**                        | `.fx-grid` e `.fx-pingrid` agora usam `display: grid; grid-template-columns: repeat(var(--fx-cols, 5), …)` — preenchimento row-major. `column-count` balanceava vertical e jogava cards no meio quando o número não dividia. |
| 2026-05-13 | **Upload de imagem curto-circuita o preview pipeline**                            | `UploadImage` agora seta `og_image_url` E `preview_status='ok'` no mesmo UPDATE. Worker's `process()` checa no início: se já tem `og_image_url`, pula HTML fetch + screenshot e zera o "capturando…". |
| 2026-05-13 | **Folders — iPhone-style organization (1:N)**                                     | Nova tabela `folder` + `link.folder_id` (`ON DELETE SET NULL`). Módulo `internal/folders/` espelhando `tags/`. List via LATERAL+jsonb_agg pra preview tiles (até 4, sort `pinned DESC, created DESC`). `links.ListQuery` ganhou `FolderID`+`Ungrouped`; `UpdateInput.FolderID` é tri-state (absent/N/null) via `UnmarshalJSON` custom. ADR-19. |
| 2026-05-13 | **Folder view com URL state**                                                     | App.tsx `openFolder` sincronizado com `?folder=N` via `history.replaceState` + listener de `popstate`. Esc volta pra home. Sidebar de tags desabilita dentro da pasta. CommandPalette ganhou grupo "Pastas". |
| 2026-05-13 | **FolderCard + FolderDialog**                                                     | Card iPhone-style com 2×2 mini-thumbs (`+N` overlay quando >4), "Pasta vazia" quando 0. Dialog espelha TagDialog (color picker sólido/gradient via `tagColor.ts`); delete confirmando que links sobrevivem. |
| 2026-05-13 | **Exporter/Importer round-trip de folders**                                       | JSON v2 ganhou `folders[]` + `folder` em cada link. Importer aceita v1 e v2 (backward compat). Netscape exporter agrupa por folder via `<H3>`. Netscape parser inverteu semântica: `<H3>` mais profundo vira folder, H3s ancestrais viram tags. Folders criadas via `ensureFolder(name)` (match-or-create por nome). |
| 2026-05-13 | **Drag-and-drop iPhone-style pra folders**                                        | `LinkCard` virou `draggable` com payload `application/x-foldex-link`. Drop sobre `FolderCard` → PATCH `folder_id`. Drop sobre outro `LinkCard` → POST "Nova pasta" + 2× PATCH + abre `FolderDialog` em edit pra renomear. Visual feedback (`.fx-card-dragging` / `.fx-card-drop-over`). Same-card drops são no-op. Mutations centralizadas em App.tsx (cards UI-only). |
| 2026-05-13 | **Folder delete cascade opcional**                                                | `DELETE /api/folders/{id}?cascade=1` deleta a pasta E os links dentro numa transação (FKs existentes cuidam de `link_tag`/`click_log`). Sem o param, mantém behavior atual (`ON DELETE SET NULL`). `FolderDialog` em edit ganhou 2 botões: "Excluir pasta" (soft danger ghosted) e "Excluir pasta + links" (full danger). Cada um com mensagem de confirm específica. |
| 2026-05-13 | **CTA "Nova pasta" no topbar + atalho ⌥P**                                       | Botão `.fx-cta-folder` (gradient cyan→emerald) ao lado do "Novo link" indigo. Atalho `⌥P` (⌘P é hard-claimed pelo browser). Removeu botão redundante do `fx-pagehead` da Home. |
| 2026-05-13 | **SDD atualizado pra cobrir folders + DnD**                                       | VISION.md: tirou "favoritos hierárquicos" do out-of-scope; adicionou linha de "Pastas iPhone-style" em experiências core; ajustou `⌘N` → `⌥N` em New link; reformulou Goal #2 pra incluir pastas. ARCHITECTURE/CLAUDE/TASKS/README já estavam cobertos. |
| 2026-05-13 | **Per-folder viewMode persistente**                                               | `viewMode` virou map (`foldex.viewMode.map`) com chave `home` ou `folder.<id>`. Home e cada pasta lembram a escolha (cards/compact/list) independentemente. Default `cards` quando ausente. |
| 2026-05-13 | **Sort alfabético A→Z / Z→A**                                                    | Backend: `sort=alpha` / `sort=alpha_desc` no `ListQuery` → `ORDER BY l.pinned DESC, lower(l.title) ASC|DESC, l.created_at DESC`. Frontend: 2 botões `A→Z` / `Z→A` no segment do topbar. CLAUDE.md ganhou regra: folders vêm antes de links exceto em sort alfabético, onde misturam por nome. Em alpha, CardsView monta lista combinada e ordena por `localeCompare('pt-BR')`. |
| 2026-05-13 | **Atalhos: ⌘K→⌥K, ⌥P→⌥F**                                                       | Convenção: todos os atalhos do foldex são Alt-based (⌘K compete com URL-bar focus do browser; ⌥P colidiu com outros handlers). `⌥K`=palette, `⌥N`=novo link, `⌥F`=nova pasta. Atualizado App.tsx hotkeys, Topbar `<kbd>`s, footer do CommandPalette, README, CLAUDE, VISION. |
| 2026-05-13 | **Folder counters + previews invalidate em link mutations**                       | `useCreateLink`/`useUpdateLink`/`useDeleteLink`/`useRefreshPreview` agora invalidam `['folders']` além de `['links']`/`['tags']`. `LinkDialog` também invalida `['folders']` após upload/remove de imagem. Sem isso, `link_count` e `preview_links` ficavam stale (folder cards na home não atualizavam). |
| 2026-05-13 | **Cor do CTA Nova pasta: cyan→emerald → amber→orange**                            | Cyan/emerald lia parecido com o indigo do "Novo link" em alguns contextos. Trocou pra `linear-gradient(180deg, #F59E0B, #EA580C)` — warm vs cool, distinção máxima a olho nu. |
| 2026-05-13 | **Tooltip via Portal (viewport-clamped)**                                         | Substituiu pseudo-element CSS por `<TooltipPortal>` global + portal pra `document.body`. Calcula posição via `getBoundingClientRect` + clampa pra viewport. Mesma API (`data-tooltip` + `data-tooltip-side`), zero clipping em qualquer borda. |
| 2026-05-13 | **IDs internos saem da URL e dos tooltips**                                       | Removido sync `openFolder ↔ ?folder=N` (history.replaceState + popstate listener). Navegação agora 100% em memória; URLs antigas com `?folder=N` são limpadas no mount. Tooltip "Acessar via /go/N" simplificado pra "Acessar". |
| 2026-05-13 | **Pastas aninhadas (parent_id self-FK)**                                          | Migration 000008: `folder.parent_id BIGINT REFERENCES folder(id) ON DELETE SET NULL`. Backend `List` aceita `?root=1` ou `?parent_id=N`; `Create` aceita `parent_id`; `Update` aceita `parent_id` em tri-state; `DeleteCascade` agora recursivo via CTE. Frontend: App.tsx mantém `folderPath: number[]` stack; home usa `useFolders({scope:'root'})`, folder view usa `useFolders({scope: openFolder})`; criar "Nova pasta" de dentro cria subpasta; Esc/breadcrumb sobe um nível em vez de pular pra raiz. LinkDialog/CommandPalette continuam flat. ADR-19 atualizada. |
| 2026-05-13 | **Subpastas aparecem dentro do pai (fix do CardsView)**                          | Bug: `folders={openFolder === null ? folders : []}` zerava as subpastas quando dentro de uma pasta — herança do design flat antigo. Fix: passar `folders` direto; `useFolders({scope})` já cuida do escopo. |
| 2026-05-13 | **Self-healing folderPath na deleção da pasta atual**                            | `useEffect` em App.tsx observa `allFolders.data` e poda `folderPath` quando um id sumiu. Apagar a pasta enquanto está dentro dela agora volta automaticamente pro ancestral válido (ou home). |
| 2026-05-13 | **Tags filtram dentro de pasta (AND com folder_id)**                              | Revertida a decisão de desabilitar a sidebar de tags dentro de uma pasta. Agora os filtros compõem com o escopo: `folder_id = N AND tag IN (...)`. Backend já suportava. Removida prop `disabled` do TagSidebar + CSS órfão `.fx-sidebar-disabled`. ADR-19 e CLAUDE.md atualizados. |
| 2026-05-14 | **Card "Objects stored" na página de Estatísticas**                              | Novo método `storage.Client.Stats(ctx)` (ListObjects recursivo somando Size). `stats.Handler.WithStorage()` registra `GET /api/stats/storage` só quando MinIO acessível. 5º `<KpiCard>` na StatsPage com `formatBytes` (B/KB/MB/GB/TB); grid de KPIs subiu de 4→5 colunas com breakpoints. |
| 2026-05-14 | **Bug: "Remover imagem" no LinkDialog efetivava antes do Salvar**                | `handleRemoveImage` deixou de chamar `removeLinkImage` na hora — agora só seta `imageRemoved=true`. A DELETE só dispara em `submit()` quando o usuário clica Salvar (Cancel aborta). |
| 2026-05-14 | **Bug: screenshot fallback não atualizava o card**                                | Worker fazia `UpdatePreview(StatusOK)` ANTES do `maybeScreenshot`. Polling do frontend para quando status sai de 'pending' — perdia o screenshot que aterrissa depois. Fix: quando `og:image` veio vazio E screenshot está configurado, mantém status 'pending' durante a captura; flip final pra 'ok'. |
| 2026-05-14 | **Backup & Restore (DB + MinIO em um ZIP)**                                      | Novo módulo `internal/backup/` com `Service.Export/Validate/Restore`. ZIP contém `manifest.json` (kind=`foldex.backup`, schema_version=8, SHA-256 dos entries, counts), `database.json` (todas as 5 tabelas incl. link_tags + click_logs, version=3) e `files/screenshots\|images/`. Export sob REPEATABLE READ. 3 endpoints (`POST /api/backup{,/validate,/restore?mode=…}`) — 2GiB cap, multipart streaming via `MultipartReader`. 3 modos: wipe (TRUNCATE 5 tabelas + DELETE prefixes + restore com IDs originais), skip (`ON CONFLICT DO NOTHING`, mapping old→new), duplicate (tags renomeadas pra `nome (N)`, folders sempre novos, links com URL colidindo caem pra skip + warning — URL é UNIQUE). Storage ganhou `ListObjects/OpenObject/PutObjectStream/ObjectExists/DeleteObjectsPrefix`. Frontend: `BackupCard` na ImportPage (grid 2-cols), `BackupRestoreDialog` com validation summary + 3-way mode picker (wipe pinta de danger). Histórico em `localStorage.foldex.backups` (10 entries). Manifest gravado uncompressed (`zip.Store`) pra que client-side extraia counts sem inflate. SDD completo em `docs/SDD-BACKUP-RESTORE.md`; ADR-20 em `ARCHITECTURE.md`. |
| 2026-05-14 | **Hardening: upload de imagem + proxy de arquivos**                              | `ScreenshotHandler.UploadImage` agora capa `r.Body` via `http.MaxBytesReader(maxSize)`, lê via `io.ReadAll(io.LimitReader(file, maxSize+1))` (não confia mais em `header.Size`), detecta MIME via `http.DetectContentType(data)` ignorando o `Content-Type` enviado pelo cliente, e aceita um closed set `{png, jpeg, gif, webp}` (SVG cai fora — pode carregar script). `ProxyFile` agora exige key sob `screenshots/` ou `images/` (rejeita `..`, `/` inicial, prefixos arbitrários), faz `DetectContentType` no objeto servido (não confia no content-type armazenado) e adiciona `X-Content-Type-Options: nosniff`. Stored-XSS via upload disfarçado fechado. |
| 2026-05-14 | **Hardening: backup correctness**                                                | (a) `topoSortFolders` parou de aliasar o backing array em `remaining[:0]` → folders aninhadas (≥3 níveis) podiam ser silenciosamente droppadas. Fix: `make([]FolderRow, 0, len(remaining))`. (b) `backup.Handler.export` agora bufferiza o ZIP em `bytes.Buffer` antes de escrever — os headers `X-Foldex-Backup-Counts-*` / `Duration-Ms` finalmente chegam ao cliente (antes eram setados após `Export` já ter flushado o body). Injetado `*slog.Logger` no `Handler`. (c) `applyFiles` reforça que `key` final cai em `bucketPrefixes` mesmo se passar pelo check de `..`. |
| 2026-05-14 | **Hardening: backend cleanup**                                                   | `importer/validate.go` checa `rows.Err()` depois do loop (estava silenciando erros de cursor). `storage/minio.go` `GetObject` troca `err.Error() != "EOF"` por `errors.Is(err, io.EOF)` + `io.ReadFull` (resolve EOF wrappado + leitura parcial). `preview/fetcher.go` cacheia `PREVIEW_STRICT_SSRF` em `safeDialer.strict` no constructor — não há mais `os.Getenv` por `DialContext`. `importer.insertLinkIfNew` ganhou `wipeFirst bool`: DELETE + INSERT na mesma tx, fim do risco de "link apagado mas substituto não inserido" em modo wipe. `exporter.exportJSON` coleta tag/folder rows em slices e fecha cada `rows` antes de chamar `queryAll` — não segura mais 3 conexões do pool simultaneamente. |
| 2026-05-14 | **Hardening: UI invariantes §5**                                                 | Trocado `title=` nativo por `data-tooltip` em `LinkCard.tsx`, `CompactGrid.tsx`, `CommandPalette.tsx`, `GradientPicker.tsx`. `CompactGrid.tsx` também parou de vazar `/go/${l.id}` no tooltip (era violação §4 — IDs internos não aparecem na UI). |
| 2026-05-14 | **Limpeza de identificadores internos**                                          | Removidas todas as referências a marcadores corporativos, nicknames pessoais, mirror npm privado e caminhos hardcoded com usuário em `CLAUDE.md`, `AGENTS.md`, `docs/VISION.md`, `docs/ARCHITECTURE.md`, `docs/SDD-BACKUP-RESTORE.md`, `docs/TASKS.md`, `scripts/seed.sql` (tag renomeada para `work`), `scripts/backup.sh` (path no comentário generalizado). `web/bun.lock` reescrito: 258 URLs do mirror npm privado → `registry.npmjs.org/` (proxy transparente, integrity hashes preservados, zero mudança de versões). Sweep replicável: `grep -ri "<termo>" --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=dist`. |
| 2026-05-14 | **P0: cycle check de folders em tx serializável**                                | `folders/repository.go` `Update`: o cycle check + UPDATE estavam em duas chamadas pool separadas — outra request podia mover pasta entre os dois e criar ciclo. Agora ambos rodam em `BeginTx(IsoLevel: Serializable)`. |
| 2026-05-14 | **P0: import inteiramente atômico**                                              | `importItemsWithMode` agora abre **uma única** transação para o loop inteiro. Falha no item N → rollback total (antes: cada item em tx própria, falha no meio = banco semi-aplicado). `ensureTags`/`ensureFolder` aceitam interface `dbtx` (pool ou tx). `insertLinkIfNew` split: wrapper + `insertLinkInTx`. Worker enqueue movido pra depois do commit (evita race com visibilidade da tx). |
| 2026-05-14 | **P0: hardening de I/O e DoS**                                                   | `storage.DeleteObjectsPrefix`: goroutine produtora agora respeita `ctx.Done()` em `select` (não fica pendurada se RemoveObjects falha cedo). `backup.Validate`: cap em `manifest.Checksums ≤ 100_000` antes do loop. `backup.readZipFromRequest`: `http.MaxBytesReader` aplicado ao `r.Body` no topo (capa ambos paths: raw zip + multipart). `main.go`: warn explícito no boot quando `SHARED_SECRET` está vazio. |
| 2026-05-14 | **P1: import cap 20MB → 100MB**                                                  | Chrome export de 50k+ links passa de 20MB facilmente. Backup é 2GB, import só 20MB era incoerente. Teste de body-too-large ajustado pra 101MB. |
| 2026-05-14 | **P1: UX + acessibilidade**                                                      | Nova hook `useFocusTrap(ref, open)` aplicada em todos os dialogs (LinkDialog, FolderDialog, TagDialog, TagManagerDialog, ConfirmDialog, BackupRestoreDialog, ImportPreviewDialog, CommandPalette) — Tab/Shift+Tab cycla dentro do modal, foco restaurado ao fechar. `<ErrorBoundary>` no root em `main.tsx` com fallback "recarregar" — qualquer crash em render para de virar tela branca. `axios` ganhou `timeout: 30000`, request interceptor anexa `X-Foldex-Secret` de localStorage (`foldex.secret`), response interceptor captura 401 e re-pergunta o secret (uma vez por sessão). |
| 2026-05-14 | **P1: housekeeping + infra**                                                     | `viewModeMap` no `App.tsx` agora prune chaves `folder.<id>` órfãs no mesmo useEffect que faz prune do `folderPath` (antes o localStorage só crescia). `web/Dockerfile` virou multi-stage: stage 1 builda dentro de `oven/bun:1.3-alpine`, stage 2 nginx só com o output — fim do "esqueci de rodar bun run build" silencioso. CI ganhou step `npm test` explícito antes de `npm run coverage` (failures de teste viram visíveis mesmo se a coverage gate for relaxada). |
| 2026-05-14 | **UI: rótulo "Netscape" → "Bookmarks HTML"**                                     | Termo técnico (Netscape Bookmark File Format) era jargão obscuro pra usuário final. UI agora diz "Bookmarks HTML" com tooltip "Formato Netscape — padrão de export do Chrome, Firefox, Safari, Edge". Valor do `format=netscape` no protocolo permanece (API key). |
| 2026-05-15 | **Paste-to-create-link**                                                          | Ctrl/Cmd+V em qualquer lugar da página agora abre o LinkDialog com a URL pré-preenchida. Listener `usePasteUrl` (`web/src/hooks/usePasteUrl.ts`) é no-op quando target é editável (INPUT/TEXTAREA/SELECT/contentEditable) OU quando qualquer `.fx-overlay` está aberto. Detecção `looksLikeUrl` (`web/src/lib/url.ts`) tolerante (16 unit tests verde): aceita `http(s)`/`ftp`/`file`/bare host com dot; rejeita palavras, números, espaços, schemes não-web. Gotcha resolvido nos testes: `new URL("https://42")` resolve pra IPv4 `0.0.0.42` (octet implícito) — checagem de `'.'` no input original antes de tentar implicit https. `App.tsx` ganha state `pastedUrl` limpo no close. `CLAUDE.md §5` documenta a invariant. 189/189 frontend tests verde. |
| 2026-05-15 | **Slugs amigáveis (`/go/jira-board`)**                                           | ADR-7 implementada após menção original "quando necessário". Migration 000009 adiciona `link.slug TEXT NOT NULL UNIQUE` com CHECK `[a-z0-9]+(-[a-z0-9]+)*` rejeitando slug puramente numérico (evita ambiguidade com IDs). Backfill SQL inline com `unaccent + regexp_replace` slugifica os ~16 títulos existentes; colisões resolvidas com `row_number() … ORDER BY id` + sufixo `-2`. Backend: novo `internal/links/slug.go` (`Slugify` + `SlugIsValid`, 22 unit tests verde), `repository.Create` retry-on-collision pra auto-gen, `Update` com tri-state slug (`null` = regen do título), `GetBySlug`/`ClickAndResolveBySlug`. `redirect.handler` faz ID-first (`/go/42` continua) → slug-fallback (`/go/jira-board`) → 404. Importer/backup/exporter propagam slug end-to-end (`uniqueLinkSlug` + `nextAvailableSlug`). Frontend: `Link.slug` no types, `goHref(link)` prefere slug, `LinkDialog` ganha campo "URL curta" com auto-derivação live do título via `slugifyClient` (espelho do `Slugify` Go) + botão de reset. i18n: 6 keys novas em en/pt/es. CLAUDE.md §4 ganha invariant; ADR-7 vira "Done". 173/173 frontend + suíte integration backend toda verde. |
| 2026-05-17 | **Compressão e re-encode de imagens antes do MinIO**                              | Novo pacote `internal/imageopt` (pure Go, sem CGO): decode → downscale Catmull-Rom (≤1024 px lado maior) → composite alpha sobre branco → JPEG q82. Plugado em `links.ScreenshotHandler.UploadImage`, `CaptureAndStore` e `preview.Worker.maybeScreenshot` — todos passam pela mesma pipeline. Saída canônica vira `{prefix}/{id}.jpg`; extensões legadas (.png/.gif/.webp) do mesmo id são purgadas no upload via `storage.Client.DeleteObject` (novo método, idempotente — NoSuchKey = sucesso). Guard de não-regressão só pra entrada já JPEG (PNG/GIF/WebP sempre re-encoda, garante ext previsível). Refatorado `ScreenshotHandler.repo` pra interface `screenshotRepo` (Get/UpdateOGImage/ClearOGImage) — agora CaptureAndStore + UploadImage são unit-testáveis com fake repo + fake uploader, sem DB. `Uploader` interface ganha `DeleteObject` tanto em `internal/links` quanto em `internal/preview`. Fallbacks: optimize falha (corrupção) → grava bytes originais sob ext legada; ProxyFile continua tolerante a qualquer ext sob `screenshots/`/`images/`. Sem backfill — arquivos pré-deploy permanecem como estão. Deps novas: `golang.org/x/image/draw` + `webp` (decoder, encoder não existe sem CGO; ok porque saída é sempre JPEG). 92.7% coverage no pacote novo. CLAUDE.md §4 + ARCHITECTURE.md ganham seção "Pipeline de imagens". |
| 2026-05-25 | **Compactar pastas + RapidView popover**                                          | Novo toggle no Topbar (`fx-viewseg`, só aparece quando `viewMode === 'cards'`) que minimiza os `FolderCard`: esconde a área de preview 2×2 (150 px) e renderiza o card como tira fina (mini-swatch + nome + contagem). Estado per-context (igual `viewMode`), persistido em `foldex.foldersCompact.map` keyed `home`/`folder.<id>` — pruning de chaves órfãs no mesmo `useEffect` que limpa `viewModeMap`. Novo componente `FolderRapidView` (portal) dispara hover/focus no título e lista `preview_folders` + `preview_links` (cap 10, `+N mais` footer derivado de `link_count + folder_count − rows.length`). Usa o payload existente de `useFolders` — **sem nova rota de API**. Show delay 220 ms (igual `TooltipPortal`), posicionamento com viewport-clamp. Replicado no `MobileOverflowMenu`. i18n: `topbar.folders_compact_{on,off}` e `folder_card.rapidview_more_{one,other}` em en/pt/es. CLAUDE.md §5 ganha invariant; FolderCard.test ganha 5 casos novos (preview hidden / RapidView com subpasta+links / "+N mais" / empty folder sem popover). 196/196 frontend tests verde (coverage delta +0.55pp; o gate de 85% já estava off-baseline em ~62%, não é regressão desta mudança). |

| 2026-05-29 | **P5+P6: MEDIUM/LOW security closure + frontend perf/UX wins**                    | **P5 (6 itens, security closure):** (M2) Novo `internal/pkg/cssvalid` valida color contra regex (hex 3/4/6/8 chars ou `linear-gradient(135deg, #hex, #hex)`). DTOs de tags + folders agora rejeitam CSS injection vectors (`red url("…")`, `expression(…)`, gradients fora do padrão). 19 unit tests + dto tests reforçados. (M4) `stats.handler` ganha `clampInt` helper — `?days` clamp em [1,365] e `?limit` em [1,100]. Antes `?days=2147483647` derrubava o Postgres via `generate_series`. (M5) `config.validateSecureDefaults` recusa boot quando `BACKEND_BIND != 127.0.0.1 AND SHARED_SECRET == "" AND CORS_ORIGINS=*` — combinação insecure-by-default fechada. Novo `BACKEND_BIND` env (default `127.0.0.1`); main.go usa em vez de bind ":port" all-interfaces. (L3) `nginx.conf` ganha 5 headers obrigatórios: HSTS, X-Frame-Options DENY, X-Content-Type-Options nosniff, Referrer-Policy no-referrer, Permissions-Policy (camera/mic/geo/cohort=()), CSP completo (`default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data: https:; connect-src 'self'; frame-ancestors 'none'; base-uri 'self'; form-action 'self'`). Todos `always` (cobrem 4xx/5xx). (L4) 13 GitHub Actions pinadas para SHA full + tag comment (actions/checkout@11bd7190 # v4.2.2, etc.) — major tags são mutáveis e poderiam ser sequestradas. SHAs resolvidos via `gh api` no momento do pin. (L5) CI ganha 2 steps informacionais `continue-on-error: true`: `govulncheck` no backend job e `bun audit --audit-level=high` no frontend job. **P6 (5 wins frontend):** (1) `LinkCard` + `FolderCard` agora `React.memo` — re-render storms em grids de 200+ cards eliminados. (2) `StatsPage` + `ImportPage` agora `React.lazy` + `Suspense` — bundle inicial trim. (3) Novo `usePinLink` hook com `onMutate`/`onError`/`onSettled` — pin badge anima imediatamente, sem esperar round-trip. (4) `useApplyImport` + `useRestoreBackup` agora `useMutation` com invalidação explícita de links/folders/tags/stats — antes home view ficava stale por 30s pós-import/restore. (5) **~13 aria-labels hardcoded migrados pra i18n**: novos `common.*_aria` keys em en/pt/es (open_link, edit_folder, open_folder, reload_folder, edit_tag, delete_tag, remove_tag, color_swatch, view_mode, tag_filter, delete_folder_keep_links, delete_folder_and_links). LinkCard, FolderCard, ListView, CompactGrid, FolderDialog, TagDialog, TagManagerDialog, TagChip, LinkDialog, Topbar, App.tsx atualizados. Build, vet, full integration, `tsc --noEmit` todos verdes. |
| 2026-05-29 | **P3+P4: test gap fill + quick wins sweep**                                       | **P3** (closes ~13 invariant gaps + 3 IT suites). Novos arquivos: `internal/folders/repository_integration_test.go` (5 tests: nested CRUD, delete sets parent_id+folder_id to NULL, cascade=1 recursivo, empty leaf, cycle check), `internal/backup/service_integration_test.go` (2 tests: export produz zip válido com manifest Store-compressed + callback `onCountsReady` recebe Counts iguais ao Report, callback erro short-circuita). Adicionados em `links/repository_integration_test.go`: `TestRepository_UngroupedExcludesLinksInFolders` (§4 home query), `TestRepository_ListByFolderANDTag` (§4 composição AND), `TestRepository_GoEndpointIsOnlyClickInserter` (§4 click_log único source), `TestSchema_NoCachedClickColumns` (locks migration 000006 — colunas link.click_count/last_clicked_at devem estar ausentes). Mock drift fix em `web/src/test/server.ts`: `patchFolder` agora aplica `parent_id`, `patchLink` aplica `folder_id`/`pinned`/`slug`, `createLink` carrega `folder_id` + `pinned` da request — antes DnD tests em App.test eram vácuos. **P4** (10 quick wins mecânicos): backend `mimeToExt` (dead) + `startsWith`/`indexByte` (reinventados) deletados, `err == io.EOF` → `errors.Is(io.EOF)` em 2 sites. Frontend `LinkGrid.tsx` + seu test deletados (dead code com "carregando..." hardcoded e hint `⌘N` errado). i18n: `link_dialog.folder_aria` adicionado em en.json (estava só em pt/es), `tags_search_aria` dead key removido dos 3 locales, plurais `link(s)` convertidos para `_one`/`_other` em `folder_dialog.delete_confirm_body`, `delete_cascade_confirm_body`, `tag_manager.delete_confirm_with_links` (en/pt/es); `folderTree.ts` `localeCompare(name, 'pt-BR', ...)` → `undefined`. Performance: `loading="lazy" decoding="async"` adicionado em 5 `<img>` sites (LinkCard, FolderCard x2, Favicon, FolderRapidView). `TagChip` `as any` → `as never`. `linksKey` agora sort `tagIds` antes do join (`.toSorted` na verdade pelo spread+sort) — toggling A→B vs B→A produzia cache misses fantasmas. `LinkDialog`: blob URL agora é revogado quando nova imagem substitui ou dialog desmonta — antes vazava por sessão. `@mui/icons-material` removido do `package.json` (zero imports). Build, vet, full integration suite verdes, frontend `tsc --noEmit` clean. |
| 2026-05-29 | **P2 follow-up: 3 sweep findings fechados + 4 testes novos**                     | Sweep dos agentes Phase 2 surfaceou 1 MEDIUM Security + 2 critical Test gaps + 4 Code Review minors. Fechados: (Sec MEDIUM) `readZipFromRequest` agora recebe `w http.ResponseWriter`, passa pro `MaxBytesReader`, e `streamToTempZip` retorna sentinel `ErrPayloadTooLarge` quando `errors.As(*http.MaxBytesError)` — handler mapeia pra 413 `payload_too_large` em vez de 500 genérico. (Code Review) `service.go:Export` agora commita a tx REPEATABLE READ logo após `readSnapshot + ListObjects`, antes do stream ZIP — antes ficava aberta por minutos no cliente lento, tripando `idle_in_transaction_session_timeout`. (Code Review) post-dial SSRF type-assert `!ok` agora fail-closed (refusa a conn) em vez de fall-through silencioso. (Code Review) 4 `w.Enqueue(link.ID)` em `worker_integration_test.go` ganharam `_ =` pra match o resto. Tests novos: `TestServerNewPanicsWhenScreenshotterMissingPolicy` locks o boot panic (sem ele, remover o guard ship green); `TestCheckRemoteAddrSSRF_BlocksRebinding` (9 cases — extraído `checkRemoteAddrSSRF` pra função pura testável, locks o post-dial defense contra DNS rebinding via fake `net.Addr`); `TestHandler_CreateStillReturns201WhenEnqueueFails` afirma que `ErrQueueFull` do worker não vira erro pro client; `TestHandler_CreateRejectsLargeBody` reforçado pra assertar `invalid_json` code (não só 400 status). Build, vet, full unit + integration suites verdes. |
| 2026-05-29 | **P2 sweep: backup streaming + worker lifecycle + SSRF TOCTOU + 5 follow-ups**   | (P2.1) `backup.Service.Export` ganhou callback `onCountsReady(Counts) error` que dispara após `readSnapshot` + `ListObjects` mas ANTES do primeiro byte do ZIP — handler usa pra flushar headers (Content-Type, Content-Disposition, X-Foldex-Backup-Counts-*) e depois streamar entries direto pro ResponseWriter. Memória de O(2 GiB) → O(maior objeto único). Manifest ainda uncompressed (`zip.Store`). REPEATABLE READ mantido. (P2.2) `readZipFromRequest` agora stream pra `os.CreateTemp` em vez de `bytes.Buffer` 2 GiB — `zip.NewReader` aceita `*os.File` (ReaderAt). Cleanup fecha + remove no defer, sucesso e falha. (P2.3) `preview.Worker.Enqueue` agora retorna `error` (`ErrQueueFull`/`ErrStopped`). `Stop` seta `atomic.Bool stopped` antes de cancel → Enqueue pós-Stop não enche silenciosamente o buffer. Callers (links.Handler, importer.Handler) usam fire-and-forget com `_ =`. Interface `links.Enqueuer` quebrada — tests fakes atualizados. (P2.4) `testdb.Reset` agora TRUNCATE inclui `folder` e `click_log` (estavam ausentes; stale rows vazavam entre subtests). (P2.5) JSON handlers `links/folders/tags` Create+Update ganham `http.MaxBytesReader(w, r.Body, 64<<10)` antes do `json.Decoder` — antes um body de 100 MB ficava todo em RAM. (P2.6) `links.isURLUniqueViolation`/`isSlugUniqueViolation` migrados de string-match pra `errors.As(*pgconn.PgError)` + `ConstraintName` — espelha o padrão já usado em `tags`. (P2.7) Nil `URLPolicy` no `CaptureAndStore` agora retorna 500 `policy_unconfigured` (não 400 `private_target`) — ops distingue misconfig de SSRF real. Router boot panic se `Screenshotter != nil && ScreenshotURL == nil` — surfacing wiring error no startup, não em request. (P2.8) `safeDialer.DialContext` ganha post-dial check via `conn.RemoteAddr().(*net.TCPAddr)` — fecha conn se peer real cai em IMDS/private mesmo após LookupIP ter passado (DNS rebinding). Dead code removido: `var _ = json.Marshal` em backup/handler.go. Build, vet, unit + integration suites verdes. Novos tests: `TestWorker_EnqueueAfterStopReturnsErrStopped`, `TestHandler_CreateRejectsLargeBody` (IT). `TestCaptureAndStore_NilPolicyFailsClosed` atualizado pra novo status/code. |
| 2026-05-29 | **P1 follow-up: worker decode-bomb skip + 3 test gaps fechados**                | Sweep dos agentes Phase 1 surfacearam um MEDIUM (Security) e 3 critical test gaps (Test Quality). Fixes: (a) `preview/worker.go:maybeScreenshot` agora checa `errors.Is(err, imageopt.ErrTooLarge)` e aborta o fallback inteiramente em vez de cair pro fallback PNG-cru — antes, uma página hostil devolvendo uma decode-bomb teria os bytes gravados no MinIO e qualquer browser abrindo `/api/files/screenshots/{id}` faria OOM. (b) `TestCaptureAndStore_RejectsNonPublicTarget` agora captura o URL que a policy recebe e afirma `assert.Equal(storedURL, captured[0])` — futuro refactor que sanitize a URL antes do gate fica travado. (c) Novo `TestUploadImage_Rejects5MBPlus` afirma 5 MiB+64 KiB → 400 (cap baixou de 20→5 MiB). (d) Novo `TestOptimize_PixelCapBoundary` testa exatamente 50_013_184 px (rejeitado, `>` strict) e 49_999_041 px (aceito) — pega off-by-one futuro. Build + vet + unit + integration verdes. |
| 2026-05-29 | **P1 sweep: 4 HIGH security blockers + URL conflict 500→409**                    | (H1) Manual screenshot endpoint (`POST /api/links/{id}/screenshot`) ganhou gate SSRF via `links.URLPolicy` injetada — main.go passa `preview.IsPublicURL`. Sem policy = fail-closed (deny). Rejeita scheme não-http(s) com `invalid_scheme`, IMDS/RFC1918/loopback com `private_target`. Antes: Chrome `--no-sandbox` navegava em `file:///etc/passwd` e devolvia PNG via `/api/files/screenshots/{id}` (read-anywhere se SHARED_SECRET vazio). (H2) `importer.ParseNetscape` agora valida scheme antes de aceitar `href` — `javascript:`, `data:`, `file:`, `vbscript:`, `mailto:`, `tel:`, `ftp:` silenciosamente skippados. JSON importer + DTO já validavam; Netscape era o único path sem essa checagem. (H3) `exporter.exportNetscape` trocou `%q` (Go-syntax) por `html.EscapeString` no atributo HREF — URL com `"><script>` agora vira `&#34;&gt;&lt;script&gt;` em vez de quebrar o atributo. (H4) `imageopt.Optimize` agora chama `image.DecodeConfig` antes de `image.Decode` e rejeita com `ErrTooLarge` se width×height > 50_000_000 pixels. Decode bomb 8000×8000 (~200 KB comprimido, ~256 MB decodificado) é refusada antes de alocar o framebuffer. Upload cap em `UploadImage` baixado de 20MB → 5MB (consistente com o cap de pixels + comprime suficiente pra qualquer foto de celular). (Go #3) `links.Repository.Create` + `Update` ganharam `isURLUniqueViolation` matching `link_url_unique` constraint name — duplicate URL agora vira 409 `url_taken` em vez de 500. Browser extension e import bulk dependem desse typed conflict. 5 fixes + 7 testes novos: `TestCaptureAndStore_Rejects{NonHTTPScheme,NonPublicTarget,NilPolicyFailsClosed}`, `TestParseNetscape_RejectsDangerousSchemes`, `TestIsHTTPScheme`, `TestExportNetscape_EscapesHostileURL` (IT), `TestOptimize_RejectsDecodeBomb` + `TestOptimize_AcceptsUpToPixelCap`, `TestRepository_{Create,Update}DuplicateURLReturns409` (IT). Unit + integration suites todos verdes. Coverage backend em 49.6% — pré-existente, gate de 85% já estava falhando antes (Phase 3 do plano cuida disso). |

| 2026-06-14 | **Licença MIT + audit de segredos pré-público**                                  | Preparação pra tornar o repo público. Audit de segredos varreu árvore + **histórico inteiro do git** (todas as revisões): zero `.env`/`.pem`/`.key`/vapid.json commitados, zero tokens (AWS/GitHub/Google/Slack/Docker), zero blocos PRIVATE KEY, nenhum `.idea`/datasource, nenhum dump. CI usa `${{ secrets.* }}` (não exposto ao virar público); compose só interpola `${VAR:-default}`; `.env.example` só placeholders. **Seguro pra publicar.** Adicionado `LICENSE` MIT (© 2026 Valmir Justo), seção de licença nos dois READMEs (era "no license granted") e `"license":"MIT"` no package.json. Limpeza pré-público: 7 paths absolutos hardcoded no AGENTS.md trocados por `<repo-root>` (não vaza estrutura local/username em arquivo rastreado). |
| 2026-06-13 | **CLAUDE.md trim conservador (−5%) + ADR-25 (oEmbed)**                           | Passada super-conservadora pra reduzir o always-loaded CLAUDE.md sem perder spec: 35.6KB→33.6KB (~8.9k→8.4k tokens, −5,4%), **70 invariantes §4+§5 todos preservados**. Bullet de oEmbed (o mais inchado) virou regra terse + `Detail: ADR-25`; o HOW (resolveRelatives, sub-deadline 5s, provider shortcut) migrou pro novo **ADR-25** no ARCHITECTURE.md. §9 colapsado num roster de 1 linha/agente + ponteiro pro AGENTS.md (descrições "does NOT review" vivem lá, canônicas). 2 frases de restatement puro cortadas (url-metadata, backup-streaming) — códigos/constantes intactos. Registrado: corte real (~20-25%) exigiria migrar rationale de ~8-10 bullets do §4 pra ADRs; não feito por escolha (super conservador). Docs-only, sem código. |
| 2026-06-13 | **testdb alinhado a Postgres 18.2 (test↔prod) + guard anti-drift**               | Bug de consistência: `internal/testdb` subia testcontainers em `postgres:16-alpine` desde o commit inicial enquanto compose/prod (`docker-compose.db.yml` + `docker-compose.services.yml`) rodam `18.2-alpine` — `git log -S` confirmou que nunca estiveram alinhados (não foi drift gradual). Passava despercebido porque o SQL usado é cross-version (16/17/18). Fix: extraído `const pgImage = "postgres:18.2-alpine"` em `testdb.go`, usado no `pgmod.Run`. O bump revelou fragilidade latente de **cold pull**: o budget de 90s do `New(t)` estourava ao puxar a imagem de 404MB sob carga paralela (vários pacotes sobem container ao mesmo tempo) → vários `context deadline exceeded`; elevado pra 240s (absorve cold pull em runner fresco/CI; warm continua em segundos). Novo `drift_test.go` (tag integration, sem container — leitura de arquivo) lê o pin `image: postgres:` dos dois compose files e falha se divergir de `pgImage` — torna o invariante imposto pelo CI em vez de comentário. CLAUDE.md §1 atualizado: versão pinada em 3 lugares que andam em lockstep. Sweep de 5 agentes (Code Review, **Code Quality**, Test Quality, Performance, Security) — 0 HIGH; o guard test e a correção de doc-accuracy (nota citava só 1 dos 2 compose) saíram de recomendações do sweep. Suíte de integração completa verde no PG18.2 (66.5%). |
| 2026-06-13 | **Auditoria ultra-deep (20 agentes) + 1 HIGH + 4 quick-wins**                    | Auditoria multi-agente (segurança/perf/arquitetura/dirty-code/complexidade/test-quality) com verificação adversarial: 1 HIGH, 10 MEDIUM, 30 LOW. **HIGH fechado:** `backup.Restore` (o código mais destrutivo do sistema) não tinha nenhum teste a nível de DB — novo `internal/backup/restore_integration_test.go` (6 tests): wipe preserva IDs originais + bump de sequence + troca de prefixo MinIO; skip pula colisões URL/nome, re-keya `link_tag`/`click_log` pro id sobrevivente, idempotente p/ entidades UNIQUE; **assertado que skip RE-INSERE click_logs em re-run (3→6→9) — NÃO-idempotente p/ click_log/folders** (clicks não têm chave natural; quirk vs §4 "idempotent by default", candidato a ADR); duplicate renomeia tag→`(2)`, folders sempre novas, link URL-colidente cai pra skip+warning; `uniqueLinkSlug` sufixo `-2` coberto; applyFiles rejeita `..` (path traversal) e prefixo fora de `screenshots/`/`images/`. Restore subiu de ~0% pra 57-81% nas funções de restore. **Quick-wins:** (1) `importer/json.go` clampa `click_count` em `maxImportClickCount=1_000_000` — fecha bomba de `generate_series` (DoS via JSON import); +3 testes; sweep de segurança confirmou que todo path passa por `Validate`. (2) `gofmt -w` em 22 arquivos + novo alvo `make fmt-check` + step no CI + gate §6.1 atualizado (drift formatava em silêncio porque o gate só rodava `go vet`). (3) `App.tsx`: callbacks dos cards wrapped em `useCallback` — `React.memo` de LinkCard/FolderCard estava derrotado (toda re-render do App re-renderizava todos os cards); sweep de perf confirmou fix EFETIVO. (4) `ci.yml` ganhou `permissions: contents: read` top-level. Dead code removido em `storage/minio_test.go`. Sweep §9 (4 agentes) verde: 0 blockers, 0 HIGH security/perf. Coverage gate informacional + 303 frontend tests verdes; gofmt/vet/typecheck limpos. |
| 2026-06-05 | **oEmbed enrichment p/ YouTube/Vimeo + discovery genérico (v1.4.0)**                | Usuário reportou que YouTube auto-fill em containers Docker retornava `"- YouTube"` em vez do título real. Root cause: YouTube serve HTML degraded para fingerprint de container (IP/TLS — UA/headers/cookies não ajudam). Solução: `preview.Fetcher.Fetch` agora curto-circuita pro endpoint oEmbed quando o host bate `knownOEmbedProviders` (YouTube + variantes m./music./youtu.be + Vimeo); pra resto do tráfego, `parseHead` captura `<link rel="alternate" type="application/json+oembed">` e enriquece campos faltantes via segundo fetch (HTML always wins; oEmbed só backfills). Reusa o mesmo `*http.Client`/`NewSafeTransport` — SSRF posture preservada (CLAUDE.md §4). **HIGH security finding fechado**: scheme guard em `fetchOEmbed` rejeita `file://`/`gopher://`/`unix://` antes do `http.NewRequestWithContext`, porque o IP-level dialer não dispara pra esses schemes. **Bug real fechado** (surfaceado pelos sweeps): `OEmbedURL` agora passa por `resolveRelatives` antes do segundo fetch — antes, paths relativos (WordPress/SoundCloud/Flickr) falhavam em silêncio. Plus: sub-deadline 5s por leg oEmbed (não inherita budget esgotado do parent ctx), TrimSpace check no shortcut (whitespace-only title não satisfaz). 13 unit tests novos cobrindo: hardcoded routing case-insensitive, parseHead capture, mergeOEmbed contract, shortcut fall-through em erro/title vazio, scheme rejection (6 sub-cases), body cap 64 KiB graceful truncate, HTML image wins over oEmbed, relative URL resolution. Backend coverage 100% em oembed.go. v1.4.0 (feat backwards-compat — preview worker async também ganha enrichment como bônus). |
| 2026-06-05 | **Auto-fill de Título/Descrição ao adicionar novo link**                          | LinkDialog passa a chamar um novo endpoint síncrono `GET /api/links/url-metadata?url=…` em debounce de 500 ms enquanto o usuário cola/digita uma URL, e pré-preenche Title + Description quando vazios (nunca sobrescreve campo já digitado). Reusa `preview.NewFetcher` via adapter em main.go — mesmo SSRF guard duplo (IMDS sempre bloqueado, RFC1918 gated por `PREVIEW_STRICT_SSRF`) que o worker async. Backend: novo `internal/links/metadata_handler.go` com 8 unit tests cobrindo trim, URL ausente, > 2 KiB, schemes não-http(s) (javascript/file/mailto/ftp/data), 502 mascarado de erros internos, 503 quando fetcher não wired. Wiring opcional via `Handler.WithMetadataFetcher` — não quebra callers existentes. Frontend: novo `useFetchUrlMetadata` mutation hook em `api/links.ts`; `LinkDialog` ganha `useEffect` [url, link] com `setTimeout` + `AbortController` (cancela request em voo a cada keystroke novo). 5 testes novos: happy path, never-overwrite (com `fireEvent.change` por causa do focus restoration pollution do `useFocusTrap` entre testes), invalid URL = zero requests, edit mode = skip total, 502 silencioso (UX não trava). Mock `installAxiosMock` em `test/server.ts` ganha extração de `config.params` para que axios calls com `{params: {url}}` cheguem ao route handler com query strings. ARCHITECTURE.md ganha linha na tabela API surface; CLAUDE.md mantido (invariants §4 sobre SSRF cobertos por design — não foi preciso novo bullet). Minor release (feat backwards-compat). |
| 2026-05-31 | **v1.1.2 — 3 follow-ups do sweep v1.1.1 (PR #12)**                               | Fecha as 3 pendências marcadas como follow-up no release v1.1.1: (1) checkpoint test pra worker idempotency em scan duplicado de mesmo link, (2) typed error em `push.Sender` quando endpoint é malformado, (3) hardening de `subscribe` handler validando body antes de salvar (rejeita keys vazias com 400 `invalid_payload` em vez de propagar pgError). Patch release. |
| 2026-05-31 | **v1.1.1 — sweep findings (PR #11)**                                              | **Security:** axios 1.16.1 (incluído o CVE bump pendente do release v1.1.0). **Test Quality:** 4 testes novos cobrindo o `Monitored` chip do PR #8 (rendering condicional por `check_interval`, sem chip quando NULL, tooltip mostra intervalo formatado, `data-tooltip` segue invariant §5 vs `title` nativo). Patch release. |
| 2026-05-31 | **v1.1.0 — Monitored chip em LinkCard (PR #8)**                                  | Chip "Monitored" sempre visível em cards cujo `check_interval IS NOT NULL`. Renderiza ao lado dos chips de tag, com cor neutra (não compete com a badge âmbar de "atualizado"). Tooltip mostra o intervalo formatado via i18n (`time_*` keys, plurais `_one`/`_other`). Minor release (feature). |
| 2026-05-31 | **CI speedup (PR #7)**                                                            | Backend job dedup: rodava unit suite duas vezes (uma standalone, uma dentro do coverage). Removida a primeira; `make coverage-backend` já loga falhas. Frontend job: `bun run test` removido em favor de `bun run coverage:nogate` único. Backend ~4m → ~2.5m; frontend ~2m → ~1.2m. |
| 2026-05-31 | **`scripts/release.sh` portable em macOS BSD (PR #10)**                          | `sed -i ''` (BSD) vs `sed -i` (GNU) divergem. Script ganha detecção de plataforma + flag correto. Bloqueava `make release-patch` no host do dev — antes só rodava em CI Linux. |
| 2026-05-31 | **CLAUDE.md §6.2 — bump after every merge (PR #9)**                              | Política explícita em prosa: cada merge a main = `make release-{patch,minor,major}` + push da tag. `:latest` é tag móvel, não release. Tabela de critérios (feat → minor, fix → patch, mix → minor). Documentado deferral one-shot quando o dev opta por batch. |
| 2026-06-14 | **Camadas de segurança no CI: SAST + DAST + Dependabot (ADR-26)**                 | **SAST:** 3 engines em paralelo — CodeQL (`security-extended`, Go matrix `build-mode: manual` porque go.mod fica em `backend/`, + JS/TS `build-mode: none`) em `codeql.yml`; Semgrep (container `v1.166.0` via `docker run`, packs security-audit/secrets/owasp-top-ten/golang/typescript/react/dockerfile/github-actions) + gosec (`v2.27.1`, `go install` + `$GITHUB_PATH`) em `sast.yml`. Ambos sobem SARIF via `github/codeql-action/upload-sarif` (SHA-pinned `v3.36.2`) pra Security ▸ Code scanning. **DAST:** `dast.yml` — OWASP ZAP baseline (passivo) mensal (`cron 0 6 1 * *`) + `workflow_dispatch`. Pré-gera cert self-signed em `web/certs` (mount `:ro`, entrypoint não escreve em ro), `docker compose --build`, espera `/healthz` (backend 9089 + web 9444), roda `ghcr.io/zaproxy/zaproxy:stable zap-baseline.py -t https://web` na rede `foldex`, sobe relatório HTML/MD/JSON (artefato 30d), teardown `down -v`. **Dependabot:** `dependabot.yml` 5 ecossistemas (github-actions /, gomod /backend, npm /web, docker /backend + /web), grupos minor+patch. Todos **informativos** (`|| true`/`-no-fail`/`continue-on-error`, postura CLAUDE.md §2). **Sweep de 5 agentes** rodado; hardening aplicado: tag Docker do semgrep era `1.166.0` (sem `v` — `v1.166.0` não existia, teria quebrado o pull), corrigida e pinada por **digest**; ZAP idem (digest, não `:stable`); cert efêmero `-days 365`; guarda `hashFiles()` no upload de SARIF; `paths-ignore` (md/docs) no SAST pra cortar runner em commit só-doc; health-check 60→90 tries; `--tail 200` no `compose logs`. Falsos-positivos descartados após verificação: nginx `/healthz` faz `proxy_pass` ao backend (health check OK), Go 1.26 existe (`go1.26.4`), `web/certs/*.pem` gitignored. Novo job `actionlint` (digest-pinned, +shellcheck) em `ci.yml` linta workflows em cada PR. Validado `actionlint` exit 0 nos 4 + YAML parse. Docs: README + README.pt-BR (tabela de scanning), ADR-26. Sem código de app tocado. |
| 2026-05-30 | **Change-detection per-link + Web Push**                                          | **Schema (mig 000010):** `link.{check_interval,last_checked_at,last_fingerprint,last_change_detected_at,change_seen_at}` (todos nullable) + CHECK em check_interval IN (hourly/daily/weekly) + 2 índices parciais (scanner due + sidebar recent). **Schema (mig 000011):** `push_subscription(endpoint UNIQUE, p256dh, auth, last_used_at)` — single-user, sem user_id. **Backend (`internal/changecheck`):** worker novo (256-cap channel, atomic.Bool stopped, sync.Once Stop, mesmo skeleton do `preview.Worker`) com ticker + `findDue` SQL CASE WHEN intervalo. `Fingerprinter` híbrido: extrai `<link rel="alternate" type="application/(rss\|atom)+xml">` e hashea GUIDs/IDs sorted; fallback content hash em `<main>`/`<article>` (whitespace-normalized, sem `<script>`/`<style>`/`<nav>`/`<header>`/`<footer>`). Prefixo `feed:`/`content:` no hash armazenado bloqueia falsos positivos quando uma página ganha feed mid-life. **Backend (`internal/push`):** `vapid.go` LoadOrGenerate (env→state file→autogen+persist 0o600), `subscription_repo.go` (upsert por endpoint), `sender.go` fan-out via `webpush-go v1.4.0` (404/410 → DELETE row, 2xx → MarkUsed, transport errors → log+continue), `handler.go` com `/api/push/{vapid-key,subscriptions,test}`. 410 cleanup é defensive — implementa convenção RFC 8030. **Sender tem `WithNotifyFunc` pra testes bypassarem encryption ECDH validation. **Wiring main.go:** `pushSenderAdapter` bridge entre `*push.Sender` (push.Notification) e `changecheck.Sender` (changecheck.Notification) — evita import cycle. Push falha em goroutine separada (background context) pra não bloquear o RecordCheckResult durável. **2 endpoints novos em /api/links:** `GET /api/links/recent-changes?days&limit` (clampInt mesmo padrão de stats), `POST /api/links/{id}/seen-change` (idempotente, 404 se never-detected). **DTO tri-state:** CheckInterval (`null` = opt-out clears all change-check state). **Frontend:** novo SW custom em `web/src/sw.ts` (vite-plugin-pwa migrou de `generateSW` pra `injectManifest`) com `push` + `notificationclick` listeners, hand-rolled cache (sem workbox-* runtime). `useWebPush`/`useSubscribePush`/`useUnsubscribePush` hooks. `PushToggle` sino no Topbar (4 estados: unsupported/denied/off/on). `LinkDialog` ganha `<select>` de frequência. `LinkCard` ganha badge âmbar "unseen change" (`fx-card-update-alert` + ícone bell, click marca seen via optimistic update). `TagSidebar` ganha seção `RecentChangesSection` (refetch 60s, cap 10 items). `useMarkChangeSeen` + `useRecentChanges` em api/links. **i18n:** 16 keys novas (link_dialog.check_updates_*, link_card.mark_seen_aria/update_detected_tooltip, sidebar.recent_updates, topbar.push_*, common.time_*) em en/pt/es. Plurals `_one`/`_other` no time_*. **Docker:** envs CHANGECHECK_* + VAPID_* no `docker-compose.yml`, volume `foldex-data:/data` pra persistir vapid.json autogerado. nginx CSP ganha `worker-src 'self'; manifest-src 'self'`. extension/manifest ganha `"notifications"`. **Cobertura:** 21 testes novos em changecheck (fingerprint table-driven com edge cases de feed reorder/kind switch + worker mocks lifecycle/process branches), 17 em push (vapid LoadOrGenerate 7 cenários, sender status routing + transport-error-doesnt-bubble, handler validation 6 cases). Frontend: lib/push (4) + lib/time (8) novos. DTO test pra tri-state CheckInterval em links. Backend 245+ → ~283. Frontend 245 → 259. Build, vet, typecheck verdes; mock server em `test/server.ts` ganha handlers `/api/push/*` + `/api/links/recent-changes` + `/api/links/:id/seen-change` pra não quebrar testes existentes. |

| 2026-06-29 | **Hardening de imagem Docker: non-root + apk upgrade + attestations**            | Scan Docker Scout achou 4 gaps. **(1) `foldex-web` non-root:** nginx passou a escutar em `8080/8443` (não-privilegiados), `Dockerfile` adiciona `USER nginx` (UID 101 do base image) após pré-criar e chownar `/etc/nginx/certs` e `/var/cache/nginx`. `docker-compose.yml` mapeia host `9088→8080` / `9444→8443` (URLs operator-facing unchanged). **(2) `foldex-backend` CVEs fixáveis:** adicionado `apk upgrade --no-cache` antes do `apk add` (mesmo padrão que já vigorava no `web/Dockerfile`), atualizando musl/openssl/etc. **(3) Attestations em ambos:** `release.yml` ganhou `provenance: true` + `sbom: true` no `docker/build-push-action` — SLSA provenance + SBOM attachados ao digest como OCI referrers (sobrevivem ao manifest reassembly do job `publish-manifest`). **Docs:** README/README.pt-BR/ARCHITECTURE atualizados sobre ports internos 8080/8443. |
| 2026-06-30 | **Notes: pastebin-style rich-text entries, entidade de 1ª classe (ADR-27)**       | Feature grande em 2 PRs. **PR1 (backend):** migration 000014 polimorfiza `link_tag`/`click_log` via `entity_kind ∈ {link,note}` (FK pra `link(id)` dropada — cascade virou app-level) e cria a tabela `note`. Corrigido o blast radius que a migration causava em `links`/`backup`/`exporter`/`importer`/`stats`/`tags` (todo SQL contra as duas tabelas precisou filtrar `entity_kind` — achado via falha dos testes de integração, não previsto na pesquisa inicial). Novo `internal/pkg/htmlsanitize` (bluemonday, allowlist fechada batendo no output do Tiptap StarterKit, sem `data:`) sanitiza `body_html` em todo write — DTO, repository (defesa em profundidade) e **os 3 modos de restore de backup** (gap HIGH achado pelo sweep: restore escreve direto em SQL, pulando o DTO). Novo `internal/notes` (CRUD + upload de imagem + rota pública `GET /n/{id-or-slug}` que renderiza — não redireciona — e loga click). Novo `internal/entries` — projeção read-only `UNION ALL` sobre link+note (`GET /api/entries`) que virou a fonte única do grid intercalado (`ORDER BY` precisou ficar numa derived table por causa da restrição do Postgres contra expressões tipo `lower(title)` direto sob um set operation). Sweep de 5 agentes achou 1 HIGH (o gap do backup acima) + 3 blockers de Code Review (folder cascade-delete e o wipe-mode do importer também deletavam `link` direto sem purgar `link_tag`/`click_log` primeiro, órfãos silenciosos; `testdb.Reset` sem `note` na lista de TRUNCATE) — todos corrigidos + re-verificados contra o diff patched. **PR2 (frontend):** editor Tiptap 3 (StarterKit já inclui Link — não precisa do pacote `@tiptap/extension-link` separado) com upload de imagem via paste/drop (nunca `data:` inline). Grid/busca trocou de `useLinks` pra `useEntries` em Home/CardsView/ListView/CompactGrid — helper `mergeAlphaCells` compartilhado extrai a lógica de interleave que já estava duplicada 2x (virando 3x com notes). `NoteCard` com badge esmeralda kind-discriminator (canto oposto ao pin badge, sempre visível — não condicional). Drag-merge-to-folder generalizado pra link↔link/link↔note/note↔note via `MergeSource`. Bug real achado pelos testes: `editor.commands.setContent` quebrava com "Cannot read properties of null" quando o efeito de reset rodava antes do Tiptap terminar de inicializar o `commandManager` — fix com guard `editor.isInitialized`. Verificação manual em browser real (`make up-build` + Postgres real) achou um segundo gap real: nginx (`web/nginx.conf`) e o proxy do Vite dev (`vite.config.ts`) tinham `/go/` mas não `/n/` — a página pública caía no fallback do SPA em vez de bater no backend; ambos corrigidos e re-testados. Sanitização confirmada em produção-like (script tag stripada num POST real contra Postgres real). 394 testes frontend novos/atualizados, suite backend + frontend 100% verde. **Sweep de 5 agentes do PR2** achou 3 HIGH: (1) blocker real de perda de dados — o guard `editor.isInitialized` sozinho não bastava, pois o efeito de reset (deps `[open, note, defaultFolderId]`) não re-rodava quando o editor terminava de inicializar DEPOIS do `note` já ter chegado (cache de 30s do TanStack Query cobre esse caso), deixando o doc vazio e sobrescrevendo `body_html` no save; corrigido separando em dois efeitos, o segundo com `editor?.isInitialized` como dependência explícita (re-roda no exato render em que a inicialização vira `true`); teste de regressão novo prova round-trip do body carregado; (2) `NoteDialog` reusava a key i18n `link_dialog.pending_tag_color_hint_html`, cujo texto (nas 3 locales) dizia literalmente "quando você salvar o **link**" numa tela de nota — nova key `note_dialog.pending_tag_color_hint_html` em en/pt/es; (3) Tiptap/ProseMirror carregava eager no bundle principal (+140KB gzip em toda visita) porque `NoteDialog` era importado direto e sempre montado — `lazy()` + Suspense condicional resolve a montagem, mas só funcionou de fato depois de também corrigir `vite.config.ts`: a regra `id.includes('/react')` do `manualChunks` capturava `@tiptap/react` (path contém `/react`) e o comprimia de volta no chunk eager `vendor-react`; excluído `@tiptap`/`prosemirror` antes dessa regra. Também corrigidos nessa rodada: `MergeSource` morava em `NoteCard.tsx` e `LinkCard` importava de lá (layering invertido) — movido pra `api/types.ts`; callbacks `onMergeIntoLink`/`onMergeIntoNote` duplicados e mortos no topo de `App()` (a cópia real vive dentro de `CardsView`); `mergeAlphaCells` chamado sem `useMemo` no corpo do render. **Incidente**: um dos agentes de re-verificação (general-purpose, acesso total a arquivos) reverteu o fix de perda de dados no working tree enquanto tentava "reverter mentalmente" o código antigo pra comparar — pego via system-reminder de arquivo alterado e restaurado manualmente antes de prosseguir; lição registrada em memory. **Teste manual em browser real pós-rebuild** (`make up-build` contra dados reais do usuário) confirmou paste de imagem → upload → insert, badge esmeralda, busca full-text, `/n/{slug}` + click_count incrementando, e pinned-first no sort default — e achou um bug **pré-existente** (não introduzido por esta branch, já presente em `f9ef030`): `mergeAlphaCells` (herdado do merge inline duplicado que existia antes) ignora `pinned` inteiramente no sort Alfabético (A→Z/Z→A), contradizendo o comentário do próprio código e o invariante do CLAUDE.md §5 ("pinned sempre primeiro, todo sort mode"); afeta links também, não é notes-specific — não corrigido nesta PR (fora de escopo), registrado como followup. |

| 2026-07-01 | **Folder password protection (ADR-28)**                                          | Privacidade — não segurança dura (app single-user atrás de um `SHARED_SECRET` único) — mas enforced no backend, não só na UI. **PR1 (backend):** migration 000015 adiciona `folder.password_hash TEXT` nullable, bcrypt (`golang.org/x/crypto/bcrypt`, já estava em `go.sum` como indireto — zero deps novas). Dois mecanismos separados por design: (1) **redação sempre-ativa** — `folders.Repository.List`/`Get` zeram `preview_links`/`preview_folders` incondicionalmente quando `has_password=true`, sem checar token nenhum, então `FolderRapidView` (hover popover) já degrada de graça sem lógica extra no client; (2) **gate por token** cobre só as duas operações que revelam conteúdo real — `GET /api/entries?folder_id=X` e `GET /api/folders?parent_id=X` — exigindo `X-Foldex-Folder-Unlock` válido, senão `403 folder_locked`. Token (`internal/folders/password.go`) é HMAC-SHA256 incluindo o `password_hash` ATUAL da pasta no payload assinado — trocar/remover a senha invalida todo token emitido antes, de graça, sem lista de revogação. Secret HMAC segue o mesmo padrão env→file→autogen(`0600`) que `push.LoadOrGenerate` já estabeleceu pro VAPID, mesmo volume `foldex-data`. Trocar/remover uma senha JÁ existente exige `current_password` (decisão do usuário, sem bypass de admin — recovery é edição direta no banco); setar pela primeira vez não exige nada. Sem cascata pra subpastas (decisão do usuário: modelo mais simples). `password_hash` atravessa backup/restore **verbatim** nos 3 modos (wipe/skip/duplicate) — nunca re-hasheado. **Sweep de 5 agentes do PR1** achou 1 HIGH: `folders.Repository.Update` cresceu demais misturando 3 preocupações (field-diff builder, autorização de senha, cycle-check) — gocyclo 25→20 / gocognit 34→23 depois de extrair `checkPasswordChangeAuthorized`/`checkParentCycle` como helpers nomeados escopados à tx; resto limpo ou trade-off já documentado no ADR-28 (sem rate-limit além do custo intrínseco do bcrypt, sem gate em export/import/backup). **PR2 (frontend):** `usePasswordPrompt`/`PasswordPromptProvider` espelha exatamente o padrão `useConfirm`/`ConfirmProvider` já existente — a modal possui o fluxo de unlock inteiro (não só coleta a senha), permitindo erro inline + retry sem fechar. `App.tsx` ganhou um chokepoint único, `requestOpenFolder`, que toda superfície que "abre" uma pasta (FolderCard, ListView, CompactGrid, Command Palette) passou a usar em vez de `setOpenFolder` direto — resolve o id contra `allFolders.data` (lista flat já carregada) em vez de exigir que cada call site passe o objeto `Folder` inteiro. Estado de desbloqueio (`unlockedFolders`) é puramente em memória (decisão do usuário: sessão de browser, não sobrevive a reload) — mesmo precedente do `folderPath` já ser in-memory-only. Handling defensivo pra token stale (senha trocada em outra aba): um 403 `folder_locked` no folder atualmente aberto limpa o token guardado e volta um nível. Ícone de cadeado novo (`I.lock`) inline antes do nome em TODO lugar que uma pasta renderiza (card, list row, compact row, palette, folder picker) — sempre selecionável como alvo de drag/move mesmo trancada (só *entrar* é que é gateado). **Sweep de 5 agentes do PR2** achou 0 HIGH; fechados os achados reais: (1) MEDIUM de Code Quality — `FolderDialog.submit`'s catch relançava (`throw e`) qualquer erro que não fosse `wrong_password`, virando unhandled promise rejection sem feedback nenhum pro usuário (network failure, 500 inesperado); trocado por um `saveError` genérico exibido perto do footer, separado do `passwordError` (que continua escopado a "senha atual errada"), com key `folder_dialog.save_error_generic` nas 3 locales. (2) MEDIUM de Performance + non-blocker de Code Review — `requestOpenFolder`'s `useCallback` dependia do objeto inteiro `unlockedFolders`, recriando a callback (e cascateando por `onOpenFolder` até todo `FolderCard`/`FolderRow`/`CompactFolder` memoizado) a cada unlock; decoupled via um `unlockedFoldersRef` sincronizado por effect, lido no call-time em vez de via dependency — callback agora só muda quando `allFolders.data`/`passwordPrompt`/`setOpenFolder` mudam. Approveitado pra também corrigir uma inconsistência que o mesmo Code Review achou: `currentUnlockToken` lia `unlockedFolders[id]?.token` sem checar `expiresAt`, diferente do check que `requestOpenFolder` já fazia — extraído um helper `validUnlockToken` único usado nos dois lugares. (3) LOW de Security — inputs de senha sem `autoComplete`, convidando o password manager do browser a salvar como login do site; adicionado `autoComplete="new-password"` nos campos de senha NOVA e `"off"` nos de senha ATUAL/desbloqueio. (4) MEDIUM de Code Quality — ícone de cadeado tinha tooltip em `FolderCard`/`ListView`/`CompactGrid` mas não em `CommandPalette`/`FolderPicker`; confirmado que `TooltipPortal` resolve via `closest('[data-tooltip]')` (nesting seguro, sem conflito com o tooltip do row pai) e adicionado nos 2 sites que faltavam. Test Quality apontou 4 gaps "críticos" de cobertura de estado de sessão — todos fechados: teste de "já desbloqueada nesta sessão não repergunta", teste de recuperação de token stale (senha trocada em outra aba — achado um bug real de TESTE no processo: `userEvent.type` na caixa de busca dispara o onClick do palette [mesma pegadinha já documentada num comentário existente em `App.test.tsx` linha 66], silenciosamente não digitando nada; trocado por `fireEvent.change` no input via `aria-label`), teste de reset do sub-fluxo change-password ao cancelar e reabrir na MESMA pasta, teste de não-vazamento de estado ao reabrir o `FolderDialog` numa pasta DIFERENTE. 414 testes frontend (18 novos: 5 `PasswordPromptDialog`, 7 `FolderDialog`, 6 `App.tsx` cobrindo o fluxo completo do gate — incluindo o jump via Command Palette, no-reprompt e recuperação de token stale), suite backend + frontend 100% verde. |

| 2026-08-04 | **PR2 do ADR-30 — identidade: sessões, CSRF, RBAC, convites e as telas de auth** | Segunda fatia da autenticação multi-usuário; `AUTH_ENABLED` continua `0`, então nada muda para uma instalação existente. **Backend:** `internal/pkg/secrets` (tokens opacos de 32 bytes + `sha256` de armazenamento + código numérico por rejection sampling — `%` enviesa dígitos baixos em ~2.4% porque 256 não é múltiplo de 10); `internal/pkg/attemptlimit` (promoção do limiter por pasta do ADR-28 para chave `string`, preservando o `Begin`/`Release`/`CommitFail`/`CommitSuccess` que é o que segura o cap sob concorrência — check-then-act deixaria N palpites paralelos passarem enquanto o bcrypt roda — mais um `Sweep` novo, porque o bucket de login é chaveado por e-mail fornecido pelo atacante e sem eviction o mapa é vazamento de memória); `internal/mailer` (drivers `smtp` + `log`, este o padrão para que uma instância sem SMTP ainda consiga convidar alguém — o log É a caixa postal, e imprimir o link ali é deliberado); `internal/auth` (repositório sobre as tabelas de identidade, rotação de refresh em UMA transação `SERIALIZABLE` com detecção de reuso, CSRF double-submit assinado, middlewares `Authenticate`/`Optional`/`RequireAdmin`, convites, sweeper). Superfície `/api/auth/*` (bootstrap-status, bootstrap, login, logout, logout-all, refresh, me, sessions, invites) + `/api/admin/*` (users, invites). **Frontend:** `AuthProvider`/`AuthGate` acima do `<App/>` em `main.tsx` — acima, e não como membro do union `View`, porque `App` chama `useEntries`/`useFolders`×2/`useTags` incondicionalmente ANTES de olhar `view`, então um `view === 'login'` dispararia 4 queries autenticadas em toda montagem deslogada; `client.ts` ganhou `withCredentials`, injeção de CSRF nos verbos inseguros e **refresh single-flight** de escopo de módulo (os 4 401s simultâneos da montagem compartilham UM `POST /refresh`, senão bateriam no detector de reuso do próprio backend); telas de login, setup e aceite de convite; `auth.css` escopado em `.fx-auth` com keyframes `fx-auth-*` (um `@keyframes fx-spin` sem prefixo redefiniria o spinner do app inteiro); `useDarkMode` extraído de `App` para rodar ACIMA do gate, senão o usuário dark vê um login branco e um flash ao entrar. 14 namespaces i18n novos em en/pt/es + `locales.test.ts` de paridade (não havia teste de paridade; uma key faltando não lança, o i18next só renderiza a key crua). **Bug real corrigido durante a implementação:** a primeira versão da janela de graça regravava o trio de tokens na PRÓPRIA linha da sessão — o que derrota o propósito da janela, porque o servidor só guarda hashes e portanto não consegue devolver o trio que a requisição vencedora instalou; as duas requisições vêm do mesmo cookie jar, então o browser fica com a resposta que chegou por último enquanto a linha guarda a outra, e a aba perdedora é deslogada. Passou a emitir uma sessão IRMÃ herdando `family_id` (morre junto na detecção de reuso) e `created_at` (senão dava para surfar a janela e empurrar o teto absoluto de 90 dias para sempre). Achado por teste, não por revisão. **Bug de produto pré-existente achado no caminho:** o teste "cycles color on a pending tag chip" era flaky desde antes do PR1 e estava reportando um defeito real — `LinkDialog` foca o campo URL um frame após montar (workaround de iOS Safari) e roubava o foco de quem já tinha começado a digitar, espalhando metade da palavra no campo errado; corrigido com um check de `document.activeElement` e travado por teste de regressão nomeado. **Dois problemas de infraestrutura de teste corrigidos centralmente:** i18n é singleton por worker e a suíte do LocalePicker deixava o idioma trocado para quem rodasse depois (duas suítes já contornavam isso com um `beforeEach` próprio) — reset movido para o `afterEach` compartilhado; e `vitest.config.ts` ganhou `testTimeout: 20s`, porque o default de 5s é folgado no run normal (~30ms/teste) mas não sob instrumentação de cobertura v8, onde os testes de diálogo mais pesados falhavam como timeouts que não diziam nada sobre o código. `src/api/client.ts` **saiu da lista de exclusão de cobertura**: deixou de ser um wrapper fino de axios quando passou a hospedar o CSRF e o refresh single-flight. **CORS:** `AllowCredentials: true` (a sessão é cookie), `+PUT` (corrige bug pré-existente — `settings/handler.go` sempre montou `PUT /master-password` e nenhum caller cross-origin conseguia alcançá-lo; invisível porque produção é same-origin atrás do nginx), `+Authorization`, `+X-Foldex-CSRF`, e substituição do wildcard em boot com aviso, já que a Fetch spec proíbe `*` junto de credenciais. **Verificação:** guardas críticas mutation-testadas uma a uma (CSRF desligado → 2 testes falham; reuso sempre tratado como graça → 1; `RevokeSession` sem escopo de usuário → 1; `queryClient.clear()` removido → 1; sanitizador de header do mailer → 2). O teste de cache do `AuthProvider` estava passando por motivo errado — o client de teste usa `gcTime: 0`, então qualquer `setQueryData` sem observer é coletado na hora e a asserção "cache foi limpo" valia independentemente do produto; corrigido com um client dedicado. `internal/mailer` ganhou um servidor SMTP falso de verdade (listener real) em vez de mock do `smtp.Client` — o que precisa de teste É a conversa do protocolo, e mockar o client provaria só que o código chama os métodos que chama. Docs: README + README.pt-BR (seção "Accounts & sign-in" / "Contas e login"), ARCHITECTURE (ADR-30 com status e as duas correções), CLAUDE.md §2 (exclusões de cobertura) e §4 (6 invariantes novos de sessão/CSRF/rotação/login/RBAC), `.env.example`, `docker-compose.yml`, `docker-compose.mail.yml` + `make up-mail` (Mailpit). |

| 2026-08-05 | **PR3 do ADR-30 — 2FA (TOTP, códigos de recuperação, OTP por e-mail) e recuperação de senha** | Terceira fatia; `AUTH_ENABLED` continua `0`. **Migration `000019_two_factor_indexes`** — começou sem migration nenhuma (a 000017 já criara as cinco tabelas), e ganhou uma no fim, vinda do sweep: dois índices que faltavam (`email_otp(challenge_id, created_at DESC)` e `auth_challenge(user_id, purpose, consumed_at)`, ambos em statements do caminho de login alcançáveis sem sessão) mais a coluna `mailbox_already_proven`. É `000019` e não `000018` porque o PR1 já entregara `000018_click_log_user_id` — versão duplicada faz o `golang-migrate` recusar o boot, e o applier do `testdb`, que só ordena nomes de arquivo, esconderia a colisão até produção. **Backend:** `internal/pkg/keyfile` (extraído agora, com dois consumidores reais, como o §7 pede) e `secrets.Cipher` (AES-256-GCM); `internal/auth/{totp,recovery,repository_2fa,twofa_handler,password_handler}.go`; `pquerna/otp v1.5.0` com parâmetros fixos em SHA1/6/30 e QR renderizado no servidor (`boombuler/barcode` entra indireto; zero dep nova no frontend e o seed base32 nunca chega a uma lib JS de QR). **Frontend:** `OtpInput` + telas de código, esqueci-a-senha/enviado, redefinir e enrollment obrigatório, mais a seção de 2FA em Configurações (sem ela, 2FA seria inalcançável para não-admins, já que o enrollment forçado só dispara para administradores). i18n em en/pt/es. **Bug de produto real achado pela suíte:** o discriminador entre "código de 6 dígitos" e "código de recuperação" filtrava a entrada para dígitos e perguntava se sobravam seis. Um código de recuperação tem 10 símbolos de um alfabeto de 32 (10 deles dígitos), então **~1 em 23** contém exatamente seis dígitos e era roteado para o caminho TOTP, onde nunca casa — o portador simplesmente não conseguia usá-lo, sem nada na resposta explicando. Apareceu como flake de ~4%; corrigido com `numericOTP` (comprimento SEM SEPARADORES) e travado por um teste que **constrói** o caso em vez de torcer pelo acaso. **Segundo bug real, no frontend:** após um `verify` bem-sucedido a tela de código voltava a aceitar submit, e como o código é de uso único o segundo request sempre falharia e pintaria erro por cima de um login que deu certo — a tela agora fica `busy` para sempre depois do sucesso, já que o gate a desmonta. **HIGH de segurança achado pelo sweep, e era meu:** o reset de senha diverge corretamente para o desafio de 2FA, mas o fallback de OTP por e-mail mandava o código para **o mesmo endereço** que recebeu o link de reset. Quem lesse a caixa postal fechava os dois passos num canal só: `/password/forgot` → link → `/password/reset` → desafio → `/2fa/email` → código na mesma caixa → sessão. O docstring do próprio handler dizia que o desvio existia "senão uma caixa postal comprometida burlaria o 2FA inteiro" — e o fator e-mail reabria exatamente isso. Fechado com `auth_challenge.mailbox_already_proven`; mutation-testado. Junto vieram: o fator e-mail passou a exigir `MAIL_DRIVER=smtp` (o driver `log` imprime o código no stdout — troca aceitável para link de convite, não para um segundo fator), `Optional` passou a verificar CSRF (o PR3 montou dois POSTs nele), os caminhos de step-up por sessão ganharam teto de tentativas (não tinham nenhum), `Verify2FA` passou a rechecar o status da conta (um desafio sobrevivia ao admin desabilitar a conta), e o token pré-auth cru parou de ser ecoado no corpo da resposta ao lado do cookie httpOnly que existe para escondê-lo.

**Test Quality achou que nenhuma TTL era provada:** apagar `AND expires_at > now()` dos três consumos deixava a suíte inteira verde — ou seja, um link de takeover de 30 minutos numa caixa postal viraria permanente sem nada notar. Os três testes novos (backdate de `expires_at`) falham com a mutação, e o link expirado chegava a emitir sessão autenticada completa.

**Decisão de desenho que virou API:** `keyfile.Config.AllowEphemeral` separa a chave de unlock de pasta (perder = usuário redigita a senha; segue com chave de sessão) da chave que cifra o seed TOTP (perder = todo seed indecifrável e todo usuário com 2FA trancado para fora; o backend **recusa subir**). **Verificação:** guardas mutation-testadas uma a uma — divert de 2FA no reset removido → falha; predicado `user_id` do código de recuperação removido → falha; replay guard do contador TOTP removido → falha; discriminador antigo restaurado → falha; guarda de dedup do `onComplete` removida → 3 testes falham; `autoComplete` em todas as células → falha (Safari preenche as seis com o mesmo dígito). Uma primeira tentativa de mutação foi **inválida** (`$1 IS NOT NULL` sem tipo faz o pgx errar, então a query falhava fechada e o teste passava por motivo errado) — refeita corretamente. **Pendências fechadas junto:** `internal/folders/ratelimit.go` deixou de ser uma segunda cópia de `attemptlimit` (~150 linhas quase idênticas + suíte duplicada), e no caminho fechou um DoS cross-tenant real: o slot de tentativa era reservado ANTES do lookup escopado por dono, então requests paralelos para o id de outra pessoa seguravam slots contra o orçamento dela e disparavam lockout de uma hora numa pasta que o chamador nem consegue ler; o teste de corrida do "último admin" passou a rodar no nível de repositório, onde a corrida existe de fato (via HTTP a resolução de sessão acontece antes da transação abrir e os trechos críticos raramente se sobrepõem — e sem o advisory lock a instância termina com zero administradores, confirmado por mutação). `captureMailer` ganhou mutex: a maioria dos envios é fire-and-forget de goroutine destacada, então o teste que fazia poll no slice corria com o remetente — data race genuíno que `-race` reporta e um run normal esconde. |

| 2026-08-05 | **PR3 — os três itens que faltavam: redação de log, proxy confiável e confirmação de e-mail** | Fechamento do PR3 depois de eu ter declarado "completo" cedo demais e conferir só quando perguntado. **`logsafe.RedactHandler`** envolve o `slog.Handler` RAIZ em `main.go` e apaga o valor de todo atributo cuja chave nomeia credencial. Verifiquei antes que **nada loga segredo hoje** — os hits do grep eram nome de tag HTML, corpo JSON de resposta e código de erro do S3 — então isso é guarda, não correção: cobre o próximo `logger.Info` escrito às pressas durante um incidente. Redige através de `WithAttrs` (um `logger.With("token", raw)` guarda o atributo uma vez e o repete em todo registro, então um handler que só limpasse `Handle` passaria no teste óbvio e vazaria em cada linha) e resolve `LogValuer` antes de checar a chave, senão um tipo que se renderiza como grupo de credenciais escapa. **`TRUSTED_PROXY_IPS`** substitui o `middleware.RealIP` do chi, que honra `X-Forwarded-For` incondicionalmente: certo atrás do nginx, forjável em bind direto, onde torna decorativo todo bucket por IP. Default é não confiar em ninguém — endereço falsificável é pior que grosseiro, porque deixa o atacante escapar do próprio bucket E jogar o custo no de outro. A cadeia é percorrida da DIREITA para a esquerda pulando hops confiáveis; a entrada mais à esquerda é o que o cliente mandou. Um teste meu tinha a expectativa ERRADA (esperava fallback quando o lixo estava à esquerda do hop escolhido) — o código estava certo, e corrigir a expectativa me obrigou a escrever a semântica com precisão. **Confirmação de e-mail virou LINK.** Eu havia implementado código de 6 dígitos em rota autenticada; a §4.2 sempre disse `token`, e a razão aparece no caso comum — o link é aberto no cliente de e-mail, num aparelho que nunca fez login, e exigir sessão o encontraria com um formulário e um token expirando atrás. `POST /api/auth/email/verify` resolve por hash sem `user_id`, o que só é seguro porque o token tem 256 bits (um código de 6 dígitos buscado assim seria adivinhável contra toda a base de uma vez), e responde o mesmo `404 verify_invalid` para desconhecido, expirado e gasto. A tela consome `?verify=` com guarda de ref: o token é de uso único, então o duplo-mount do StrictMode gastaria na primeira chamada e mostraria "link inválido" vindo da segunda. |

| 2026-08-06 | **PR4 do ADR-30 — federação Google, tokens de API, tela de administração e `AUTH_ENABLED=1`** | Fatia final. **Migration `000021_credential_coherence`**: `auth_challenge.oauth_{provider,subject,email}` (o `sub` do Google precisa sobreviver do callback até o POST de conversão sem passar pelo browser — se o request pudesse nomear um subject, quem provasse a senha anexaria uma conta Google diferente da autenticada) e um `CONSTRAINT TRIGGER DEFERRABLE INITIALLY DEFERRED` garantindo que conta `active` sempre tem senha OU identidade. Trigger e não CHECK porque a regra atravessa duas tabelas; DEFERRABLE porque a conversão a viola legitimamente DENTRO da transação — anular a senha e inserir a identidade são dois statements, e o que precisa valer é o estado no COMMIT. **Backend:** `internal/oauthgoogle` (PKCE S256 apenas, `prompt=select_account`, sem `offline` — foldex nunca segura refresh token do Google, então dump de banco não vira acesso permanente a caixa postal; `id_token` não é parseado, chamamos `/v1/userinfo`); `auth/{oauth_handler,repository_oauth,apitoken_handler,repository_apitoken}.go`; `/api/auth/oauth/google/{start,callback,convert}` + `DELETE /oauth/google` + `/identities`; `/api/auth/password/set`; `/api/auth/tokens`; `/api/admin/users/{id}/force-password-reset`. **Frontend:** tela `convert`, `GoogleButton`, `AccountSection` e `ApiTokensSection` em Configurações, `AdminUsersPage` (lazy — a maioria das contas não é admin, então o chunk seria peso morto), botão Usuários na topbar só para admin. **Extensão MV3** migrou para `Authorization: Bearer`, mandando as duas credenciais durante a transição para que backend e extensão possam ser atualizados em qualquer ordem; o "Test connection" deixou de bater em `/healthz` (público — provava só que o servidor respondia, e dizia "tudo certo" para um token errado) e passou a usar `/api/tags`. **Bug real que só o PR4 alcançava:** `userColumns` começava em `app_user.id` e seguia com colunas nuas. Funciona em toda query de tabela única — que eram todas, até agora. `UserByIdentity` seleciona a mesma projeção de `app_user JOIN user_identity`, e `user_identity` tem seu PRÓPRIO `created_at` e `last_login_at`: o Postgres recusa a query inteira por ambiguidade e o callback devolvia `server_error` sem nada apontando a causa. Três testes falharam com a mesma mensagem inútil (`expected "convert", actual ""`) porque meu helper `first()` descartava o `oauth_error` — trocado por um que imprime a falha, e a causa apareceu no primeiro run seguinte. **Decisões de segurança que viraram invariante:** e-mail coincidente abre CONVERSÃO exigindo a senha atual, nunca sessão (deliberadamente mais estrito que o reset de senha, que já entrega a conta a quem tem a caixa postal — a troca aceita é que "esqueci a senha, entro pelo Google" não funciona); OAuth nunca pula 2FA, em nenhuma das três saídas; ordem no `/api/admin` é papel PRIMEIRO e token depois, senão qualquer portador de token receberia 403 e aprenderia que a superfície existe; `/n/{42}` recebeu o mesmo desligamento de `/go/{42}` e com argumento mais forte, porque renderiza conteúdo em vez de redirecionar. **Decisão de testabilidade:** o handler consome uma interface `GoogleProvider` em vez de `*oauthgoogle.Provider` — os endpoints do Google são constantes de propósito (um token endpoint configurável pelo operador é canal de exfiltração fantasiado de knob), e a interface é o que permite exercitar a POLÍTICA contra um duplo sem abrir isso. Junto, `UserInfo.EmailVerified` virou `bool` simples com `UnmarshalJSON` na struct: a versão anterior usava um tipo não-exportado que tornava a struct inconstruível fora do pacote. **`AUTH_ENABLED` passa a `1`** — numa instalação existente a primeira visita cai na tela de setup, que reivindica o administrador de bootstrap e com ele todo o conteúdo já adotado pela 000017. `SHARED_SECRET` fica depreciado com aviso no boot. Docs: README + README.pt-BR (Google, tokens, tela de Usuários, depreciação, `PUBLIC_NUMERIC_IDS`), `extension/README.md`, ARCHITECTURE (ADR-31/32 para Accepted), SDD §15.3 com os onze desvios, CLAUDE.md §0 reescrito (o produto deixou de ser single-user) e §4 com 8 invariantes novos, `.env.example`, `docker-compose.yml`. |

| 2026-08-11 | **SEC-SEN-008 + RACE-HER-004 — 2FA admin sem exceções e confirmação CAS** | RED→GREEN em cinco regressões: bootstrap, convite admin, promoção com sessão viva, política ligada após a sessão/refresh e substituição do seed entre verify/confirm. Bootstrap e aceite por senha agora passam por `completeLogin`; promoção revoga sessões no mesmo commit da troca de papel; `RequireAdmin` consulta TOTP confirmado atual (403 para admin legado sem fator, 404 preservado para non-admin, enrollment alcançável). `ConfirmTOTP` compara ciphertext+nonce verificados antes de gravar `confirmed_at`/counter, então nunca ativa o seed substituto. Sem coluna de assurance e sem migration própria para o enrollment: revogação + gate central + identidade ciphertext/nonce fecham os caminhos. `internal/auth` e `internal/server` integration verdes. |
| 2026-08-11 | **SEC-SEN-009 — vínculo OAuth com step-up e state preso à sessão** | RED→GREEN: o `GET purpose=link` foi fechado e Configurações agora abre modal que faz `POST` com CSRF, senha atual e TOTP/recovery quando ativo; provas ficam no JSON e a API devolve `redirect_url`. Migration `000024_oauth_link_step_up` prende o state por cinco minutos a `user_id`, `session_id`, `token_version` e `proof_at`; callback valida antes do exchange e `LinkIdentity` revalida sob locks antes do INSERT. Logout, reset/troca de senha, revoke, outra sessão, expiração e replay retornam `state_invalid` uniforme sem vínculo. Senha e segundo fator têm budgets independentes por usuário. README en/pt-BR e SDD/arquitetura atualizados em paridade. |
| 2026-08-13 | **TEST-ARG-001/002 — coverage vira gate real no CI** | RED→GREEN com contrato shell estático: backend mantém uma única `coverage-run` e aplica `coverage-check` ao profile gerado sem `continue-on-error`; frontend troca o bypass `coverage:nogate` por um único `bun run coverage`, deixando o Vitest impor 85% em statements/lines/functions e 80% em branches. O harness falha se qualquer job perder enforcement, tolerar falha ou duplicar a suíte. O teste de access log do nginx também passou a integrar o job obrigatório. |
| 2026-08-13 | **N1-NEX-001 — import JSON/Netscape set-based** | RED→GREEN: o trace pgx media 27 chamadas para 4 links e 1.007 para 200; staging temporário + `CopyFrom`, preload owner-scoped de URLs, preload global limitado de slugs e batches de folder/tag/link/click/link_tag fixaram em 14 chamadas no skip e 15 no wipe para ambos os tamanhos. Idempotência `(user_id,url)`, wipe com purge polimórfico, cores/warnings e enqueue pós-commit foram preservados. Novo teto cumulativo de 1.000.000 clicks impede a amplificação 50k×10k. |
| 2026-08-14 | **DUP-ECH-004 — protocolo único para listas protegidas** | RED→GREEN: `folders.ListWithContentGate[T]` centraliza folder opcional → pre-check owner-scoped → closure `List` tipada → post-check contra mudança concorrente do hash. Entries/links/notes não possuem mais branches nem chamadas diretas a `ContentGate.Check`; teste AST trava essa ownership. Matriz de integração comum preserva fail-closed sem lookup/token, rejeição TOCTOU, API token de conteúdo, isolamento por owner e os três tipos de resposta. |
| 2026-08-14 | **RACE-HER-007 + N1-NEX-004 + LEAK-HYD-007/010 — change-check limitado e CAS** | RED→GREEN: o claim devolve a projeção completa do worker e eliminou o `SystemGet`/aggregate de cliques por link; gravações fazem CAS pelo `last_checked_at` reservado e só notificam quando aplicadas; mudanças reais de URL/intervalo reiniciam o baseline, enquanto payloads com valores iguais preservam estado. Push usa workers fixos, fila de 32 e contexto cancelável. Preview/change-check têm teto independente de 8 workers em config e constructors. |
| 2026-08-14 | **Primeira onda MEDIUM — persistência, restore, TOTP, IDs e extensão** | Fechados `BP-MEN-002`, `BUG-ART-003/004/005/016/021` e `TEST-ARG-008` com painel 3/3. Estado persistido exige guards de domínio; restore valida checksum obrigatório de DB/files antes de ledger/DB/objetos e distingue 400/422; confirmação TOTP e recovery codes são atômicos e retry-safe; IDs públicos não fazem wrap; falha de localStorage não aborta backup; callbacks MV3 propagam `runtime.lastError` até a UI. Backup integration completo, auth/notes focados, 73 testes web + typecheck e 13 testes da extensão passaram. |

| 2026-08-17 | **RBAC de quatro papéis, superfície de administração e política da instância** | ADR-33/34/35. Migrações 000032 (papéis `owner/admin/editor/viewer` + índice de owner único) e 000033 (`audit_log`). Matriz de 14 permissões em `authctx`, `RequirePermission`/`RequireWrite` no `authgate`, gate de escrita por grupo em links/notes/tags/import e por rota em folders/backup. Owner imutável com transferência atômica em um único UPDATE. Novos endpoints `/api/admin/{metrics,roles,audit,policy}` + `transfer-ownership`. Frontend: hub redesenhado no layout do mockup sobre os tokens `--fx-*` (dark preservado), matriz RBAC lida do servidor, tabela de usuários, auditoria paginada por keyset e tela de política. i18n completo em en/pt/es. |

| 2026-08-17 | **O hub redesenhado que não estava redesenhado — `.fx-shell button` engolindo os cards** | A entrada acima dizia "hub redesenhado no layout do mockup". Estava errada, e o usuário viu a tela antes de mim: os cards renderizavam como texto solto sobre o gradiente, sem fundo, sem borda e sem padding. **Causa raiz:** `.fx-shell button { border: 0; background: transparent; padding: 0 }` (foldex.css, topo) tem especificidade `(0,1,1)` e **vence** `.fx-hub-tile`/`.fx-acard` `(0,1,0)`. Toda a aparência dos cards estava escrita e toda ela era descartada pelo reset — enquanto `.fx-metric`, que é `<div>`, aparecia normalmente, o que é exatamente o padrão que se via na tela e que eu não li como sintoma. Regra de hover/focus não sofria (`(0,2,0)`), então o CSS "parecia certo" em leitura rápida. **Fix:** selectors qualificados por elemento (`button.fx-acard`, `button.fx-scut`) — empatam em `(0,1,1)` e ganham por ordem no documento. Baixar o reset com `:where()` foi rejeitado: `overrides.css` tem regras que dependem dele vencer classe de componente (`.fx-topbar .fx-cta` existe por isso). Virou invariante em CLAUDE.md §5. **O resto do mockup, que também faltava:** página passou de coluna de 860px (grudada à esquerda, dois terços da viewport vazios) para `.fx-hub-page` de 1240px centrada; cabeçalho ganhou lede e ações próprias (exportar backup / convidar); escopo pessoal ganhou card de identidade + nudge de 2FA **condicional** (um painel que sempre diz "está tudo bem" treina o olho a pular o espaço, e aí ele pula no dia em que importa); rótulos de seção viraram régua até a borda; administração ganhou o split `1.5fr/1fr` de papéis + pendências. `HubCard`/`HubShortcut` são um componente só usado pelos DOIS escopos — antes eram dois componentes iguais escritos com uma semana de diferença. **Lição:** "type-check + testes passam" não é evidência de UI correta; nenhum dos 1.047 testes olhava para estilo computado, e nenhum poderia. A validação em navegador do §6 é o passo que faltou, e foi pulada justamente na mudança grande. |

| 2026-08-17 | **PR1 do ADR-36 — outbox transacional de e-mail e templates i18n** | Migração `000034_mail_outbox`. O problema real que o usuário encontrou: "Esqueceu a senha?" ativa numa instância sem SMTP, respondendo `202` para um link que só existia no stdout do container. Abaixo disso havia três defeitos estruturais, e o primeiro era o grave — a entrega era in-process e **sem retry**: `mailer.Dispatcher` logava e **descartava** um `Send` que falhasse, e `Stop()` cancelava o que estava em voo em vez de drenar. **Decisão central: outbox transacional, não publish-após-commit.** O `Reserve/Publish/Release` existia por um motivo preciso — reservar a vaga na fila ANTES de persistir a credencial — e publicar depois do `COMMIT` reabriria exatamente esse buraco, pior ainda porque o cooldown de 60 s já foi cobrado e o usuário não consegue pedir outro. O `INSERT` participa da transação já aberta: não falha por capacidade, não se perde, ou os dois existem ou nenhum. É **mais forte** que o invariante que substitui, e como efeito o `defer Release()` saiu dos três handlers. `auth.Repository` ganhou `MailDraft{Locale, Build(rawToken)}`, executado DENTRO da transação — a assinatura com callback já existia em `CreateAdminPasswordRecovery`, então não é padrão novo. **Payload cifrado com AES-256-GCM** sob a mesma `AUTH_ENCRYPTION_KEY` do seed TOTP: uma linha na fila contém um link de reset vivo, e `password_reset` guarda só sha256 justamente para que um `pg_dump` não seja kit de sequestro — gravar o link em claro devolveria o que aquele desenho recusou. GCM e não CTR pela tag: sem ela, escrita na tabela é ataque de substituição do destino, e a vítima vê só um e-mail legítimo apontando para o lugar errado. **Desvio consciente do SDD:** o documento propunha o `Dispatcher` como sink do transporte `inproc`. Não implementei — ele descarta falha em silêncio, então o PR1 entregaria "durável" só até a primeira recusa de SMTP. O relay virou o próprio worker: envia, marca `published` só no sucesso, e no fracasso reagenda por `next_attempt_at` com backoff 1/5/15/30/60 min até `max_attempts`. Assim **retry, backoff e dead-letter existem sem broker nenhum**, que era a promessa da fase. O `Dispatcher` foi deletado; mantê-lo ao lado do relay seria código morto com aparência de camada. Backoff é COLUNA e não `sleep` no worker, que seguraria o slot e transformaria um destinatário lento em fila parada para todos; `claim_token` faz CAS na liquidação para que um relay que dormiu além do TTL não sobrescreva o resultado de outro; falha permanente (`unknown_template`, `undecryptable_payload`) liquida na hora em vez de gastar seis tentativas. **Templates:** um layout por braço + `strings.{en,pt,es}.json`, `embed.FS`, `html/template` escapando por contexto — some o `html.EscapeString` manual, que dependia de cada autor lembrar de chamá-lo. O outbox guarda `(template, params)` e não corpo renderizado, então uma correção de copy alcança mensagem já enfileirada e o `locale` continua significando algo. `missingkey=error` porque `<no value>` impresso num e-mail de reset é o defeito que chega em produção parecendo copy. Braço texto continua obrigatório; `session_revoked` e `reset_unavailable` continuam sem link. **Consequência:** `503 mail_queue_full` **deixou de existir** — um `INSERT` não falha por capacidade. Três testes travavam o invariante antigo e foram reescritos para o novo: credencial e mensagem comitam juntas, e transporte recusando deixa a mensagem enfileirada em vez de perdê-la. **Não adotado:** o force-reset administrativo continua síncrono, com rollback do token em falha de SMTP. É §12.1 do SDD e mudança de invariante de segurança — fica pendente de decisão explícita em vez de entrar de carona num refactor de entrega. |

| 2026-08-18 | **ADR-36 fases A e B — idioma no perfil, force-reset assíncrono e transporte AMQP** | Migração `000035_user_locale`. **Fase A.** `app_user.locale` nullable, editável pelo próprio usuário em Configurações → Perfil; NULL significa *sem preferência*, não inglês, então quem nunca escolheu segue caindo no `Accept-Language` de quem disparou. Resolução virou **destinatário → remetente → `en`**, o que fecha o custo que a fase 1 registrou: convite de admin anglófono agora chega no idioma do convidado. Sem CHECK no banco contra lista de idiomas — os catálogos vivem no binário e um CHECK só muda com migration, então as duas listas divergiriam no primeiro idioma novo, com o banco recusando algo que o binário sabe renderizar. Force-reset administrativo passou a ser assíncrono (§12.1): `503 mail_unavailable` deixou de existir, e a propriedade que motivava o envio síncrono passa a vir de durabilidade. **O bug que a fase A quase escondeu:** `userColumns` ganhou `locale` mas `verifyPassword` mantinha um `Scan` escrito à mão — 13 colunas, 12 destinos, e **todo login virou 500**. A suíte de integração pegou (40+ testes), mas só depois de eu já ter anunciado a fase como pronta. Corrigido pela classe, não pelo site: a lista de destinos virou `userDest(u, id, extra...)`, variádica, e `verifyPassword` a estende com o hash — uma coluna nova é uma edição só, e o silêncio do compilador deixa de ser load-bearing. **Fase B.** `MAIL_TRANSPORT=inproc\|amqp`, sink AMQP publicando o **mesmo blob selado** que a linha guarda, `cmd/mailer` como binário próprio com `config.LoadMailer` (existe para o worker subir **sem `DB_URL`** — ele é o processo que decifra credenciais, e dar-lhe banco também tornaria um comprometimento dele um comprometimento de tudo). Três desvios do SDD, todos registrados em §13.1: escada de retry virou **uma fila por degrau** (`.retry.1m/.5m/.30m`) porque o RabbitMQ expira só a partir da CABEÇA e um TTL por mensagem faria a de 30 min segurar todas atrás; o worker **republica** em vez de dar nack, porque nack roteia por chave fixa e não sabe escalonar; e ganhou um `DeadLetterWatcher` que o documento não previa — o SDD dizia "o relay marca `failed`", mas o relay não consome nada e o worker não pode falar com o Postgres, então ninguém marcaria e `published` passaria a significar só "o broker aceitou". **O defeito que o sweep pegou e que os testes não pegariam:** `mandatory=true` sem `NotifyReturn`. No AMQP, mensagem não-roteável com `mandatory` é devolvida **e mesmo assim confirmada** — o confirm diz "a exchange terminou de processar", não "uma fila tem". Sem observar o retorno, o relay marcava `published` e o worker dava `Ack`: perda total e silenciosa, categoricamente diferente do trade documentado (duplicar uma mensagem que já falhou). Virou `ConfirmingChannel`, caminho único de publicação, que só reporta sucesso quando houve confirm **e** não houve return — e de quebra matou a duplicação de lógica entre sink e `Republish`. `MarkDead` passou a devolver se casou linha, porque zero linhas atualizadas estava sendo logado como sucesso. **Também do sweep:** o serviço compose `mailer` apontava para `justoeu/foldex-mailer`, imagem que o `release.yml` **nunca constrói nem publica** — `--profile amqp up` falharia no pull. Passou a reutilizar a imagem do backend com `entrypoint` sobrescrito; publicar uma terceira imagem exige repositório no Docker Hub e um braço a mais no matrix de release, e um serviço apontando para tag inexistente é pior que imagem gorda. O isolamento que importa (sem credencial de banco) é preservado. |

| 2026-08-18 | **ADR-37 fase C — e-mail como segundo fator permanente** | Migração `000036_email_second_factor`. Antes disso o OTP por e-mail só existia **dentro** de um desafio que já era TOTP: `emailFactorAvailable` exigia `purpose == 'totp'`, e o desafio só nascia `totp` para conta com autenticador confirmado — então `/2fa/email` era inalcançável exatamente para quem não tinha outro fator. Agora `email_factor` espelha `totp_secret` (mesmo binding de época, mesmo CHECK, mesmos padrões de confirmação sob lock) e `HasSecondFactor()` substitui `TOTPEnabled` como a noção de "esta conta tem segundo fator". **O invariante que NÃO cai:** `mailbox_already_proven` continua recusando o e-mail num desafio nascido de link de reset — por isso o cadastro do fator **obriga** códigos de recuperação, que são a única saída do lockout que essa regra cria de propósito. **Três defeitos que a suíte encontrou, e cada um valia mais que o código novo.** (1) *Compatibilidade quebrada por mim:* `admin_second_factor` entrou com validação estrita, e um documento de política salvo antes desta release não tem a chave — decodifica para `""` e era recusado, deixando o dono **sem conseguir salvar política alguma** por um campo que nunca tocou. Ausente passou a significar o piso; presente-mas-desconhecido segue recusado, porque aí é erro e não omissão. Onze testes caíram. (2) *Bug real no código antigo, achado por teste novo:* `DisableTOTP` apagava os códigos de recuperação incondicionalmente — correto enquanto TOTP era o único fator possível, e lockout no instante em que o e-mail pôde ser cadastrado, porque desligar o autenticador deixava a conta com fator e-mail e **sem saída**. Agora só apaga quando nenhum fator sobra, lido sob o lock que a transação já segura. (3) *Copy errada exposta por acidente:* eu havia reusado o template `login_code` para o código de cadastro; ele anuncia um login e avisa "alguém pode saber sua senha — troque". Para quem acabou de pedir o cadastro isso é falso, alarmante, e empurra para uma troca de senha desnecessária. Ganhou template próprio nos três idiomas. **A lacuna que a suíte cobrou depois:** três dos quatro call sites de step-up ainda aceitavam **só** TOTP, então uma conta cujo único fator é e-mail não conseguia definir senha nem regerar códigos, e para desligar o próprio fator teria de gastar uma credencial de saída de lockout. Entrou `POST /2fa/email/send` sob purpose próprio (`step_up_2fa`, separado de `enroll_email_2fa` porque um prova uma caixa que ninguém aceitou ainda e o outro é um fator já aceito se apresentando), o discriminador de seis dígitos passou a **cair através** de TOTP para e-mail em vez de encadear `else if` (as duas formas são indistinguíveis; comprometer com o ramo TOTP tornaria o fator e-mail inútil justamente para quem não tem autenticador), e o ramo de e-mail exige `Email2FAEnabled` — sem isso, controle da caixa postal sozinho autorizaria remover um autenticador. De quebra, `SecondFactorProof` passou a ser **verificado sem ser gasto**, consumido dentro da transação da operação que autoriza, para os três métodos: a propriedade que o §4 já exigia do TOTP vale mais ainda para código de recuperação. `SetPassword` também lia só `totp_secret` na re-checagem dentro da transação, dispensando o proof para a conta que só tem e-mail. **"Este fator pode ser removido?" virou função única** (`mayRemoveFactor`), consultada pelos dois disables **e** pelo `GET /2fa`, que devolve `can_disable_totp`/`can_disable_email` — duas cópias da regra divergem na direção que ninguém percebe: tela que esconde botão permitido parece funcionalidade faltando, tela que mostra botão recusado parece defeito na conta. **Frontend:** seção de configurações lista os métodos independentemente e oferece "Enviar um código por e-mail"; a tela de cadastro obrigatório pergunta o método **só quando há escolha** — sem SMTP ela começa o autenticador direto, porque pergunta com uma resposta possível é fricção pura numa tela da qual o admin não pode sair. Ref-guard de mount preservado (agora chaveado por método) e a regra de "sem skip" intacta. i18n completo em en/pt/es. |

| 2026-08-18 | **Vazamento de testcontainers: causa raiz, correção e guard** | O usuário apontou o sintoma ("não faz sentido subir tudo isso e não matar depois") e a raiz é pior que esquecimento. O mecanismo de limpeza **já existia** — `testdb.StopShared`, e a própria doc dele avisa que sem `TestMain` o contêiner sobrevive à rodada — mas o Makefile exporta `TESTCONTAINERS_RYUK_DISABLED=true`, então **não há rede de segurança nenhuma**: um pacote que esquece o hook vaza até a máquina reiniciar. Um pacote esquecia: `internal/mailoutbox`, criado por mim no lote anterior, que ainda segurava um RabbitMQ compartilhado **sem teardown algum** — com um comentário meu afirmando que "o Ryuk limpa", falso pelo motivo acima. Resultado observado: 25 contêineres órfãos (alguns de 17 horas), `docker info` levando 28 s, criação de contêiner estourando deadline, e a suíte reportando testes travados — uma cadeia de sintomas em que nada apontava para a causa. **Três correções, não uma.** (1) `TestMain` no `mailoutbox` terminando Postgres e broker. (2) `TestEveryPackageUsingTestdbStopsIt` + `TestStopSharedIsOnlyCalledFromTestMain` em `internal/security`, no mesmo padrão de `TestResetCoversEveryTable`: transformam "lembrar do TestMain" de disciplina em falha de build, e o segundo impede chamar `StopShared` de dentro de um teste, onde ele mataria o contêiner debaixo dos irmãos. Casam na **AST**, não no texto — a primeira versão comparava corpos impressos e **se auto-acusou**, porque a mensagem de erro dela cita a função; guard que falha pelo motivo errado ensina a contorná-lo. Provados por reversão: removendo o `TestMain` do `mailoutbox`, o guard fica vermelho. (3) `TEST_PARALLEL ?= 4` no Makefile: o `-p` do Go defaulta para `GOMAXPROCS` e cada pacote de banco sobe seu próprio Postgres, então uma rodada pedia ~19 contêineres simultâneos; passando de um punhado, o startup vai de segundos a mais de um minuto e pacotes estouram o timeout de 10 min — reportado como teste travado, a pior descrição possível de "o Docker estava saturado". **Não corrigido de propósito:** `internal/pkg/slug` apareceu na primeira varredura mas usa `testdb.New`, que já registra `t.Cleanup`; cheguei a adicionar um `TestMain` lá e reverti, porque seria código morto. |

| 2026-08-18 | **Broker de teste compartilhado em `internal/mailoutbox`** | Achado ao rodar a suíte completa da fase C, e é defeito meu do PR anterior: `startRabbit` subia um contêiner RabbitMQ **por teste**, seis deles dividindo o timeout único de dez minutos do Go. Numa máquina carregada (51 contêineres, startup de 40–75 s cada) o pacote estourava e reportava `TestDeadLetterWatcher_AcksGarbageInsteadOfSpinningOnIt` travado — falha que não diz nada sobre o código e custa uma tarde para ler. Isolado, o mesmo teste passava em 101 s. O contêiner virou único por pacote (`sync.Once`, o mesmo padrão que o `testdb.Shared` já usava para Postgres) e o isolamento passou para a **topologia**: `topologyFor(t)` dá exchange e filas próprias a cada teste, o que é mais forte que contêiner por teste — dois testes compartilhando nome de fila interfeririam mesmo com um contêiner cada, se um vazasse consumidor. Pacote caiu de >600 s (estourando) para ~104 s. **Limite conhecido:** a garantia vale para saída NORMAL do binário de teste; um `go test` morto por SIGKILL (Ctrl-C, timeout do orquestrador) não executa `TestMain` e ainda deixa contêiner para trás — isso é do desenho do Go, não do hook, e a limpeza aí é manual. |

| 2026-08-18 | **Gate de versão do Compose recusava toda release** | Consequência não prevista do serviço `mailer` da fase B: ele roda a imagem do backend, então `docker-compose.yml` passou a ter duas linhas `justoeu/foldex-backend:` — e os dois gates exigiam **exatamente uma** de cada. `make release-minor` recusava com "versions are out of sync" num arquivo onde `bad=0`, ou seja, nada fora de sincronia; o mesmo `== 1` em `scripts/validate-release-ref.sh` recusaria a publicação de qualquer tag dentro do `release.yml`, o que só apareceria no primeiro dispatch. A aridade virou `>= 1`, preservando o predicado que o gate realmente defende (toda linha correspondente fixada na versão). **A primeira tentativa de prova foi ruim e a revisão a derrubou:** um `grep -Fq` no texto do awk de `validate-release-ref.sh`, que dois agentes mataram com o mesmo mutante — injetar `if (backend > 1) bad = 1` mantendo a linha do `END` intacta ressuscita o defeito e o teste segue verde. Isso é exatamente o que o CLAUDE.md §2 já registra sobre guards que casam texto. A prova passou para `scripts/test-release-gate.sh`, que **executa** o validador contra um repositório git de fixture. Achados no mesmo lote: a metade "pelo menos um" do predicado não tinha teste (a simplificação `END { exit !(!bad) }` faria o gate aceitar um compose sem imagem foldex nenhuma), o relaxamento em `web` era código não testado, e um subshell sem guarda fazia a suíte morrer imprimindo o `✓` do caso anterior. |

| 2026-08-18 | **Injeção de comando na versão lida do disco (`scripts/release.sh`)** | Achado pelo Cérbero no mesmo lote, pré-existente. `CUR` sai de `grep -oE '"version"…"[^"]+"'` — e `[^"]+` admite `$`, `(` e `[` — e cai em `$((PAT+1))`. Bash resolve substituição de comando dentro de subscrito de array em contexto aritmético, então uma string `"version"` envenenada num commit executa na máquina de quem roda `make release-patch`: a workstation que detém push em `main` e dispatch do `release.yml`. O script já exigia semver estrito do que o operador **digita**; agora exige o mesmo do que ele **lê**. Detalhe que quase produziu um teste vacuoso: sob `set -u` a base do subscrito importa — `x[$(…)]` aborta como *unbound* **antes** de rodar a substituição, enquanto uma base definida (`PATH[$(…)]`) executa o payload e só depois erra. O caso 9 usa `PATH` por isso, e envenena os três arquivos de versão porque o check de sincronia recusaria primeiro e a aritmética nunca seria alcançada. |

| 2026-08-18 | **Gate de release: uma fonte só, e o que ele garante ficou mais forte** | Os três follow-ups que o PR anterior deixou registrados foram fechados juntos, porque são o mesmo defeito visto de três ângulos: o gate julgava **linhas**, não o arquivo. (1) **Duplicação** — o awk vivia em três cópias manuais (`compose_version_is` e `update_compose_version` em `release.sh`, mais o gate de ref), e foi exatamente por isso que a suposição errada de aridade precisou ser corrigida em dois arquivos. Agora é `scripts/lib/compose-image-pin.awk`, um programa com `expected`/`new`/`require_services`; os três call sites o invocam. (2) **Serviço × imagem** — contar linhas não diz qual serviço roda qual imagem, então um compose com `backend:` apontando para `attacker/evil:latest` e um serviço-isca carregando a linha fixada passava. O programa agora rastreia o bloco `services:` (fechando em qualquer chave de topo, senão `networks:`/`volumes:` viram serviços) e exige que `backend` e `web` rodem suas próprias imagens. (3) **Formas invisíveis** — valor aspado ou com registry explícito nunca casava o prefixo, ou seja, carregava imagem Foldex e **não era verificado**. Silêncio é a direção errada de falha para um gate: o aspado agora é entendido e validado, o registry-prefixado é **recusado** como referência não verificável, e o gate lê **todos** os `docker-compose*.yml` em vez de só o primário — se o mailer migrar para `docker-compose.mail.yml`, a versão dele continua coberta. **Duas lições de método:** um `sed`/`perl` em massa sobre o arquivo de teste corrompeu oito linhas de `echo` e a suíte **continuou verde**, porque essas linhas só são alcançadas quando um caso falha — reverti e refiz com edições dirigidas, conferindo o diff linha a linha. E os primeiros testes das regras novas afirmavam só o veredito: dois mutantes sobreviveram recusando **pela razão errada** (`docker.io/…` reprovava como "não pinado", serviço com imagem estranha virava "no service named backend"). A razão virou parte da asserção, e aí morreram. |

| 2026-08-19 | **Corrida entre registro e teardown de túnel no proxy de screenshot** | Apareceu como `TestCaptureProxy_RejectsCONNECTAboveTunnelLimit` reprovando no CI de um PR de `mailoutbox` — pacote sem nenhuma relação. O caminho fácil era chamar de flake, e o próprio comentário do teste convidava a isso: o timeout **já tinha sido levantado de 1 s para 5 s** por falhar em runner de dois núcleos. Mas 110 execuções locais passavam em microssegundos, e precisar de mais de 5 s quando o normal é instantâneo não é lentidão — é algo travado. O `serveConnect` adquire o slot do semáforo ao admitir o CONNECT, mas só entra no mapa rastreado **depois** de dar flush no `200 Connection Established`; o cliente vê esse 200 e abre o próximo túnel enquanto o handler ainda está entre as duas coisas. O `exceedBudget` então roda `closeTunnels()` sobre um mapa que ainda não contém aquele túnel: ele sobrevive ao teardown, seu `io.Copy` bloqueia num peer que ninguém fechou, e o slot nunca volta. **Não era só um teste chato** — em produção é um túnel continuando a relaying bytes depois de a captura ter sido invalidada, que é exatamente o que o orçamento existe para impedir. O `trackTunnel` já tinha esse interlock para o `Close()` do proxy (`if p.closed`); faltava a metade do orçamento. Provado injetando um `Sleep` de 300 ms na janela: o teste reproduz a falha do CI byte a byte, e com o interlock passa **mesmo com o Sleep no lugar**. O teste de regressão permanente exercita `trackTunnel` direto (`net.Pipe`, sem timing, 0,00 s) porque a janela é acidente de escalonamento e um teste que dependa dela volta a ser flaky. |
| 2026-08-19 | **Contador de tentativas do mailer sem teto (gosec G115)** | Alerta de code-scanning #73, `int32(attempt)` em `amqp_topology.go`. Não era ruído: o valor entra por `Attempt(d.Headers)`, que lê um header de entrega, e o CLAUDE.md §4 coloca o broker **fora** do threat model — compartilhado entre projetos, qualquer um com permissão de publish escreve o inteiro que quiser. Com `math.MaxInt64`, o `+1` do worker dá a volta para negativo; o teste de desistência (`attempt >= MaxAttempts`) lê isso como tentativa inicial, o `retryKey` clampa no degrau de 30 min, e o `int32` trunca o contador de volta a zero. **Não há crash nem log**: a mensagem circula para sempre em vez de chegar à dead queue — num worker cuja única função é entregar link de reset e código de login. Corrigido no ponto de entrada (`clampAttempt`), não no cast, porque é o teste de desistência que decide o destino e ele roda **antes** da conversão. Detalhe que quase deixou o alerta aberto: com o clamp numa função separada, o gosec continuava marcando — ele não segue o limite através da chamada. `clampAttempt` passou a devolver `int32`, com a comparação contra o teto imediatamente acima da conversão; G115 zerou. |

| 2026-08-19 | **`pinned` era descartado no sort Alfabético** | Bug pré-existente desde antes das Notes, registrado nos followups desde 2026-06-30. O backend faz o certo — `listquery.Planner.AddPage` emite `pinned DESC, lower(title) ASC` no modo alpha —, mas `mergeAlphaCells` achatava folders e entries num único `sort` por nome e descartava exatamente esse prefixo. Resultado visível: um card fixado chamado "Zebra" renderizava abaixo de um não-fixado "Apple", no único modo de ordenação em que o CLAUDE.md §5 diz que pinned continua vindo primeiro. **O comentário do helper afirmava o contrário** ("Pinned entries still sort first … via the backend's `pinned DESC` prefix"), o que provavelmente é o motivo de ninguém ter olhado de novo — e **nenhum dos cinco testes usava `pinned: true`**, então a suíte verde não dizia nada sobre o assunto. Agora são dois blocos: fixados primeiro, depois folders e não-fixados interleaved por nome. Folders ficam no segundo bloco porque não existe coluna `pinned` para folder. A ordem é **re-derivada** no helper em vez de confiar na que chegou: folders vêm de outra query, e a ordenação relativa das entries não sobrevive ao merge sozinha. Três dos quatro testes iniciais ficavam vermelhos com o `sort` plano de volta — e a revisão mostrou que eu tinha errado sobre o quarto: ele não é asserção de completude, é o **único** matador de dois mutantes (remover o `.sort` do bloco fixado, e inverter o `dir` dentro dele). Afirmar o contrário na mensagem de commit é o mesmo modo de falha que o defeito: dar ao próximo leitor motivo para não olhar. A revisão também derrubou dois mutantes vivos — o bloco fixado ignorando a direção em Z→A, e o comparador trocado por codepoint, que o teste `is case-insensitive` não pegava porque a fixture `('banana','Apple')` era um par em que os dois comparadores concordam. E fechou a costura que manteve o bug vivo: nenhum teste de view renderizava card fixado no alfabético, então a garantia existia só na camada do helper — exatamente a distância entre o teste de integração do backend (verde) e a suíte do frontend (verde) em que o defeito morava. |

| 2026-08-19 | **Conta desativada continuava sendo varrida e notificada** | Followup do sweep de 2026-08-14, verificado contra o código antes de mexer: `SystemFindDueForCheck` não fazia join com `app_user`, e `push.Repository.List` também não. Desativar uma conta revoga as sessões e mata os API tokens, mas **uma subscription Web Push é um canal do navegador que sobrevive aos dois** — foi instalada pelo service worker e nada no caminho de disable a toca. O resultado é que a conta desligada continuava recebendo "esta página mudou" num dispositivo que já não consegue entrar. Filtrado nas DUAS pontas, de propósito: no **claim**, porque é onde está o custo (os links param de consumir orçamento de fetch, não só de notificar), e na **entrega**, porque o claim é um retrato e não diz nada sobre o presente — uma conta desativada entre o claim e o envio ainda receberia. A linha de `push_subscription` sobrevive ao disable: desativar é reversível, e apagar o canal ali desinscreveria silenciosamente um navegador que pode ser reativado um minuto depois sem ninguém perceber. Ambas as metades provadas por reversão. **A revisão pegou uma regressão que eu mesmo introduzi:** o `FOR UPDATE SKIP LOCKED` do claim não era qualificado, e com `app_user` no FROM o rowmark passou a incluí-lo — o que faz `SKIP LOCKED` pular um link porque o DONO está travado (todo caminho de auth segura `app_user FOR NO KEY UPDATE`), e faz a varredura segurar `FOR UPDATE` em `app_user`, que conflita com o `FOR KEY SHARE` de toda FK e **bloqueia INSERTs em `link`, `note`, `session`, `audit_log`, `mail_outbox` e `click_log`** — este último sendo o `/go/` público. Trabalho de background bloqueando autenticação e redirect, direção que não existia. Corrigido com `FOR UPDATE OF l`. **E quatro mutantes sobreviviam aos meus testes:** o fixture de push tinha um usuário só, então desativá-lo zerava as contas ativas e um join sem correlação de id passava (numa instância multi-usuário ele devolveria as linhas do desativado, duplicadas); o teste do claim afirmava o que era *retornado* e não que a linha ficara *sem ser reivindicada*, então filtrar depois do UPDATE em Go passaria e o argumento de custo evaporaria; `pending` não era exercitado em nenhuma das metades, deixando `= 'active'` e `<> 'disabled'` indistinguíveis; e nada testava o `Notify`, só o `List` — a garantia chegava à entrega por construção, não por asserção. **Armadilha encontrada no caminho:** `testdb.Shared(t)` chama `Reset` a CADA invocação, então chamá-lo uma segunda vez dentro do mesmo teste apaga o que o `setup(t)` acabou de semear — o primeiro rascunho falhava com FK violation mascarada por um `_` no retorno do `Create`. |
| 2026-08-21 | **`amqp://` na LAN: o operador decidiu, e o guard passou a verificar em vez de acreditar** | O operador tem RabbitMQ na rede local sem TLS e, ciente do risco, pediu texto claro. O guard do ADR-36 tratava "broker remoto" como uma coisa so e nao tinha escape, entao o custo caia inteiro sobre o caso mais comum do foldex. `AMQP_ALLOW_PLAINTEXT` (default `0`) abre a porta **conferindo a afirmacao**: o flag e uma declaracao sobre a REDE, e um endereco publico literal segue recusado mesmo com ele ligado, porque nenhuma declaracao torna `203.0.113.4` interno e a forma realista de chegar num e errar um octeto. **A revisao de seguranca mostrou que metade da garantia nao existia.** A checagem estava no PARSE, mas o dial acontece depois — e hostname e a forma MAJORITARIA de enderecar broker (o nome de servico do compose inclusive), resolvida por infraestrutura que o operador nem sempre controla e que pode mudar a cada reconexao. Pior: este repo ja resolve exatamente isso em `preview.safeDialer`, com `LookupIP` pre-dial e `RemoteAddr` pos-dial, e o `dialAMQP` nao fazia nenhum dos dois. A verificacao ganhou a segunda perna (`requirePrivatePeer`), que fecha a conexao ANTES de o handshake escrever a credencial SASL PLAIN e falha fechada quando o endereco nao e `*net.TCPAddr` — de quebra cobrindo o literal que `net.ParseIP` recusa mas o resolvedor aceita (`3221225985` disca `192.0.2.1`). **Dois erros meus, ambos da mesma familia.** Primeiro usei `netpolicy.IsPrivateIP`, que responde outra pergunta: feito para bloquear SSRF, trata o registro de uso especial da IANA inteiro como proibido, entao `203.0.113.4` le como privado — correto para um fetcher, errado para decidir se credencial cruza link em claro; meu proprio teste pegou. Depois escrevi um teste que discava `203.0.113.4` esperando recusa: gastou 30 segundos ate o timeout e terminou em SKIP, provando nada. A checagem foi extraida para ser testada contra um peer construido. **E um mutante sobreviveu ao primeiro conjunto:** trocar `amqp.DialConfig(Dial: ...)` por `amqp.Dial` deixava `requirePrivatePeer` compilando, testado e nunca chamado — a garantia sumia com tudo verde. Fechado com uma costura que observa QUAL dialer cada esquema usa. O predicado virou `internal/pkg/privatenet`, com os limites presos dos DOIS lados (100.128.0.0/9 e espaco publico alocado a uma comparacao de distancia). Doc corrigida nas cinco superficies, inclusive o inventario de exposicao: em claro vao tambem o DESTINATARIO e o TEMPLATE de cada mensagem — quem recebe qual credencial e quando —, nao so "metadados de roteamento" como eu havia escrito; o corpo segue selado. |
| 2026-08-20 | **TLS de broker privado ja funcionava; faltava documentacao -- e eu quase enviei codigo que quebrava** | Ao ligar o transporte AMQP numa instancia real, o boot recusou: o guard do ADR-36 nega `amqp://` contra host nao-loopback e o broker da LAN nao tem TLS. Diagnostiquei **buraco de projeto**: `AMQPConfig.TLSConfig` nunca era preenchido por nenhum dos dois `main`, entao `DialTLS` recebia `nil` e verificava contra as raizes do SISTEMA -- que nenhuma cadeia privada satisfaz. Escrevi `AMQP_CA_FILE`, com helper, wiring nos dois processos, quatro testes (inclusive prova por reversao num listener TLS real) e documentacao nas cinco superficies. **O diagnostico estava errado.** A revisao de CORRECTNESS apontou que passar um `*tls.Config` nao-nil faz o `amqp091-go` PULAR `tlsConfigFromURI` (`connection.go:324`); ao ir verificar o que exatamente eu estaria desligando, descobri que essa funcao ja lia `?cacertfile=`, `?certfile=`/`?keyfile=` (mTLS) e `?server_name_indication=` da propria `AMQP_URL`. Provei empiricamente contra um listener TLS com CA de teste: **sem** `?cacertfile=` o handshake e recusado, **com** ele passa -- no `main` de hoje, sem uma linha de codigo. A capacidade nunca faltou; faltava alguem escrever que ela existe. Meu codigo nao so era redundante como **introduzia um defeito**: quem ja usasse mTLS ou SNI na URL perderia os dois em silencio ao definir a variavel. Codigo revertido por inteiro. Ficou a documentacao dos parametros da URI (`.env.example`, os dois READMEs, ADR-36, CLAUDE.md), o `./certs:/etc/foldex/certs:ro` no backend e no mailer -- necessario porque os caminhos resolvem DENTRO do container -- e um aviso explicito para a proxima pessoa nao 'consertar' isso com uma env var. **A licao nao e sobre AMQP.** Eu li um campo `TLSConfig` sempre nil e concluí capacidade ausente sem verificar o que a biblioteca fazia com o `nil`. O `nil` era o caminho FELIZ. Tres revisores, quatro testes e cinco documentos foram construidos sobre uma premissa que dez minutos de leitura da dependencia teriam derrubado -- e o que a derrubou foi um achado lateral, nao o trabalho principal. |
| 2026-08-19 | **`.env.example` fecha a fase B — e a revisão achou que o compose padrão não subia** | Fechar a última pendência documental da fase B (as 8 variáveis de `MAIL_TRANSPORT`/AMQP/outbox fora do template que o §7 define como fonte canônica) expôs dois defeitos reais que nada exercitava. **(1) `docker-compose.yml` derrubava a stack DEFAULT.** O serviço `mailer` declarava `AMQP_URL: ${AMQP_URL:?...}` — a forma com dois-pontos falha para vazio E para ausente — e o compose interpola serviços de profile INATIVO, então `make up` sem broker nenhum morria na interpolação antes de qualquer container subir. Entrou em `5f44a92` (2026-08-18) e nunca foi pego porque o DAST é mensal e sua última execução foi 2026-08-01: o bug viveu uma janela inteira sem ninguém rodar o caminho default a partir de um `.env` recém-gerado. Corrigido para `${AMQP_URL:-}`. **A exigência não se perdeu ao sair do compose** — ela já vivia em `validateMailTransport` (`config.go:512`), coberta por `TestLoad_AMQPTransportRequiresAURL`, que é onde ela consegue explicar o próprio erro em vez de reportá-lo como falha de interpolação de YAML. O porquê ficou escrito no compose para a forma `:?` não voltar. **(2) `dast.yml` não subia a stack por um motivo independente:** usava `cp .env.example .env`, e o template traz as chaves do RustFS vazias DE PROPÓSITO, para o `init-env.sh` gerá-las — as formas `:?` legítimas delas quebravam a interpolação igual. Trocado por `bash scripts/init-env.sh` (o mesmo que `make env` roda, cujos defaults já são `.env`/`.env.example`), provado por reversão: `cp` → exit 1, script → exit 0. **Um erro meu que a revisão de escopo pegou:** eu havia registrado como verificado que "o dast continua funcionando porque `inproc` retorna antes de ler `AMQP_URL`" — verifiquei o caminho Go e afirmei sobre a interpolação do compose, que é outra camada. É exatamente a armadilha do followup-que-mente descrita na linha seguinte deste mesmo arquivo, cometida por quem a escreveu. **Lição transversal:** o painel de 3 revisores achou mais do que o gate completo. Backend e frontend fecharam verdes nas duas passadas porque `go test` e `vitest` não interpolam YAML — nenhuma suíte deste repo boota o compose, então a distância entre "tudo verde" e "a stack sobe" era invisível por construção. O CORRECTNESS ainda observou que nada trava o template contra drift futuro da config (`mailFromEnv` lê 18 envs e nada exige que apareçam no `.env.example`), que é a razão de a lacuna ter durado uma fase inteira — registrado como followup abaixo. Também entraram: `AMQP_ROUTING_KEY` nas tabelas dos DOIS READMEs (faltava nos dois, simetricamente, então §3 estava satisfeito e a superfície documentada mesmo assim incompleta) e a correção de `ARCHITECTURE.md:1050`, que afirmava que `inproc` "mantém o dispatcher atual como sink" dois parágrafos acima de 1052 dizer que esse dispatcher não foi implementado. |
| 2026-08-22 | **ADR-38 — `GET /metrics` Prometheus atrás de bearer token** | Novo `internal/metrics` (contadores/histograma HTTP por padrão de rota do chi, in-flight, pgxpool, runtime Go), montado antes do Recoverer e do auth stack; `METRICS_TOKEN` vazio = 503, comparação em tempo constante, scrape concorrente limitado a 1 com timeout 10s. Labels imunes a cardinalidade (rota = padrão chi, método colapsa em `other`, `unmatched` para não-roteado). **HIGH achado pelo sweep (3 agentes convergiram):** o wrapper de status caseiro não implementava `Unwrap()`, quebrando silenciosamente o `http.NewResponseController` do `extendArchiveDeadlines` (backup) — streams multi-GB seriam cortados no WriteTimeout default; trocado pelo `WrapResponseWriter` do chi (mesmo padrão do `slogRequest`) e travado por `TestInstrumentPreservesResponseControllerDeadlines` + tripwire de ordem de middleware (`TestRecoveredPanicIsCountedAsInternalServerError`). Consumido pela stack central de observabilidade (app-deployments/observability, Coolify 192.168.68.70). Docs: ADR-38, READMEs (en/pt), `.env.example`, compose. Cobertura total 85.2% (gate 85%). |
| 2026-08-22 | **497 → 301: HTTP puro na porta TLS redireciona em vez de morrer no 400** | Navegador que recebe `host:9444` sem esquema assume http:// e falava plaintext com o listener TLS — o nginx respondia o 400 default ("The plain HTTP request was sent to HTTPS port"), beco sem saída visto em produção no Coolify. `error_page 497 =301` no server 8443 redireciona para `https://__WEB_PUBLIC_HOST__$request_uri` — host ASSADO, nunca o Host do request (mesma regra anti host-header-injection do redirect :8080). Asserção nova em `scripts/test-nginx-headers.sh` (301 + Location com host assado e path preservado, enviando Host hostil); provado por reversão: script novo contra config antiga sai 1, com o fix sai 0. |
| 2026-08-22 | **Card de duas etapas redesenhado — e o motivo do botão ausente foi para dentro da linha** | O card era uma pilha plana de linhas cinza de 12px: status, os dois métodos, a contagem de códigos e a nota de política, todos no mesmo peso — e `required_note` no PÉ do card, quatro linhas abaixo de dois métodos que ela não conseguia nomear. Um admin via um botão faltando e, em outro lugar da tela, uma frase solta; o relato foi "os botões ficam bloqueados". Agora estado, ação e o motivo de uma ação ausente vivem na MESMA linha: cadeado âmbar dentro do método a que se aplica, e só quando esse método está ligado — `can_disable_*` também é falso para método não cadastrado, então travar nele sozinho diria que todo método não usado está protegido. Inscrição em andamento e os códigos de recuperação passaram a SUBSTITUIR a visão geral em vez de aparecer dentro dela (uma tarefa, uma próxima ação). **Uma correção minha, registrada porque eu a anunciei errado primeiro:** inspecionando o DOM vi a linha de e-mail com "Configurar" e "Desativar" ao mesmo tempo e chamei de defeito real — era artefato do meu próprio fixture de preview. O servidor nunca manda essa forma (`can_disable_email` é `Email2FAEnabled && mayRemoveFactor(…)`); o gate extra ficou mesmo assim, agora comentado pelo que é: uma repetição que impede a LINHA de se contradizer se as duas pontas divergirem, não uma segunda opinião sobre a política. **E o sweep matou um ramo morto que eu tinha escrito:** `locked_note` ("este é o único método protegendo sua conta") nunca podia renderizar — `mayRemoveFactor` só recusa sob `require2FAForAdmins && IsAdmin`, exatamente o que `required` já reporta — e a frase afirmava uma proteção de último fator que não existe: um usuário comum pode remover o último livremente. Chave apagada dos três locales, ternário colapsado. **E o quarto membro da mesma família de CSS:** `.fx-btn-danger` não tinha regra em stylesheet nenhum, com dois call sites — ambos o botão que REMOVE um segundo fator, renderizando idêntico ao neutro ao lado. Entrou no `scripts/test-css-button-reset.mjs`. Oito testes novos, todos provados por mutação (inclusive dois que o sweep pediu: regenerar códigos não tinha teste de "desabilitado sem prova", e a fronteira exata de 3 códigos distinguia `<` de `<=` sem asserção); um teste antigo passava cedo demais porque a lista de métodos agora existe antes de a query resolver (`findAllByRole` resolvia no primeiro tick contra os defaults). |
| 2026-08-22 | **Eu quebrei a tela de dois fatores e chamei o sintoma de "artefato da captura de tela"** | O redesenho do card saiu com o texto invisível, a tela piscando e a aba travada. Causa: `.fx-auth` é um OVERLAY (`position: fixed; inset: 0`, fundo opaco) — a tela deslogada — e tudo que ela estiliza e o shell logado também precisa (campo OTP, QR, grade de códigos) está sob seletores `.fx-auth .coisa`. Alcançar isso significava vestir a classe do overlay num `div` dentro do card e cancelar `position`/`padding`/`background` na mão. **O cancelamento só valia porque era estilo INLINE.** Ao virar classe em `foldex.css`, perdeu para `.fx-auth` por ordem de documento (`auth.css` carrega por último, ambos `(0,1,0)`), e o wrapper virou uma folha opaca do tamanho do card pintada por cima — mais uma tempestade de repaint (caixa fixed com `overflow-y: auto` dentro do bloco contenedor do `backdrop-filter` do `.fx-card`) que travava a main thread. **E eu VI o sintoma antes de enviar:** o preview mostrou exatamente isso — texto sumido, só `<button>`/`<input>` pintando — e eu classifiquei como falha da extensão de screenshot e segui. O relato do usuário foi "pedi para melhorar e você piorou 100%", e estava certo. Corrigido de raiz em vez de com outro cancelamento: `.fx-authfield` carrega os aliases `--fxa-*` e responde aos mesmos seletores via `:is(.fx-auth, .fx-authfield)` — mesma especificidade, layout nenhum — nos cinco call sites do shell (três já usavam a gambiarra inline). O painel de códigos de recuperação vestia o overlay CRU desde antes desta sessão, sem cancelamento nenhum, numa tela que aparece uma vez só. **Dois guards, porque cada um enxerga a metade que o outro não vê:** `scripts/test-css-auth-overlay.mjs` (no CI, provado por reversão nas duas metades) recusa a classe em qualquer `className` fora dos dois arquivos permitidos e confere que `.fx-authfield` ainda declara os tokens e ainda casa com o seletor da célula; e uma asserção em jsdom no teste do componente — `css: false` esconde o stylesheet do Vitest, mas **não esconde o nome da classe**, então essa asserção não precisava de CSS nenhum e teria pego. **A lição não é sobre CSS:** eu tinha a evidência do defeito na tela, ela não batia com a minha explicação ("a extensão está com problema"), e eu preferi a explicação que não me custava nada. |
| 2026-08-22 | **Os quatro painéis da conta viraram um card só — e o defeito de CSS órfão ganhou guard** | Perfil, Acesso e Sessões não estavam estilizados de forma diferente de propósito: não estavam estilizados por ninguém. Perfil era um form sem moldura, Acesso duas linhas soltas, Sessões uma frase com dois botões — ao lado do card de Dois fatores, redesenhado na sessão anterior. Extraí `components/account/SectionCard.tsx` (`SectionCard`/`SectionBadge`/`SectionBlock`/`SectionRow`/`Notice`) em vez de copiar o markup, e os quatro painéis passaram a compor os mesmos primitivos. **Foco em MENSAGENS, que era o pedido:** erro e sucesso eram linhas de 12px no mesmo peso de um hint de campo — uma recusa de credencial que se lia como rodapé. Viraram bandas com fundo próprio, com a região viva por TOM (`bad` = `alert`, `ok` = `status`, `info` = nenhuma, porque `info` explica algo que já estava na tela e anunciar interromperia sem evento), e subiram para ANTES dos campos. Cada linha passou a ser `<div role="group" aria-label>` em vez de `<li>`: é um título mais os controles que agem sobre aquela coisa, então "Senha, grupo" é o anúncio útil, e é o que deixa um teste (e um leitor) saber a qual método o "Desativar" pertence. `Sessões` agora distingue as duas saídas pelo que cada uma encerra, com a destrutiva tingida ANTES do clique, e diz que token de API não é sessão. **Sexto caso do defeito de classe sem regra, achado por mim desta vez:** `.fx-acct-row .fx-auth-oauth` dimensiona o botão do Google e deixou de casar quando a classe da linha foi renomeada. Isso motivou `scripts/test-css-orphan-classes.mjs` (no CI, provado por reversão): toda classe `fx-*` no markup precisa ter regra em algum stylesheet. Ele achou **mais 14 órfãs pré-existentes** em telas que ninguém estava mexendo — `.fx-error` no formulário de política da instância é uma falha de salvamento renderizada como texto puro, `.fx-chart-tooltip` é um tooltip sem fundo sobre o gráfico — que entraram numa baseline `KNOWN_ORPHANS` que só pode DIMINUIR, porque consertar seis superfícies não relacionadas para conseguir subir um guard é como um guard acaba revertido. **O sweep pegou o quarto painel:** Tokens de API ficou no markup antigo dentro do MESMO rail — migrado também, então os cinco compartilham a forma. Também do sweep: chip de estado e badge do card não tinham asserção nenhuma (fixar `on: true` no `PasswordRow` fazia a linha afirmar "Definida" numa conta sem senha, com o arquivo inteiro verde); um teste cujo TÍTULO prometia preservar o formulário nunca olhava o campo; e o piso de senha no `blocked` não era exercitado. `.fx-card:hover` levanta e sombreia — affordance escrita para os cards arrastáveis da grade — e estava sendo aplicado a painel de configuração; neutralizado como já era para `.fx-modal-card-wrap`. Um achado do sweep foi REFUTADO com evidência: `.fx-authfield` não é órfã, ela é declarada em `auth.css` (o agente só varreu dois dos três stylesheets). Dez testes novos; 1178 no total. |
| 2026-08-23 | **Cor sugerida para pasta e tag novas** | Pedido do dono: sugerir cores em vez de sempre abrir na mesma. Toda criação começava no mesmo indigo, então uma biblioteca construída aceitando o default vira uma parede de chips idênticos e a cor deixa de ser informação. **"Inteligente" aqui quer dizer evitar colisão, não sortear às cegas:** um `Math.random()` puro sobre vinte entradas colide em torno de metade das vezes já na sétima tag (paradoxo do aniversário), então `suggestColor` pula uma cor em uso enquanto sobrar alguma livre, e só cai na paleta inteira quando tudo está tomado — que é a resposta honesta nesse ponto, porque aí nenhuma escolha evita repetição. Três detalhes que a implementação exigiu: a comparação passa por `primaryColor`, senão um gradiente feito com o indigo da paleta deixaria esse indigo parecendo livre; **a EDIÇÃO nunca sugere**, porque repintar uma pasta que a pessoa abriu para renomear é uma mudança que ela precisa notar para desfazer; e a lista de cores tomadas é um SNAPSHOT do momento em que o diálogo abre, deliberadamente fora das deps do efeito — como dep, qualquer refetch sobrescreveria a cor recém-escolhida sem causa visível. O `TagPicker` conta também os chips adicionados no MESMO diálogo, senão duas tags criadas em seguida ainda colidiriam. **Quatro testes existentes quebraram e estavam certos em quebrar:** travavam `INLINE_PALETTE[0]`/`[1]` no payload, que era o default fixo antigo. Passaram a afirmar que a cor vem da paleta — que é o que aqueles testes de fato tratam (a forma do payload). Provado por duas mutações: não evitar cor usada derruba 2, devolver sempre a mesma cor derruba 4. |
| 2026-08-23 | **Imagem quebrada não se recuperava — e o diagnóstico desmentiu metade da premissa** | O dono reportou que quando a imagem falta ou está quebrada não há tentativa automática de screenshot, e concluiu que a funcionalidade não existe. **Ela existe e tinha rodado**: o link do print (`linktr.ee`, id 1) estava com `preview_status = ok` e `og_image_url = /api/files/screenshots/1.jpg` — o fallback do ADR-16 capturou e salvou. O que falhava era SERVIR: `proxy file: get object failed` → 404. A causa na instância dele é configuração, não código — o banco é o local e o `RUSTFS_ENDPOINT` ficou apontando para o remoto (`192.168.68.70:9000`) de quando o `.env` foi trocado, então as chaves referenciam objetos que nunca foram escritos naquele store; os 33 links têm `og_image_url` e nenhum resolve. **E o item "estreito" já existia para links**: o quadrado amarelo com "L" do print É o fallback do `LinkCard` funcionando. **Mas o gap real que ele intuiu existe:** `maybeScreenshot` só dispara com `og_image_url` VAZIO, então uma linha apontando para bytes que sumiram fica presa para sempre com `preview_status = ok`. Agora o proxy limpa a referência, marca `pending` e re-enfileira. **Três coisas sustentam isso.** (1) Um sentinela novo `ports.ErrObjectNotFound`, traduzido do CÓDIGO `NoSuchKey` do minio e nunca da mensagem, separa "não existe" de "a rede falhou" — sem ele um blip limparia o `og_image_url` da instância inteira e re-screenshotaria toda a biblioteca; o teste cobre `connection refused`, timeout e um erro cujo TEXTO diz "not found" mas não é o sentinela, e mutar a guarda para `if true` deixa os três vermelhos. (2) O UPDATE é condicional a `og_image_url = $3`: é o que faz 33 cards quebrados virarem UMA regeneração e o que impede um upload manual chegado no meio do caminho de ser descartado por um 404 velho. (3) Chaves `notes/` nunca são curadas — mídia de nota é upload do usuário e limpar a referência apagaria o único registro de que a imagem existiu. O item estreito foi completado no `NoteCard` e no leitor, onde uma capa quebrada pintava o ícone de imagem quebrada num slot de 150px. |
| 2026-08-23 | **Ler uma nota deixou de gastar uma aba** | O botão "Abrir" de uma nota era um `<a target="_blank">` para `/n/{slug}` — a página PÚBLICA. Ler a própria nota significava sair do app e cair na mesma visão anônima que um link compartilhado dá para um estranho. Agora abre um `NoteViewDialog`: título, tags, frescor e então o texto, com o `body_html` renderizado como rich text (seguro porque o servidor sanitiza em TODO caminho de escrita, inclusive restore de backup — §4 — não por suposição). **A consequência que precisava ser tratada, não só notada:** `click_log` é a única fonte de verdade de visualizações e a rota pública `/n/` é o ÚNICO caminho que insere nela. Então o incremento otimista de `click_count` no `onOpenNote` saiu junto com a navegação — mantê-lo mostraria uma contagem que o servidor nunca registrou, que o próximo refetch corrige em silêncio: o número sobe e desce sozinho. O leitor mantém um link para a página pública, que é o caminho que conta uma visita de verdade. **O guard de classes órfãs pegou um erro meu na hora:** pus `fx-noteview` no markup e escrevi só regras `fx-noteview-*`. Sem ele, a classe ficaria lá sem nada atrás. E o teste antigo do card (`links to the public /n/ route with the slug`) travava o comportamento ANTIGO — substituído por um que exige que não exista `<a>` nenhum, senão a regressão para link volta a passar. 7 testes novos no leitor; o de rich text provado por reversão (renderizar como texto deixa 3 vermelhos). |
| 2026-08-23 | **O modal de senha da pasta quebrado — a mesma armadilha do menu, por outra porta** | O dono mandou print: o campo de senha do desbloqueio de pasta com uma pill dentro da outra e o olho de revelar jogado para fora do campo. **Medi no navegador em vez de teorizar** — três greps no CSS não explicavam nada, e o `getComputedStyle` deu a resposta em uma chamada: `inShell: false`. O `.fx-overlay` é IRMÃO do `.fx-shell` sob o `#root`, não descendente. Sem o reset `.fx-shell input`, o input fica com a borda nativa `2px inset` e fundo branco (a pill de dentro); sem o `box-sizing` do shell, vira `content-box` e mede 482px dentro de um container de 460px — que é o que empurra o olho para fora. **É o mesmo defeito do menu do topo, mas o guard de portal não pega**: lá havia `createPortal`, aqui não há portal nenhum. O `.fx-portalmenu` e o `.fx-overlay` agora dividem o mesmo bloco, e ele passou a carregar também os resets de ELEMENTO (`input`/`button`), não só a escala tipográfica — a escala sozinha deixa a chrome nativa, que é justamente a metade que parece quebrada. O guard checa as três coisas para cada classe. **Duas lições de teste na mesma rodada:** a primeira versão do `blockFor` exigia `{` logo depois do nome da classe e não achava nada, porque a lista de seletores é compartilhada (`.fx-portalmenu,\n.fx-overlay {`) — o guard reportou a regra como ausente estando ela lá. E a minha primeira mutação de box-sizing **passou**, o que eu quase li como guard fraco: era a mutação que estava incompleta, sobrava um `.fx-overlay *::after` sem vírgula no fim da lista. Com as três ocorrências removidas, vermelho. |
| 2026-08-23 | **Fechando a dívida do sweep — inclusive a que eu tinha dito que faria e não fiz** | O dono perguntou se eu tinha aplicado tudo, e a resposta honesta era não. **(1) O custo que a revisão de segurança levantou e eu prometi registrar na ADR-41:** a sondagem de username entrega uma LISTA DE ALVOS para a trava de login que já existia — com um handle na mão, cinco senhas erradas deixam a conta 15 minutos sem login, renovável. A trava é antiga e antes exigia saber endereços; o que mudou é a lista ser obtenível. Registrado com os dois limites que o atenuam: a sondagem CONFIRMA um palpite e não enumera, e o `loginByIP` limita a ~4 vítimas por IP por janela. **(2) `TestAvailability_EmailProbeIsMountedOnlyUnderAdmin`** — a colocação do probe de e-mail era o argumento inteiro de segurança e vivia só em comentário. Anda pela árvore de rotas do harness (que monta as duas superfícies) e exige que todo `email-available` esteja sob `/api/admin/` e todo `username-available` sob `/api/auth/`, com `NotEmpty` nos dois para o guard não passar verde sobre uma rota renomeada. Provado espelhando a rota em `/api/auth`: vermelho. Espelhar "por simetria com a linha do username" é a regressão mais provável desta feature. **(3) `userBucketKey(prefix, uid)`** substitui cinco construções inline, duas delas repetindo `"stepup-password:"` literal. **(4) Testes de 500 dos dois handlers**, forçando a falha por renomear a coluna que cada consulta lê — é o ramo que um formulário lê errado com mais facilidade, porque um `available: true` vindo de consulta quebrada seria um check verde que o banco nunca concordou em dar; asserta também que o texto do pgx não vaza. **(5) Fake timers** nos testes do hook: 3,6 s de sono real viraram 135 ms, e uma suíte que paga tempo real para testar um timeout está a um runner lento de virar flaky por motivo alheio ao código. **(6) O único achado que resolvi NÃO mudar, agora documentado como decisão em vez de omissão:** a cobrança do orçamento acontece mesmo quando o cliente aborta. Cobrar depois da consulta faria quem desliga antes da resposta não pagar nada — que é o orçamento inteiro, já que nada obriga um atacante a ler o corpo para aprender pelo tempo da conexão. O custo real só aparece quando o servidor é mais lento que o debounce de 450 ms. |
| 2026-08-23 | **`make migrate-up` não alcançava um Postgres no host — e o guard que escrevi vazou a senha** | A migração 000038 não subia: `connection refused` numa instância cujo backend estava conectado e saudável. Causa: `POSTGRES_HOST=localhost` é suportado — o compose aliasa `localhost` para o host-gateway no backend via `extra_hosts` — mas o CLI do migrate roda em container próprio, sem esse alias, onde `localhost` é ele mesmo. **A ordem de leitura importa aqui e eu quase errei:** achei que `--add-host=localhost:host-gateway` resolveria e fui TESTAR antes de escrever — o Docker anexa ao `/etc/hosts` e a linha `127.0.0.1 localhost` preexistente continua vencendo, o nome resolve para `::1` mesmo com o alias. A correção reescreve o host na URL (`MIGRATE_DB_URL`), o que também cobre um `DB_URL` sobrescrito à mão, não só o derivado de `POSTGRES_HOST`. O guard existente (`test-make-migrate-path.sh`) ganhou as duas metades: um host local VIRA `host.docker.internal`, e qualquer outro passa INTOCADO — sem a segunda, um Postgres genuinamente remoto seria silenciosamente redirecionado para a máquina do desenvolvedor. Provado por três mutações, todas vermelhas. **E o guard, na primeira execução, imprimiu a senha real do Postgres da instância no terminal** — exatamente o defeito que o `logsafe` existe para impedir, no único arquivo cujo trabalho é ler essa expansão. Reescrito para sondar com `DB_URL` SINTÉTICO passado na linha de comando (que vence o `.env`) e nunca ecoar uma DSN: as mensagens nomeiam só o host. A senha ficou exposta no meu contexto e no scrollback do terminal; avisei o dono. Índice conferido em `pg_indexes` e `schema_migrations` em 38, não-suja — nunca pelo código de saída. |
| 2026-08-23 | **Sweep de 5 agentes na disponibilidade — 5 HIGH, e o mais grave nenhum humano veria** | **Três dos cinco agentes acharam o mesmo HIGH independentemente:** `availabilityUser` ficou de fora de `SweepLimiters`. Parecia vazamento de memória e não é — o mapa é limitado pela contagem de contas. O defeito real é outro e eu não tinha visto: `attemptlimit` conta falhas CONSECUTIVAS, zeradas por `CommitSuccess` (que este chamador nunca tem, toda sondagem é uma cobrança), por um bloqueio já expirado, ou pelo sweep despejando uma entrada ociosa. Sem o sweep sobra só o segundo caminho, então `fails` sobe monotonicamente pela vida do processo e **uma conta que edita o username poucas vezes por ano acaba levando um bloqueio de cinco minutos que não fez por merecer**, com a tela dizendo apenas "não deu para verificar". Meu próprio comentário mentia dizendo "60 por 5 minutos". **O teste que deveria pegar isso era cego por construção:** `TestSweepLimitersEvictsEveryBucket` semeava 4 dos 10 baldes e afirmava o literal `Equal(t, 4, …)` — ficou verde através de seis adições e ficaria através desta. Reescrevi derivando de `limiters()` e **a mutação continuou passando**, porque um teste que varre o que a fatia contém não vê um balde ausente DELA. A versão final reflete sobre os campos do `Handler` e exige que todo `*attemptlimit.Limiter` esteja na fatia; aí a mutação ficou vermelha. Um teste cujo nome promete completude tem que derivar completude. **O agente de Code Review achou uma contradição que eu tinha criado:** o probe contava uma troca de e-mail pendente como `taken`, mas o `AdminCreateUser` conflita só em `app_user` — então a tela apagava um Create que o servidor aceitaria, sem saída, porque a lista de usuários não mostra conta nenhuma naquele endereço. É o §5 ao pé da letra. Virou `reason: "pending"` junto de `available: true`: avisa, não recusa. **O agente de Test Quality provou por mutação que o lado admin não tinha teste nenhum** — trocar o gate por `false` deixava 21/21 verdes. Escrevi o teste e **ele também sobreviveu à mutação**, porque eu não preenchia a senha e o botão já estava travado por isso; só depois de preencher é que o gate passou a ser medido. Mesmo padrão do resto da sessão: o teste que "pega" o bug precisa ser provado que pega. **Um defeito que só apareceu porque um teste usou a API de um jeito que o produto não usa:** ao injetar o probe como função em vez de URL, ele entrou nas deps do efeito — os dois chamadores passam referência estável de módulo, mas o teste passou arrow inline e o resultado foi loop infinito até OOM, não um aviso. O probe agora vive num ref, fora das deps. Outros corrigidos: guarda de abort faltando no braço de SUCESSO (uma resposta que chega logo antes da próxima tecla escrevia sobre o valor anterior, e como o gate deriva desse estado, a ordem inversa limpava uma recusa legítima e reabilitava o Salvar); Enter submetia o que o botão recusava; `p, _` que falharia ABERTO se a rota fosse remontada em `Optional`; `Retry-After` descartado; a regra de nomes reservados re-derivada no handler em vez de compartilhada; `AvailabilityHint` sob `account/` sendo importado por `admin/`; ternário aninhado onde `reason: 'empty'` caía no braço de forma por acidente do `else`; e a rota ausente do `test/server.ts`, sem a qual os testes de componente passavam por sorte. **Uma decisão contra o conselho do agente:** a migração 000038 (índice parcial em `email_change(new_email_normalized)`, medido 90→4 buffers com 5k pendências) **não** sobe o `RequiredSchemaVersion`. A constante significa "as consultas deste binário dependem deste schema", e nenhuma consulta depende de um índice EXISTIR — o mesmo SQL devolve as mesmas linhas sem ele, só mais devagar. Subir recusaria o boot de toda instância ainda não migrada, trocando uma indisponibilidade real por uma melhoria de planner. 12 testes de integração + 25 de frontend nesta rodada. |
| 2026-08-23 | **Disponibilidade ao vivo de username e e-mail — e onde ela NÃO foi ligada** | O dono pediu que criar usuário, alterar username e trocar e-mail verificassem existência enquanto se digita, com um check verde, bloqueando o envio se não estiver liberado. **A ADR-41 tinha descartado exatamente isso**, com a justificativa "mais um oráculo, sem ganho: o save já responde 409" — e metade dessa frase estava errada. O ganho existe e é o que o dono pediu: descobrir antes de apertar o botão. A outra metade continua certa e é ela que decide o alcance: um endpoint de disponibilidade responde sobre o identificador de OUTRA conta sem pedir senha, então o que muda em relação ao 409 não é a existência do oráculo, é o **preço de cada pergunta** — hoje uma senha válida ou ser admin. Levei o custo ao dono antes de escrever código e ele escolheu o alcance recomendado. **Ligado:** username (só sessão, teto de 60 por 5 min por usuário) e e-mail na criação pelo admin (sem teto, porque depois do `RequireAdmin` o chamador já lista todas as contas com endereço — não revela nada que ele não leia direto). **Não ligado:** a troca de e-mail, que mantém a resposta no envio, onde a senha atual é o custo por tentativa; incluí-la daria a qualquer pessoa convidada um enumerador rápido e sem senha das caixas com conta. **Detalhes que a implementação exigiu:** o orçamento é cobrado ANTES da consulta e também para formas recusadas, senão basta acrescentar um caractere inválido para sondar de graça (mesma razão do balde do login incrementar para endereços inexistentes); probe vazia é gratuita, porque não consulta nada e cobrar só puniria quem limpa o campo; o próprio username do chamador conta como disponível, ou o formulário reportaria "em uso" sobre o nome que o dono está olhando; e uma troca de e-mail PENDENTE conta como endereço ocupado, porque o índice único guarda só a coluna viva e um endereço em migração passaria no teste para perder a corrida na confirmação, depois de anunciado livre. **Um teste meu pegou um erro de UX que eu tinha acabado de introduzir:** bloquear o envio enquanto a consulta está em voo deixa o botão morto pelos 450 ms do debounce, sem causa visível — agora só a RECUSA bloqueia, e um clique que corre com a checagem chega ao servidor, que recusa igual. Outro teste do orçamento falhou por culpa dele mesmo: o valor "malformado" chegava vazio e probe vazia não é cobrada. 8 testes de integração + 7 de frontend; `useAvailability` com debounce de 450 ms e abort por tecla. |
| 2026-08-23 | **Menu do usuário fora do shell, e o username no lugar certo** | Duas queixas do dono na mesma sessão, e as duas apontaram para erros meus de natureza diferente. **(1) O menu do topo estava "feio, diferente de todo o resto":** é o INVERSO da armadilha de cascata já registrada aqui. Lá as classes eram nuas e perdiam a disputa de especificidade; aqui o CSS estava correto e o ANCESTRAL de que ele dependia não existia — `createPortal` manda o dropdown para `<body>`, fora do `.fx-shell`, e com ele vão embora o `box-sizing: border-box`, a fonte da interface, a escala 14px/600 e o reset `.fx-shell button`. Resultado: borda e fundo nativos do botão, tipografia do agente e itens `content-box` cujo `width: 100%` mais padding transbordavam o próprio container. Nenhum guard existente enxergava: a classe ESTAVA declarada, então o de classes órfãs passava, e o de reset só conhece `.fx-shell button`. `.fx-portalmenu` reestabelece o contexto e `scripts/test-css-portal-menu.mjs` (com self-test, no CI) recusa um `createPortal` com `<button>` na subárvore cujo raiz não a vista — provado por reversão contra a árvore real. O LocalePicker escapava da metade visível só porque define as propriedades inline; faltava `font-family` e `box-sizing` nele desde sempre. **O tooltip por cima do menu me custou DUAS correções erradas, e a segunda passou num teste.** Remover o `data-tooltip` ao abrir ENCALHA a bolha: `onOut` acha o âncora com `closest('[data-tooltip]')` e, sem o atributo, nada casa — some só no Escape ou num scroll. Fechar no `mousedown` também não basta: `close()` zera o âncora, o ponteiro continua ali, e o próximo evento de mouse reabre em 180 ms, de volta por cima do menu. Escrevi um teste que só verificava a bolha sumida logo após o clique e ele ficou verde com o bug intacto — a tela real é que mostrou. A supressão agora é presa ao elemento pressionado e liberada no `mouseout`. **(2) "Por que o username não está em Acesso?"** — pergunta certa, e o erro era de arquitetura da informação, não de layout: um username é IDENTIFICADOR, digitado na tela de login exatamente onde vai o e-mail, e `verifyPassword` resolve um só identificador contra as duas colunas. Arquivado em Perfil, ao lado do nome de exibição e do idioma, ele lia como apelido — e é por isso que ninguém pensaria em procurá-lo lá. Mudou para Acesso, entre o e-mail e a senha, com `SectionRow` próprio, e SAIU do formulário de perfil para um identificador não ter dois lugares que o editam. Escreve por `auth.updateUsername`, que manda o username SOZINHO: todo campo do `PATCH /profile` é tri-state, então replicar um nome de exibição em cache reverteria um rename feito em outra aba. Limpar é oferecido na mesma linha, porque username que não se remove não é opcional, é adiado. **Uma falha de processo minha:** rodei `bun run typecheck` e não `bun run build`, e o `tsc -b` do build pega o que o `--noEmit` não pegou — a imagem web falhou ao compilar e eu li o `make up-build` como sucesso porque o `| tail -8` engole o código de saída do pipeline. Reportei "rebuild concluído" para uma build que não existiu. |
| 2026-08-22 | **ADR-41 — username opcional e troca de e-mail em duas etapas** | Nenhum dos dois existia: `username` não aparecia em lugar nenhum do backend e não havia caminho de troca de e-mail. Duas decisões foram levadas ao dono antes de qualquer código, porque mudavam a arquitetura, e ele escolheu as duas seguras: troca **verificada** (link para o novo endereço) e username **opcional**. **A proibição do `@` é a metade que sustenta o username:** o login resolve UM identificador contra as duas colunas na MESMA instrução — ramificar em "parece um e-mail?" daria dois tempos de resposta, que é o oráculo de enumeração que bcrypt-sempre-roda, o 401 único e o piso de 250 ms existem para fechar —, então um username com forma de endereço ficaria no espaço de nomes das caixas de todo mundo e recolheria as tentativas de senha de outra conta. Recusado no CHECK do banco E no handler. **O orçamento de tentativas passou a ser resolvido antes de ser cobrado** (`loginBucketKey`): chaveado pela string digitada, o atacante alternaria os dois nomes da mesma conta e teria o dobro das tentativas enquanto o teto continuaria marcando cinco. **A troca de e-mail verifica tudo no COMMIT, não no pedido** — época de credencial, endereço ainda livre, linha ainda não gasta — e gastar o token e mover o endereço são uma instrução só. O endereço ANTIGO recebe aviso **sem link em nenhum dos dois braços** (`shape_notice`), porque quem o lê pode ser alguém cuja conta está sendo tomada por quem já tem a sessão. Consumir revoga TODAS as sessões, com razão própria `email_changed`. **Dois erros meus no caminho, ambos pegos por teste:** um `replace` sem assert não inseriu a rota pública e o 404 só apareceu na integração (é exatamente a armadilha que já está registrada neste arquivo); e uma inserção de i18n assumiu que a chave-âncora era a última do objeto, quebrando o JSON dos três locales — consertado por um reparo geral de vírgulas, não por `git checkout`, porque a sessão inteira está sem commit. O guard de boot do mailer pegou a terceira: template sem braço de texto recusa o processo, como projetado. **O sweep de 5 agentes achou dois HIGH e três MEDIUM reais, todos corrigidos.** (1) `TestLinklessMessagesCannotBeGivenALink` — o teste que É a garantia do aviso sem link — tem uma lista fixa de templates e eu não adicionei o novo; ao adicionar, ele FALHOU, porque a injeção não fornecia os params legítimos e o `missingkey=error` quebrava o render antes de checar o que o teste pergunta. (2) `looksLikeAddress` reimplementava o `validateEmail` já existente e perdia o limite de tamanho: um endereço de 400 caracteres recebia 202 e virava 500 opaco na confirmação, para um chamador não autenticado sem caminho de correção — e ao reusar o validador compartilhado descobri que ELE aceitava `a@b.` (ponto no fim do domínio), o que agora também está apertado no registro e nos convites. (3) `email_change.session_id` era gravado e nunca revalidado, com o comentário da migração prometendo o contrário: revogar UMA sessão não incrementa `token_version`, então quem visse um dispositivo estranho e revogasse só ele deixaria a troca pendente viva para quem estivesse nele. (4) O username era gravado normalizado nas DUAS colunas, o que fazia o par de colunas e o CHECK que o guarda não descreverem nada — agora a caixa digitada sobrevive e só a coluna de lookup é dobrada. (5) Faltava índice não-parcial em `email_change(user_id)`: o CASCADE precisa enxergar as linhas consumidas, que nunca saem da tabela. Uma guarda do próprio repo também pegou `email_change` fora do `testdb.Reset`. **A tripla revisão de correção, rodada DEPOIS do sweep, achou o que os cinco não acharam** — e a lição é que as duas não se substituem: o sweep pergunta "está limpo?", a tripla pergunta "está certo?". (a) A revisão adversarial REFUTOU a afirmação do próprio código de que o aviso sem link não pode carregar um link: `validateEmail` não checava a parte local, então `https://evil.tld/verify?redirect=@ok.com` passava como endereço e era interpolado como `{{.NewEmail}}` na mensagem cuja garantia inteira é não ter link — e todo cliente de e-mail linkifica um `https://` cru, para a pessoa que está sendo atacada, enviado por nós. A correção é allowlist (`atext` do RFC 5321), não blocklist, com `/` deliberadamente fora para que `//host.tld@ok.com` não leia como URL. (b) A caixa do e-mail estava sendo dobrada nas duas colunas — o MESMO defeito que o sweep tinha achado no username uma linha ao lado e que eu corrigi só de um lado. (c) `loginBucketKey` engolia erro real de banco no mesmo caminho de "identificador desconhecido", devolvendo orçamento dobrado justamente durante uma falha. (d) O `EmailRow` confundia "carregando" com "não há troca pendente", fazendo o formulário sumir com o que o usuário já tinha digitado. **E a lacuna positiva que a revisão A apontou foi fechada:** faltava travar que a rotação de refresh NÃO mata uma troca pendente. Só existia a direção negativa, então uma regressão que passasse a matar a troca a cada rotação entraria verde — e entraria em silêncio, porque o confirm responde o mesmo 404 de qualquer outra falha. Dois testes novos (rotação comum e o irmão da janela de graça), ambos provados por reversão: mutar a guarda de sessão para recusar qualquer sessão já rotacionada deixa os dois vermelhos e mantém o negativo verde. 20 testes de integração + 17 de frontend; migração 000037; ADR-41; SDD-AUTH-RBAC atualizado no modelo de dados, no diagrama de login e na tabela de rotas. |

### Lições / followups

- **Follow-up NOVO (nada trava o template contra drift da config):** `mailFromEnv` lê 18 variáveis de ambiente e nada garante que elas apareçam em `.env.example`. Foi por isso que as 8 da fase B ficaram de fora por uma fase inteira, e remover o bloco hoje não quebraria nenhum teste. O repo já articula o princípio em outro contexto — a lista de redação do `logsafe` é travada pelos DOIS lados justamente porque "bounded by construction beats bounded by discipline" — e aqui não há nada. Um teste que compare o conjunto lido por `mailFromEnv` (e pelas outras funções `*FromEnv`) contra as chaves declaradas no template fecharia a classe inteira, não só esta ocorrência. Não foi feito neste lote para não misturar correção com prevenção: o lote já cresceu de uma pendência documental para dois defeitos de boot.
- **Follow-up NOVO (o gate do backend nao e hermetico):** `backend/Makefile` faz `include ../.env` + `export`, entao `make coverage-run` entrega o `.env` inteiro do desenvolvedor ao binario de teste, enquanto o CI -- que nao tem `.env` -- pula o include. **Mais de dez testes de `internal/config/config_test.go` ficam vermelhos** com um `.env` completo, e nenhum deles esta errado: eles afirmam sobre variaveis ausentes. Medido nesta rodada com `MAIL_TRANSPORT=amqp AMQP_URL=... AUTH_PUBLIC_URL=... BACKEND_BIND=0.0.0.0`. O efeito e pior que um teste frouxo: a falha aparece so na maquina de quem esta no meio de uma mudanca, que le como "meu patch quebrou" e procura no lugar errado. Consertei o `mail_transport_test.go` (helper `withCleanEnv`) e uma linha de `config_test.go` porque bloqueavam o gate; o resto do pacote segue exposto. Conserto de raiz e o Makefile passar so o que os testes precisam em vez de exportar o arquivo todo.
- **Follow-up NOVO (o SMTP: verificar antes de concluir):** `internal/mailer/mailer.go:201` monta o `tls.Config` sem `RootCAs`, e `MAIL_INSECURE_SKIP_VERIFY` e recusado contra host nao-loopback -- o que sugere que um relay SMTP de intranet com CA privada esta inalcancavel. **Mas essa e exatamente a forma do diagnostico que errei no AMQP.** Antes de propor qualquer variavel nova: verificar se `net/smtp` ou a montagem atual ja oferecem caminho (o pool do sistema pode ser estendido no host, o relay pode aceitar STARTTLS oportunista, o certificado pode ser instalado como confiavel na imagem). So depois decidir se falta capacidade ou documentacao.
- ~~**Follow-up NOVO (`docs/SDD-EMAIL-ASYNC.md` desatualizado)**~~ **Fechado em 2026-08-21** — a tabela ganhou `AMQP_ROUTING_KEY` e `AMQP_ALLOW_PLAINTEXT`, e a frase da recusa incondicional foi corrigida. A versao anterior deste followup citava `AMQP_CA_FILE`, variavel que EU havia revertido no mesmo lote: um followup apontando para um fantasma, que e a mesma armadilha do comentario desatualizado que este arquivo descreve duas vezes.
- **Follow-up da fase B (imagem do mailer):** hoje o worker AMQP roda a partir da imagem do backend, com `entrypoint: ["/foldex-mailer"]`. Uma imagem própria seria melhor — o processo que decifra links de reset não precisa de Chromium nem das migrations — mas exige criar `justoeu/foldex-mailer` no Docker Hub, um terceiro braço no matrix de `release.yml` e generalizar `scripts/lib/compose-image-pin.awk`, cuja lista de imagens exigidas (`want`) e cujo casamento por nome ainda são `foldex-backend`/`foldex-web`. Enquanto isso não acontece, **um serviço com imagem Foldex de nome novo é recusado, não ignorado**: o programa reconhece `justoeu/foldex-<qualquer>` e exige que esteja fixada na versão da release, então a linha entra no gate desde o primeiro dia — o que falta é apenas ela ser *exigida*, como `backend` e `web` são.
- ~~**Follow-up da fase C (`.env.example` segue pendente):** nenhuma variável nova entrou na fase C, mas as sete da fase B continuam fora do arquivo canônico.~~ **Fechado em 2026-08-19** — ver a linha da fase B abaixo.
- **Follow-up da fase C (OTP de step-up e o `PolicyReader`):** `POST /2fa/email/send` usa `h.otpTTL`/`h.otpCooldown`, que já leem a política — mas `emailOTPTTL` continua constante nos caminhos que não passam por eles. É o mesmo follow-up do ADR-35 registrado abaixo, agora com um call site a mais.
- **Follow-up da fase C (backfill deliberadamente NÃO feito):** contas com e-mail já verificado **não** foram inscritas em `email_factor`. Inscrevê-las cadastraria pessoas num fator que ninguém escolheu, e mudaria o significado de "tenho 2FA" sem que elas soubessem. A consequência é comportamental e está nos READMEs: numa instância com SMTP, quem antes recebia código por e-mail como alternativa dentro de um desafio TOTP agora precisa cadastrar o método primeiro.
- ~~**Follow-up da fase B (`.env.example`):** as variáveis novas estão documentadas nos dois READMEs e no compose, mas **não** em `.env.example`.~~ **Fechado em 2026-08-19** — as OITO (a anotação original dizia sete e listava oito) entraram no template canônico com os mesmos defaults que `docker-compose.yml` já declarava: `MAIL_TRANSPORT=inproc`, `AMQP_URL=`, `AMQP_EXCHANGE=foldex.mail`, `AMQP_QUEUE=foldex.mail.send`, `AMQP_ROUTING_KEY=send`, `AMQP_PREFETCH=4`, `MAIL_OUTBOX_BATCH=32`, `MAIL_OUTBOX_POLL_SEC=5`. Verificado antes de escrever, não deduzido: `validateMailTransport` recusa o boot num valor desconhecido em vez de cair para `inproc`, exige `AMQP_URL` sob `amqp` e recusa `amqp://` contra host não-loopback; `LoadMailer` lê apenas `Mail` e a chave de cifra, o que é a prova de que `cmd/mailer` não tem credencial de banco. Dois riscos checados e descartados: `scripts/init-env.sh` faz `case` na LINHA INTEIRA e só reescreve `RUSTFS_ROOT_SECRET_KEY=`/`RUSTFS_SECRET_KEY=`, então o `AMQP_URL=` vazio é copiado verbatim e não vira segredo gerado (`scripts/test-init-env.sh` verde); e `dast.yml` copia o template para `.env` e sobe a stack — aqui eu afirmei "continua funcionando" com base no caminho Go (`inproc` retorna antes de ler `AMQP_URL`), o que **não alcança a interpolação do compose** e a revisão de escopo derrubou: `docker compose config` falhava para o arquivo INTEIRO, com ou sem `--profile amqp`, porque `docker-compose.yml` usava `${AMQP_URL:?...}` — a forma com dois-pontos falha para vazio E para ausente, e o compose interpola serviços de profile inativo. Reproduzido nos dois lados (idêntico antes e depois do diff, então não era regressão minha) e **corrigido neste mesmo lote** para `${AMQP_URL:-}`, com o porquê registrado no compose. A exigência não se perdeu: ela já vivia em `validateMailTransport`, coberta por `TestLoad_AMQPTransportRequiresAURL`. O bug entrou em 5f44a92 (2026-08-18) e nunca foi exercitado porque o DAST roda mensalmente e sua última execução foi 2026-08-01 — `make up` a partir de um `.env` recém-gerado estava quebrado desde então. Os READMEs já estavam em paridade (5 menções em cada), então não houve mudança de README.
- ~~**Follow-up do ADR-35:** a validade do OTP e o cooldown de reenvio não são LIDOS pelos caminhos de 2FA.~~ **Já estava feito** — auditado em 2026-08-19: `Handler.otpTTL`/`otpCooldown` (`internal/auth/handler.go`) leem a política e fazem clamp nos pisos compilados, e os quatro call sites (login, reset, enroll e step-up de e-mail) passam por eles. `emailOTPTTL` sobrou apenas como fallback para `h.policy == nil`. A anotação ficou para trás quando a fase C aterrissou — registrado aqui em vez de apagado, porque followup que afirma estado de código desatualizado é a mesma armadilha do comentário que manteve o bug do sort Alfabético vivo por sete semanas.
- **Follow-up do ADR-33:** a matriz é exibida como leitura. Editar quais permissões cada papel detém exigiria persistir a matriz em vez de compilá-la, e a decisão consciente foi manter a matriz no código — onde o compilador a verifica — até haver demanda real por papéis customizados.

- **Capacity follow-up:** os tetos de 8 são independentes por pool; medir starvation do pgxpool antes de introduzir um semáforo agregado entre preview, change-check e push.
- Backend compila e passa `go vet` limpo em Go 1.26.
- Web type-checa com `tsc --noEmit`; o `npm run build` local pode quebrar caso o npm registry configurado localmente sirva um `@rollup/rollup-darwin-arm64` com versão vazia. Não afeta o **Docker build** porque o estágio `node:20-alpine` usa o registry público padrão. Se quiser rodar `vite build` localmente, criar `web/.npmrc` apontando pra `https://registry.npmjs.org/`.
- **T11 (testcontainers)** ficou de fora: rotina típica pgxpool + repository é coberta pelos smoke tests. Quando virar produto real, voltar.
- Extension foi feita **vanilla** (sem `@crxjs` / Vite / React) pra não trazer mais um bundler — o popup tem ~80 linhas de JS. Se for crescer (history, multi-account, sync), promover a `extension/src/` com TS.
- Migration trivial pendente: `UNIQUE(url)` em `link` se quisermos garantir dedup forte no nível do DB (hoje é via re-SELECT na importação).
- ~~**Bug pré-existente (pré-Notes, achado 2026-06-30 durante teste manual da feature Notes):** `mergeAlphaCells` ignora `pinned` no sort Alfabético.~~ **Corrigido em 2026-08-19** — ver entrada no log acima.

> Templates de entrada: `YYYY-MM-DD | Tn | <git short hash> | <observação curta>`.

### `span.user.id` — identidade no trace (emenda ao ADR-39)

O ADR-39 já entregava SDK OTel, exporter OTLP/gRPC por `OTEL_EXPORTER_OTLP_ENDPOINT`,
`otelpgx` e correlação `trace_id`→Loki. O que faltava para o APM por usuário era o
atributo: `user.id`, `user.roles` e `foldex.auth.via` no span SERVER.

**Lição — um mount anota o grupo em que foi montado; um seam anota toda identidade que
existe.** O primeiro rascunho era um middleware montado no grupo `/api`, com teste de
integração através do `server.New` real matando dois mutantes (desmontar; montar antes do
auth). Passou nos dois — e ainda assim estava errado: `/api/auth` monta o PRÓPRIO
`Authenticate` dentro do handler, fora daquele grupo, então ~20 rotas autenticadas
(sessões, troca de senha, 2FA, tokens) não recebiam nada. Sem erro de build, sem panic,
resposta idêntica. Achado pelo agente de Code Review do sweep, não pelos testes.

O que o teste provava era "o mount funciona onde eu montei", não "toda identidade é
anotada" — a mesma classe de falso-verde do sweep anterior, um nível acima. A correção
move a anotação para os três pontos onde um principal nasce (`Authenticate`, `Optional`,
bootstrap de `AUTH_ENABLED=0`), e os guards novos vão através de sessão real.

**Recusado:** anotar dentro de `authctx.WithPrincipal` seria impossível de esquecer, mas
`authctx` importa só `context` e puxar OTel para lá o colocaria em todo repositório.

**E os seams corrigiam a INSTÂNCIA, não a CLASSE** — achado do agente de Test Quality, a mesma
lição um nível abaixo outra vez: teste por seam só prova os seams que existem hoje, e um quarto
(callback OAuth, gate de token público) reabriria o buraco em silêncio.
`TestEveryPrincipalSeamAnnotatesTheSpan` varre o AST de `internal/**` e exige a anotação na mesma
função de todo `authctx.WithPrincipal`; um quarto seam plausível morre nele. O mesmo agente
mostrou que os guards `IsRecording` e `Role/Via != ""` podiam ser deletados sem quebrar teste
nenhum — o primeiro é guarda de ALOCAÇÃO, então só `testing.AllocsPerRun` o distingue da própria
ausência. Ambos têm mutante morto agora.

**Conjunto de atributos FECHADO, testado por asserção negativa.** Nada de e-mail, nome ou
`session_id` — INV-170.

**Achados do sweep, todos fechados:** o exporter fala texto claro por padrão e as docs não
diziam (Security MEDIUM); leitura do store de traces é privilégio administrativo e isso não
estava escrito (Security MEDIUM); helpers de span duplicados em 4 arquivos viraram
`internal/pkg/spantest` (Code Quality MEDIUM); slice de atributos pré-alocado (Performance
LOW). Em aberto por decisão: não existe kill-switch de identidade separado — quem não aceita
a egress desliga o tracing.

**Achado colateral:** o ADR-39 shipou sem seção no README (§3 pede). Fechado nesta mudança
nos dois idiomas, e `OTEL_EXPORTER_OTLP_ENDPOINT` — que o `docker-compose.yml` já lia — não
estava documentado no `.env.example`, então quem copiava o exemplo ficava com tracing
silenciosamente desligado.

### Card em branco ganha as duas saídas, e o `user.name` do `otelpgx` sai do trace

**Duas saídas no card, não uma.** Um card sem imagem visível passa a oferecer 🖼️ (abre o
diálogo do link já no painel de imagem) E 🔄 (recaptura). A primeira versão trazia só a de
upload, e a correção do usuário foi o achado: eu havia tirado o botão de recapturar de
`preview_status === 'ok'` argumentando que recapturar não ajudaria — **exatamente o caso em
que ele funciona**. Página sem `og:image` responde `ok` (nada falhou) e é precisamente a
condição que o worker atende com SCREENSHOT (INV-083). Gatear por status escondia a única
ação capaz de preencher aquele card. O gate agora é o que o leitor VÊ: `!showPreview` para o
upload, `!showPreview || preview_status !== 'ok'` para a recaptura — cobrindo ok-e-vazio,
`failed` e imagem que dá 404 em tempo de render (INV-082).

**`user.name` do `otelpgx` era ambiguidade, não ruído.** Assim que a identidade da conta
entrou no span (INV-170), o `user.name` que o `otelpgx` carimba por padrão passou a competir
por significado: o semconv tem UMA chave e a aplicação passou a ter dois sujeitos que cabem
nela — o papel do Postgres e a conta servida. O modo de falha não é ruidoso, é **convincente**:
"requests por `user.name`" responde `user_foldex` para 100% do tráfego e parece uma quebra
funcionando. `db.New` desliga os connection details do otelpgx e reinjeta o mesmo conjunto
menos aquela chave (`connAttrs`); `server.address`/`server.port`/`db.namespace` ficam porque
variam por deployment, o papel não varia e já está no `DB_URL`.

**Dois testes, e só um deles serve.** `TestConnAttrs_*` fixa o conjunto sem Docker mas fica
verde se a option sumir; `TestQuerySpansNeverCarryThePostgresRole` roda query real sob pai
SERVER e afirma sobre TODO span gravado. Os dois mutantes (remover a option; remover o
`WithTracerAttributes`) morrem só no segundo — é a mesma lição da rodada anterior, em que um
teste provava que um middleware estava montado onde eu o havia montado.

**Correção de comentário:** `main.go` e a ADR-39 diziam que o tracer do pgx "resolve o provider
por query". Ele o captura UMA vez, em `NewTracer`. A ordem `Setup` antes de `db.New` continua
obrigatória — pela razão certa agora.

**Achado colateral do gate — asserção que media o encoder, não o código.** O
`TestOptimize_RejectsDecodeBomb` fixava `len(bomb) < 1_000_000` como sanity check.
Verde no Go 1.26.6 (pin da CI), vermelho no 1.27.0 local: o `image/png` da 1.27 emite
1,35 MB para os mesmos pixels sólidos. Nenhum arquivo do repo mudou — a asserção
media a biblioteca padrão. Virou razão contra o framebuffer decodificado
(`dim*dim*4 / 50`), que é o que o comentário sempre disse significar e sobrevive à
troca de encoder. Sem isso, o §1 ("sempre no latest stable") entraria em conflito com
o gate no dia do bump para 1.27.

### ADR-43 PR4 — job `user_zip`: o ZIP per-user agendado (2026-08-26)

O quarto job do backup-agent existe: `BACKUP_USERZIP_AT` (âncora `HH:MM`, vazio = off)
faz o agente rodar `backup.Service.Export` para cada `app_user` ativo, cifrar com age,
tirar sha256 do ciphertext e subir em `backups/users/<uid>/<ts>.zip.age`, com retenção
simples dos últimos `BACKUP_RETAIN_USERZIP=7` por usuário (só em `RETENTION_MODE=agent`).

- **Um run para o job inteiro, falha por usuário isolada.** `meta` = `{users,
  bytes_total, failed_users?, deferred_users?, prune_error?}`; o run só falha
  (`user_zip_failed`) se a listagem falhar ou TODOS os exports tentados falharem.
- **Deferência ao restore por usuário** (`restoreInFlight`, INV-104): probe antes de
  CADA export; ocupado ⇒ o usuário pula o ciclo e entra em `deferred_users`, nunca em
  `failed_users`.
- **INV-105 de graça**: o Export já exclui auth — o agente não adiciona nem remove nada.
- **Chave com timestamp UTC de largura fixa** ⇒ ordem lexicográfica é cronológica; a
  retenção poda sem parse de data e ignora qualquer chave que o job não escreveu
  (`isUserZipKey`). `RetainUserZip < 1` desliga a poda — zero mal configurado não pode
  significar "apague todos os backups de todos os usuários".
- **Seams para unit** (`export`, `listActive`, `restoreBusy`): o pipeline
  spool→age→sha→upload→retenção→deferência é provado sem Postgres nem bucket; a
  integração roda o Export REAL com 2 usuários semeados + origem vazia e prova que cada
  ZIP decifra com age padrão, abre com `archive/zip` e o manifest conta 1 link do DONO.
- **Construção opt-in**: `RegisterJob` no Agent (registro genérico — PR3 reusa);
  `cmd/backup-agent` só constrói o client RustFS de origem quando a âncora está setada, e
  o `Load` recusa âncora sem `RUSTFS_SECRET_KEY` (boot fail-fast, nunca meio-configurado).
- **Pendência registrada**: o tooling desta sessão bloqueia `.env*` — as novas vars
  (`BACKUP_USERZIP_AT`, `BACKUP_RETAIN_USERZIP`, `RUSTFS_*` no serviço backup) estão no
  `docker-compose.yml` e documentadas no README/SDD, mas o `.env.example` precisa ganhar
  o bloco correspondente num follow-up manual.
### ADR-43 PR5 — superfície de status do backup operacional (2026-08-26)

Entregue: pacote `internal/backupstatus` (repositório sem `net/http`, handler com
`httperr`, montado em `/api/admin/backup` atrás de `RequireAdmin` + o gate da permissão
nova `instance.backup`), migração 000041 (seed `('admin','instance.backup')`), bump
`RequiredSchemaVersion` 37→41 (o `backupagent.RequiredSchemaVersion` fica em 40 — o
agente só precisa da 000040), banda "Backup da instância" no settings hub (seção
`backup` do escopo admin + card no overview, TanStack Query, confirmação antes do
POST, i18n en/pt/es), mock server em sincronia, dashboard Grafana completo em
`deploy/observability/grafana-backup-dashboard.json` pinado ao registry pelo teste
`TestDashboardOnlyReferencesSeriesTheAgentServes`.

**Lições / decisões:**

- **`consecutiveFailures` foi COPIADO do agente com as mesmas quatro razões excluídas,
  citando a origem** — importar `backupagent` só pelas constantes `Reason*`/`Job*` já
  acopla os pacotes na direção certa (status lê o vocabulário de quem escreve), e o
  número que a banda mostra é o mesmo que o threshold de alerta compara.
- **A staleness de 26 h é a ÚNICA política re-declarada no cliente**, espelhando a alert
  rule shippada — resolveu a open question 2 do SDD sem env espelhada nem cadência
  inferida (a lição do INV-138: política do servidor não se re-deriva na UI).
- **O gatilho manual usa a MESMA permissão de leitura**: um "write permission" separado
  prometeria uma distinção que o servidor não entrega — o POST só enfileira uma linha
  `requested` que o agente pode ou não assumir.
- **`/api/auth/me` não expõe permissões**; a visibilidade da banda segue o padrão do
  escopo admin inteiro (`isAdminRole`), e o servidor continua sendo o guarda (404 para
  não-admin, 403 para admin sem o grant — testado nas duas direções).
- **Divergência assumida do SDD §15:** o alerta por e-mail via outbox (§9.3) ficou fora
  do PR5 — pendência registrada no SDD e na ADR-43. Screenshot da banda para o README
  também pendente.

### Tela de acesso reescrita no padrão do mockup (2026-08-26)

`AuthShell` deixou de ser um card centrado de 980px e virou um layout de página
inteira: coluna do formulário à esquerda (marca com tagline, kicker, título grande,
campos, um único botão primário) e painel de produto à direita — badge, chamada,
três pontos com ícone e uma maquete do app com pastas coloridas. O painel continua
`aria-hidden` e some abaixo de 1024px. O link de recuperação saiu de baixo do botão
e foi para a linha do rótulo de **Senha** (`AuthField` ganhou a prop `action`), e o
rodapé passou a mostrar versão + data de build — a tela de acesso é a única
superfície alcançável sem conta, e "qual versão é esta instância?" é a pergunta de
suporte mais comum numa instalação self-hosted.

O Google e o "criar conta" NÃO entraram: o botão do Google segue renderizando
apenas quando a instância habilita OAuth (comportamento anterior, intocado), e não
existe auto-registro no produto — a entrada de uma conta nova é convite ou
`Add user`.

- **"Manter conectado" do mockup NÃO foi copiado**: o que existe é *Lembrar meu
  e-mail* (INV-152), que guarda o endereço e nada mais. Um rótulo prometendo sessão
  persistente sobre um mecanismo que só preenche um campo seria a UI mentindo sobre
  o que faz.
- **O spinner de boot quebrou e nenhum guard viu**: `AuthGate` renderiza
  `.fx-auth .fx-auth-boot` — ele É o overlay, sem painel dentro — e centralizava
  herdando o `display:flex` de `.fx-auth`. Com a tela full-bleed o overlay virou
  `display:block` e o spinner foi parar no canto superior esquerdo. Classe existia,
  regra existia, testes verdes, `test-css-orphan-classes` verde: só apareceu ao
  clicar em "Esqueceu a senha?" no browser. `.fx-auth-boot` agora centraliza a si
  mesmo.
- **A pill do badge estica quando o pai vira coluna flex** — `align-self:
  flex-start` é o que a mantém do tamanho do texto. Mesma família do defeito acima:
  invisível fora do browser.
- Copy nova em `auth_login`/`auth_marketing` nos três locales; os testes que
  fixavam "sign in to foldex"/"sign in" passaram a fixar a copy nova.
- **O sweep achou o par que a UI teria mostrado divergindo**: o rodapé novo
  imprimia `BUILD_DATE` cru enquanto a sidebar já formatava `DD/MM/YYYY` na
  cópia local dela. `formatBuildDate` subiu para `version.ts` e as duas telas
  passaram a chamar a mesma função — a mesma build não pode ter duas datas.
  Os três ícones do painel viraram `<Icon d={I.…}>` do registro compartilhado,
  em vez de SVGs à mão com atributos de stroke divergentes entre si.
- **`AuthPromo` é `memo`**: sem props e imutável, mas filho de um shell que
  re-renderiza a cada tecla do formulário — dezesseis `t()` e três SVGs
  diffados por caractere digitado, num subtree que é decoração.
- **O guard do spinner existe agora**: `scripts/test-css-auth-overlay.mjs`
  exige que `.fx-auth-boot` declare o próprio `display`/`align-items`/
  `justify-content` em vez de herdar o layout do overlay. Provado vermelho
  contra o defeito real e verde de volta. Não é um motor de layout — cobre
  exatamente a dependência que quebrou, e diz isso na mensagem.

### Tela de backup reconstruída no padrão do mockup (2026-08-26)

A banda virou tela: quatro KPIs no topo, quadro 2×2 de cards (um por job,
selecionável), editor de agenda com abas espelhando a seleção, card escuro do
drill e painel do agente na coluna da direita, e o histórico com filtro por
desfecho. Os controles por job saíram dos `fx-input` genéricos e viraram o que
o mockup pede: lista de horários com "adicionar", pílulas de dia da semana,
campo de intervalo com presets (30m/1h/6h/12h) e o mesmo switch da tela de
política para o único job que pode ser desligado.

- **O que NÃO foi copiado do mockup, e por quê.** "Baixar último dump" não
  existe: o processo web não tem credencial do S3 — por design (ADR-43) — então
  o botão só poderia mentir. O painel "Retenção e destino" (destino primário,
  espelho, retenção, criptografia) também não: esses valores vivem na env do
  agente e não passam pela API. No lugar dele, a coluna da direita mostra o
  heartbeat — versão, visto há quanto tempo, capacidade por job — que é dado
  real e responde à mesma pergunta ("para onde isso está indo, e está indo?").
- **Dois KPIs do mockup foram trocados por outros dois.** "Artefatos retidos"
  não é computável (retenção é do agente) e "falhas nos últimos 7 dias" seria
  uma janela que a query não tem — o histórico é keyset, não período. Viraram
  "tamanho do dump" e "falhas consecutivas", que é exatamente o número que a
  alert rule compara. A contagem do histórico diz "nesta página", não "nas
  últimas 24 horas", pelo mesmo motivo.
- **O filtro do histórico é local e assumido como tal**: filtra a página
  carregada, e o subtítulo continua descrevendo a página inteira para não
  sugerir que o servidor foi consultado de novo.
- **Todo botão novo tem seletor qualificado por elemento** (`button.fx-bkp-…`)
  e os seis segmentados entraram na lista do `test-css-button-reset.mjs` —
  INV-154 já shipou quatro vezes neste arquivo.
- **Validação visual sem sessão no browser**: a tela exige login e esta sessão
  não pode autenticar, então o DOM renderizado foi despejado num harness
  temporário e servido com o CSS do bundle de produção. Foi assim que apareceram
  o quadro virando 3+1 numa tela larga (`auto-fit` trocado por duas colunas
  fixas) e o campo de horário esticando a largura do card.
- **O sweep pegou um Rules of Hooks**: ao memoizar os derivados, três `useMemo`
  ficaram DEPOIS dos early returns de pending/error — a contagem de hooks muda
  no instante em que a query resolve, e a tela quebrava inteira. Os 22 testes
  do arquivo falharam junto, o que é o comportamento certo do gate: a regra é
  invisível no typecheck e óbvia no render.
- **Memo boundaries eram obrigatórios aqui, não enfeite**: a agenda revalida a
  cada 60 s e o `seen_at` do heartbeat muda em toda tick, então sem `memo` nas
  folhas cada poll re-renderizava a tabela de histórico inteira. `ScheduleCard`
  recebe `data`/`isPending`/`isError` em vez do `UseQueryResult` — o objeto da
  query troca de identidade a cada tick e anularia o próprio memo.
- **`backupFormat.ts` saiu do componente** e ganhou teste próprio: `formatBytes`
  tinha uma única asserção indireta, e as bordas (< 1 KiB, uma casa abaixo de
  dez, teto em TB) e o filtro de valores não-numéricos do `tables` do drill
  agora estão fixados sem render.
- **O MEDIUM do Code Quality foi pago em seguida** — e a decisão de adiá-lo foi
  errada por processo, não por técnica: adiar dívida é escolha de quem toca o
  projeto, e eu tratei como escopo meu. `ScheduleEditor` virou uma casca que
  detém o rascunho, as duas mutações e as confirmações, com `DumpFields`,
  `DrillFields`, `MirrorFields` e `UserZipFields` puros sobre UM `BackupScheduleConfig`
  — melhor do que "cada um com o próprio estado", porque não há nada para
  sincronizar. `seedDraft` substituiu os cinco `useState`, e `draftConfig`
  deixou de existir: o rascunho JÁ é a config que vai no PUT.
  Os 26 testes passaram sem uma linha tocada — que é exatamente para isso que
  os testes de PUT por job tinham sido escritos na rodada anterior.
- **A única exceção que precisou de estado local é o `UserZipFields`**: o
  servidor tem que receber `{enabled:false}` puro (horário em job desligado é
  agenda que ninguém lê), mas quem desliga e religa deve receber de volta o
  horário que digitou, não o default. Fixado em teste.

### Ajustes na tela de acesso após o primeiro olhar (2026-08-26)

- **A maquete do app ocupa a largura toda do painel, centrada.** Sangrar só
  para a borda direita foi tentado e lê como desalinhado — o painel tem padding
  simétrico em todo o resto. Agora só o TEXTO mantém a medida de 470px: uma chamada com 40 ems de linha
  não se lê, por mais espaço que o painel tenha.
- **O botão virou "Entrar" e só habilita com identificador E senha.** A senha é
  medida por COMPRIMENTO, nunca trimada: espaço no começo ou no fim é caractere
  legítimo, e trimar aqui recusaria uma senha que o servidor aceitaria. O
  identificador, esse sim, é trimado — só espaço é vazio.
- **O estado desabilitado perdeu o `cursor: progress`**, que prometia trabalho
  em andamento num botão que está apenas esperando o formulário.

### Log de conclusão — Auditoria remodelada (ADR-46)

**O que mudou.** A trilha de auditoria passou a cobrir conteúdo, ganhou o contexto de
origem de cada evento, agregados, uma lista de bloqueio de IP, uma tela remodelada e uma
aba de atividade própria na conta. Migrações `000044` (contexto) e `000045` (`ip_block`);
`db.RequiredSchemaVersion` 43 → 45; permissão nova `instance.ip_block` (owner-only,
travada).

**O diagnóstico, que era pior que o relatado.** O usuário notou que "só o login está sendo
catalogado". Não era um bug: a `000033` diz no próprio comentário *"Content is
deliberately out of scope"*. As 15 ações existentes eram todas de identidade, e link,
nota, pasta e tag não tinham ponto de escrita nenhum. A mesma migração também recusava
`ip` e `user_agent` de propósito. Metade do mockup pedia dados que não existiam.

**Lições.**

1. **Uma recusa antiga pode estar certa pelo motivo errado hoje.** A `000033` recusou uma
   coluna `ip` porque um valor forjável com aparência autoritativa é a pior combinação
   possível numa tabela de incidente. O argumento continua correto — mas proíbe uma coluna
   *sem procedência*, não o registro em si. Guardar o endereço observado **ao lado da flag
   que diz se alguém respondeu por ele** honra o argumento em vez de contorná-lo.

2. **Escopo de leitura tem que ser SQL.** `subject` é a única coluna fora das tabelas de
   conteúdo que guarda conteúdo. Filtrar em Go depois que a linha chegou sobrevive até
   alguém adicionar a coluna "para o CSV ficar mais rico". A projeção administrativa não
   seleciona a coluna, e apaga e-mail e detalhe das linhas de conteúdo dentro do `SELECT`.

3. **Um guard de AST precisa cobrir o lugar onde a regressão mora.** A primeira versão do
   `TestAuditSubjectIsSelectedByExactlyOneQuery` andava só por `FuncDecl`, e a mutação
   "adiciona `subject` à `adminProjection`" **passou** — a projeção é uma `const` de
   pacote. Um guard que não cobre a edição mais provável é pior que nenhum, porque parece
   cobrir. Segunda lição do mesmo teste: casar `subject` e `audit_log` no MESMO literal
   deixava passar uma projeção montada por fragmentos; a tabela passou a ser casada contra
   o arquivo inteiro.

4. **Esconder na saída e selecionar na entrada não é esconder.** A busca casava em
   `actor_email` das linhas de conteúdo. `?category=content&q=alice@example.com` devolvia
   as linhas dela, e o `actor_ref` opaco virava a identidade dela — o pseudônimo era
   cosmético enquanto o `WHERE` continuasse enxergando a coluna. Achado do sweep.

5. **Um fuso horário entrou pela porta do driver.** `generate_series($1::date, $2::date)`
   fez o Postgres inferir os parâmetros como `date`, e o driver codificou o dia de
   calendário **local do processo** enquanto `created_at` era comparado no fuso da sessão.
   Num servidor em UTC-3 o dia corrente caía depois do fim do gráfico. Só aparece fora do
   UTC, e o sintoma é "um dia quieto". O limite passou a ser o `now()` do banco, dentro do
   SQL.

6. **Um middleware não testado MONTADO é um middleware que pode ser deletado.** Os testes
   unitários de `contentAudit` e `blocklistGate` passavam com as duas linhas `r.Use(...)`
   removidas do `router.go` — a mesma classe de falha do INV-170. Os testes de wiring
   passaram a ir por `server.New` e a afirmar sobre efeitos observáveis de fora.

7. **Repetir um agregado por página é um export virando cinquenta varreduras.**
   `FailureBursts` estava dentro do laço de páginas do CSV. O resultado é invariante — a
   janela não se move — então eram até 50 `GROUP BY` idênticos sobre o volume da tabela.
   Achado do sweep.

8. **Um parâmetro que é `null` em todos os call sites é código morto com cara de trava.**
   `selfIP` era passado como `null` nos dois lugares, então o trilho de auto-bloqueio do
   cliente nunca rodava. O navegador não tem como saber o endereço público dele — aquele
   trilho é do servidor, e o parâmetro saiu.

9. **Um índice que ninguém lê é escrita a mais na tabela mais escrita.** O
   `audit_log_ip_idx` não servia consulta nenhuma: todas filtram por `created_at` e
   agrupam por `ip`, sem igualdade na coluna líder. Removido antes de shipar.

**Aberto / próximo.**

- Validação manual no navegador (Configurações → Administração → Log de auditoria e
  Configurações → Conta → Atividade) — depende de sessão autenticada.
- Screenshots da tela nova nos dois READMEs.
- Sem GeoIP: as origens mostram endereço e dispositivo, não cidade.


### Log de conclusão — Cota da API autenticada (G2) e coalescência de clique público (G3)

Fecha as duas lacunas que o `docs/SDD-ABUSE-DEFENSE.md` §3.3 mede como as mais sérias e
que não tinham controle nenhum: **G2** (sessão válida = requisições ilimitadas contra um
pool de 16 conexões) e **G3** (`/go/{slug}` e `/n/{slug}` públicos, sem limitador, cada
acerto escrevendo em `click_log`). Detalhe e raciocínio em INV-181 e INV-182.

**Entregue.**

- `internal/pkg/quota` — token bucket por chave, limite passado por chamada (a política é
  editável em runtime), teto de chaves, sweep dentro do próprio `Allow`, `Refund` para
  que uma requisição recusada não custe orçamento.
- `internal/server/quota.go` — middleware montado logo abaixo da resolução do principal;
  mapa fechado de rotas caras casado por segmento ANTES do handler; 429 com `Retry-After`
  no envelope uniforme.
- `internal/pkg/clickctx` + `internal/server/clickcoalesce.go` — gate de clique no
  contexto, coalescedor em memória chaveado por `(kind, id, HMAC do IP)`, teto e sweep.
  `links`/`notes.repository_system.go` consultam o gate na única linha onde a entidade já
  está identificada e a escrita ainda não aconteceu.
- `router.go`: um campo em `Deps` e duas linhas de montagem (o `Group` público e o
  `pr.Use` da cota).

**Lições.**

- **`attemptlimit` não podia ser reusado, e a justificativa é obrigatória** (o repo tem
  regra contra segunda implementação paralela): ele conta falhas consecutivas e
  `CommitSuccess` zera o contador — uma requisição aceita apagaria o registro inteiro.
- **O padrão de rota do chi só existe depois do roteamento.** O `contentAudit` pode lê-lo
  porque decide DEPOIS do handler; uma cota não pode, então o mapa fechado é casado
  contra o path por segmento. O guard `TestExpensiveRoutes_EveryPatternNamesARouteTheRouterMounts`
  anda pelo router real com `chi.Walk` para que um padrão que deixe de nomear uma rota
  montada falhe o build em vez de cobrir nada em silêncio.
- **Recusar sem devolver o token do primeiro balde acopla os dois tetos.** Quem estourasse
  o limite por minuto perderia também os imports da hora — exatamente o falso positivo que
  a §8 do SDD declara como critério de reversão.
- **Um relógio falso instalado depois do construtor precisa que o `lastSweep` comece no
  zero**, não semeado de `time.Now()`: comparar um agora falso com um agora real deixa o
  sweep periódico sem nunca disparar, e o teste que deveria pegar isso passa por outro
  motivo.

**Aberto / próximo.**

- Os testes de fiação (`abusewiring_integration_test.go`) foram escritos e compilam
  (`go vet -tags integration ./...`), mas **não foram executados**: o daemon Docker estava
  em uso por outra frente. Rodar `make test-integration` antes do merge.
- `limit_req` no nginx (PR 3 do faseamento) e o balde de largura do `attemptlimit`
  (PR 2 / G1) continuam abertos — são de outras frentes.
- A tela que edita `api_writes_per_minute` / `api_expensive_per_hour` /
  `public_click_coalesce_seconds` é do frontend e não faz parte desta frente.

### 2026-08-28 — SDD-ABUSE-DEFENSE implementado por inteiro (ADR-47), em quatro frentes paralelas

Fechadas todas as sete lacunas do levantamento (G1–G7), mais os dois pedidos que vieram
depois: os tetos viraram configuráveis pelo proprietário e a trilha ganhou um painel de
anomalias. Os PRs 1–5 do faseamento do SDD, mais os 6 e 7 acrescentados no caminho.

Lições, na ordem em que custaram tempo:

1. **A premissa do PR 1 estava errada, e era o único item do SDD não medido.** O documento
   se apresenta como "estado medido, não presumido", e o §4.5 tinha sido inferido de uma
   linha de log: `docker-compose.yml` já traz o default de `TRUSTED_PROXY_IPS` desde o PR3
   do ADR-30, o container em execução tem o valor, e `192.168.107.5` está dentro de
   `192.168.0.0/16`. G5 já estava fechada. A conclusão errada era plausível, citava um
   endereço real e teria gerado um PR que "corrigia" algo correto. O que ficou no lugar é
   um log de boot com o conjunto efetivo — porque "em quem esta instância acredita?" só era
   respondível lendo dois arquivos e o ambiente do container, que foi o que fez a
   investigação demorar.

2. **A mutação achou um bug que nenhuma revisão acharia.** `attemptlimit.gcLocked` media só
   `e.fails`, então um `Release` apagava a entrada inteira de uma chave em modo conjunto —
   e o login libera o slot de IP quando o balde por conta recusa, caminho que o atacante
   dispara à vontade. Martelar uma conta até trancá-la devolveria todo o orçamento de
   largura daquela origem. Teria shipado.

3. **Um teste de integração pode destruir a própria evidência.** `testdb.Shared(t)` não
   devolve só um handle: ele RESETA o banco a cada chamada. O helper `countClicks` o
   chamava inline, truncava `click_log` e contava as linhas que acabara de destruir. As três
   falhas liam como "a coalescência suprimiu tudo, inclusive o primeiro clique" — um
   defeito de produto grave que não existia. O teste que denunciou o diagnóstico foi o do
   gate ausente: ali não há coalescedor nenhum para suprimir nada.

4. **Relatório de frente não é verificação.** A frente 3 reportou que a suíte de integração
   "compilaria e passaria"; ela falhava. A frente 1 não pôde rodar a dela por causa do
   Docker ocupado. Rodar as duas no merge, e mutar o call site central de cada uma, é o que
   transformou "verde" em prova.

5. **Duas frentes acordaram no commit-base errado** (`e700748`, sem o pacote de contrato) e
   as duas se recuperaram sozinhas — mas só porque o brief nomeava o contrato e elas
   notaram a ausência. Fixar o SHA da base no brief.

6. **Um contrato meu criou trabalho inútil em duas frentes.** Eu escrevi `severity: "warn"`
   quando a trilha diz `"warning"`, e a UI ganhou uma função só para traduzir. Dois painéis
   na mesma tela produzindo o mesmo badge por caminhos diferentes é a forma exata do
   INV-159. Unificado no merge e o shim apagado.

7. **`instance.ip_block` estava faltando na união `Permission` do TypeScript desde o
   ADR-46** — a permissão existia no servidor e a grade de papéis não conseguia oferecê-la.
   Nem `tsc` nem `go vet` enxergam isso, porque nenhuma das linguagens sabe que o arquivo
   da outra existe. Entrou `scripts/test-permission-parity.mjs`, nas duas direções.

8. **Provar um guard novo por mutação vale tanto quanto escrevê-lo.** As asserções de
   `limit_req` passaram de primeira, o que é suspeito; colapsar as duas zonas numa só faz o
   guard acusar `"/api/auth/me was throttled (59 of 60) — o polling do próprio SPA divide a
   zona da credencial"`, que é a mensagem certa pelo motivo certo.

Followups abertos:

- **`/api/auth/*` fica fora da cota autenticada**, então `POST /api/auth/refresh` — uma
  transação `SERIALIZABLE`, a escrita autenticada mais cara do pool — segue sem medição;
  ali só existe `attemptlimit`, que conta falhas. O `limit_req` do nginx cobre o grosso.
- **`GET /api/links/url-metadata` faz uma busca externa por chamada e não consome cota**,
  porque a cota conta só verbos mutantes. É o caminho mais barato para um usuário
  autenticado saturar o fetcher. Merece um balde de leitura cara, não uma cota de leitura.
- **O 429 aparece em prosa inglesa numa UI em português.** `apiErrorMessage` devolve a
  mensagem do servidor verbatim por desenho — existe para regras CONFIGURÁVEIS, cujo texto
  do cliente diria o número errado — então isto vale para TODO código de erro do app, não
  só para `rate_limited`. Traduzir só um seria pior que a lacuna honesta; o conserto é um
  mapa código→i18n central, e é uma mudança própria.
- **Screenshots do README** das duas telas novas.

### 2026-08-28 (cont.) — o que o sweep de 5 agentes achou, e o que ele custou

Cinco agentes em paralelo sobre o merge das quatro frentes. **Quatro HIGH**, todos
confirmados por leitura do código antes de qualquer correção, todos corrigidos nesta
sessão com teste vermelho antes.

1. **`CommitSuccess` apagava o conjunto de largura da origem** (Code Review; ninguém mais
   viu). Falhe contra nove contas sob um teto de dez, entre na sua própria, repita para
   sempre — o balde de IP nunca tranca. O controle central do ADR-47 custava **um login**
   para resetar. Corrigido: sucesso zera a contagem escalar e preserva o conjunto.
   ↳ Consertar isso introduziu um segundo bug em três minutos: ao parar de apagar a
   entrada, o `CommitSuccess` parou junto de liberar o `inFlight`, e a chave andava uma
   reserva mais perto de um lockout a cada login bem-sucedido. **Um teste existente pegou.**
   Virou `TestEveryTerminalPathReleasesTheReservation`, para a propriedade ter nome.
2. **O conjunto não tinha janela de tempo** (achado ao consertar o item 1). "Contas
   distintas por origem" estava implementado como "desde o último lockout ou sweep" — sem
   limite superior para uma chave ativa, então dez pessoas errando uma vez cada ao longo de
   uma tarde acumulavam até o teto. É exatamente o falso positivo que o §8 do SDD declara
   como critério de reversão, e ele estava embutido no desenho.
3. **`auth.rate_limited` era declarada, classificada, indexada, consultada, renderizada e
   escrita por NINGUÉM** (achado por três agentes independentes). O sinal mais forte do
   painel não podia disparar, com teste verde: a fixture inseria a linha à mão, então o
   teste provava a consulta e nunca o recurso. Lacuna entre frentes — eu pedi a emissão a
   uma e a leitura a outra, e nenhuma era dona do encontro.
4. **`AbuseObserved.window_days` não existia no fio** (dois agentes). O Go mandava `days`,
   o TypeScript declarava `window_days`, e a tela renderizava "maior valor observado em
   ␣ dias". Passou porque o mock em `src/test/server.ts` foi escrito a partir do TIPO e não
   do servidor: as duas cópias concordavam entre si e com nada real. O `assert.Contains(body,
   "observed")` do teste de integração afirmava existência, não valor.

**Um HIGH de performance, medido e não lido:** o índice `audit_log_ip_time_idx` da migração
000046 **nunca era escolhido**. `EXPLAIN ANALYZE` sobre 1,2 M linhas mostrou as seis
consultas preferindo `audit_log_action_created_idx`, porque todas têm igualdade em `action`
ao lado do intervalo em `created_at`. Custava +43 % em INSERT e 18 MB. A migração foi
apagada antes de shipar — o ADR-47 não tem migração nenhuma.

**Segurança, dois MEDIUM que eram desenho meu:**
- O membro do conjunto era a conta RESOLVIDA, então `alice` e `alice@x.com` colapsavam
  quando a conta existia e não colapsavam quando não existia: 429-vs-401 respondia "este
  username pertence a esta caixa?" para um anônimo. Passou a ser o identificador submetido.
- Os pisos eram por knob e o produto não: `{3 contas, 1440 min}` era legal e trancaria a
  instância inteira por 24 h. `MaxLoginWindowMinutes` caiu para 60.

**Guards novos, todos provados por mutação:** paridade de permissões Go↔TS↔i18n (`instance.
ip_block` estava faltando na união desde o ADR-46, e `instance.rate_limits` sem rótulo em
três locales); e o guard do nginx passou a **rajar cada caminho nomeado na zona apertada**,
porque o comentário dizia "as sete rotas" enquanto duas rotas que gastam recurso estavam na
zona frouxa — uma contagem em prosa não é guard.

**A lição do sweep:** as quatro frentes entregaram verde, com mutação, e o sweep achou
quatro HIGH. Três deles só eram visíveis de fora de uma frente — na costura, no vocabulário
compartilhado, ou no encontro entre quem escreve e quem lê.

Followup adicionado:

- **Um clique suprimido ainda custa uma transação inteira.** O gate remove o `INSERT`, mas
  `Begin` → `SELECT` → `Commit` continuam: 3 round-trips e uma transação com permissão de
  escrita segurando uma das 16 conexões. Sob a inundação que a coalescência existe para
  absorver, o caminho defendido é só ~⅓ mais barato. O conserto NÃO é remover a transação
  às cegas — é mudar a chave do gate do id RESOLVIDO para a referência PÚBLICA (o slug da
  URL), que o middleware já conhece antes da consulta. Com isso a decisão precede o SQL e o
  resolve inteiro vira uma única instrução. Custa reabrir o desenho do `clickctx`, que hoje
  é keyed no id justamente porque o middleware não o tem.

### 2026-08-28 (cont. 2) — a re-revisão do patch achou que dois consertos deslocaram o defeito

Três agentes re-rodados sobre o commit de correção. Segurança: **nenhum HIGH**. Code Review:
**dois**, e os dois eram meus.

1. **A auditoria de lockout continuava sendo um amplificador.** O comentário que eu escrevi
   — "`Begin` recusa uma chave já trancada antes do ramo de falha, então uma expiração
   não-zero aqui é a borda e não uma repetição" — é **falso sob concorrência**, e
   concorrência é o que o atacante controla. `Begin` também GRAVA `lockedUntil` quando
   recusa pelo teto de in-flight, então cada requisição em voo lia de volta uma expiração
   não-zero e gravava uma linha. Reproduzido pelo agente: 3 commits, 1 conta sondada,
   3 linhas. Os commits passam a reportar a BORDA (`edge()`), não o estado.
2. **`CommitSuccess` limpava o `lockedUntil` de uma chave em modo conjunto** — não era
   bypass (o próximo `Begin` re-arma), mas reiniciava o relógio da penalidade com um login
   legítimo do próprio atacante, e contrariava a doutrina que o conserto anterior tinha
   acabado de estabelecer.

**E o Test Quality achou fraquezas nos meus próprios testes**, o que é o resultado mais útil
da rodada:

- `TestEveryTerminalPathReleasesTheReservation` se **auto-neutralizava**: o `Reset` no meio
  apagava a entrada e o vazamento com ela, então dois dos quatro subtestes afirmavam só
  "`Begin` num limitador novo funciona". A segunda tentativa (teto de 1000) tinha o defeito
  oposto — seis slots vazados não alcançam mil. Reescrito para MEDIR o orçamento restante,
  que é a única forma de ver uma reserva vazada. Mutante agora morre.
- Uma asserção estava **duplicada verbatim** e a irmã dela ficou sem piso: é a armadilha do
  `replace_all` que a memória do projeto já registra — dois trechos idênticos, uma edição
  com `count=1`, e as duas mudanças caem na mesma linha. Ambas viraram igualdade EXATA
  (`Equal(max, admitted)`), porque um `LessOrEqual` sozinho passa num limitador que não
  admite nada — a metade "negação de serviço" do mesmo defeito.
- A fixture do teste de oráculo não conferia `RowsAffected`: um `UPDATE` de zero linhas
  faria os dois braços medirem um par que não nomeia nada, e o teste concordaria consigo
  mesmo com o oráculo vivo.
- `quotaAuditor` **não era exercido por nada** — todos os testes usavam um dublê, então
  `return nil` incondicional deixava a suíte verde e o sinal da metade autenticada
  desaparecia em silêncio. É a tese deste próprio ADR se repetindo. Ganhou teste de fiação
  pelo router real; o mutante morre.
- O ramo CARO da cota nunca refusava sob teste (`policyWith(1, 1000)` só alcança o de
  escrita), então apagar a auditoria dali era verde.
- **O guard do nginx que eu escrevi era auto-referencial**: ele extrai a alternância do
  próprio arquivo que audita, então prova "todo caminho NOMEADO é limitado" e nunca "todo
  caminho que deve ser limitado está nomeado" — não pode pegar o defeito que o originou.
  Entrou `TestEdgeZone_EveryAuthPOSTIsClassified`, que caminha o router REAL e exige que
  todo POST de `/api/auth` esteja na zona apertada ou numa lista de exceções com motivo
  escrito. A primeira versão dele, em `internal/server`, iterava sobre ZERO rotas
  (`fullyWiredDeps()` não monta `AuthHandler`) e sobreviveu a apagar `login` da zona — o
  teste ganhou uma asserção de que o walk não é vazio por causa disso.

Correções menores da mesma rodada: `2fa/email/confirm` e `2fa/totp/confirm` entraram na zona
apertada; o 429 passa a ser escrito ANTES da auditoria (a escrita pode esperar até 5 s por um
banco lento); as locales do guard de paridade vêm do diretório e não de uma lista; o
`replace('.', '_')` do `RolesMatrix` trocava só o PRIMEIRO ponto e concordava com o guard por
sorte; e o comentário do `memoryRetain` afirmava que derrubar um balde cedo "é inofensivo,
ele se re-semeia" — verdade para uma sequência escalar, falso para um conjunto de largura.
