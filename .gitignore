# Env / secrets
.env*
!.env.example

# Go
backend/bin/
backend/coverage*.out
backend/cov*.out
backend/foldex
*.test

# Node / Vite
node_modules/
__pycache__/
.scannerwork/
dist/
.vite/
*.local

# The addon embed artifacts are BUILD INPUTS, committed on purpose: the
# backend Docker build context is ./backend, so an ignored dist/ would ship
# binaries whose download route can never serve the extension.
!backend/internal/addon/dist/

# Vitest coverage HTML reports — local-only artifact
web/coverage/

# Extension build
extension/dist/

# IDE
.idea/
.vscode/
*.swp
.DS_Store

# Docker volumes (local data)
.pgdata/
web/certs/

# DB backups produced by scripts/backup.sh
backups/

# Claude Code per-session state and local skills. No negated children: the
# directory is intentionally pruned as a whole.
.claude/

# Playwright MCP artifacts (snapshots, console logs from in-session debugging)
.playwright-mcp/

# Serena MCP project cache (symbol index, session memories) — regenerated on demand
.serena/

# graphify knowledge-graph output — regenerated on demand via `graphify update .`
# (AST-only, no API cost). Treat as cache: keep current locally, never commit.
graphify-out/

# Local audit packs (ultra-deep etc.) — never commit
docs/audit/

# Operator-supplied CA bundles (broker, etc). The directory is mounted into
# the backend and mailer; its contents are local material, never committed.
certs/*
!certs/.gitkeep
docker-compose.override.yml
.worktrees/
